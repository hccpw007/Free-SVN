## 最高优先级规则

> **规则一（最高）：每次对话结束前评估死代码** — 每次对话结束前，都要评估本次对话的修改内容是否产生死代码。如果有，及时清除，不管是否有死代码，都需要告诉我结果。此规则优先于其他所有规则。

> **规则二：每次对话结束前执行 git 提交** — 每次对话结束前，执行 `git add -A && git commit -m "本次修改的中文总结"`，无需询问直接执行。

> **规则三（最高）：引用文件必须读取，不准凭记忆回答** — 在对话中如果要引用文件信息（路径、代码、配置、文档等），必须先读取文件内容。不因担心上下文过长而省略关键信息或跳过必要的文件读取。上下文费用不是约束，输出错误信息的代价远高于 token 消耗。

## 编码准则

1. **严格按执行步骤编码** — 不自行引入设计变更，按步骤文档执行
2. **只写最简代码** — 不引入未请求的特性或抽象层
3. **外科手术式修改** — 不改动不相关的代码
4. **可验证的成功标准** — 定义目标并循环直到确认完成

## AI 编程必须遵循的文档

本项目所有代码和文档由 AI 生成和维护。AI **不得自行设计、引入未规划的变更或超出当前执行步骤的范围**。每步执行的唯一依据是对应的文档或者对话给的明确要求，不得以代码生成便利为由绕过流程。

### 表一：各阶段文档引用

| 时机 | 动作 | 需读取的文档 |
|------|------|-------------|
| **编制/修改设计文档前** | 了解业务规则和编码约束 | 项目规范 |
| **编制/修改开发计划前** | 了解功能范围 | 设计文档（最新版） |
| **编制/修改执行步骤前** | 了解阶段范围和依赖 | 整体开发计划（最新版） |
| **编制/修改执行步骤时** | 逐条核对，确保设计全覆盖 | 设计文档（最新版，对照用） |
| **编码开始前（每阶段首次）** | 掌握规范 + 当前任务 | 项目规范 + 当前步骤文件 |
| **每步骤执行时** | 编码（严格按此执行） | 当前步骤文件（唯一依据） |
| **编码完成（每次对话）** | 质量检查 + 提交 | — |
| **编制评估报告前** | 回顾范围 | 全部被评估文档 |

### 表二：文档目录索引

| 文档类别 | 目录位置 |
|---------|---------|
| 项目规范（4 份） | `doc/01-项目规范/`（含：01-前端规范、02-Rust规范、03-注释规范、**04-文档生成规范**） |
| 设计文档 | `doc/02-设计文档/{序号}-{名称}/{序号}-{名称}-v{N}.md` (最新版) |
| 整体开发计划 | `doc/03-执行计划/01-整体开发计划/01-开发计划/` (最新版) |
| 执行步骤文件 | `doc/03-执行计划/01-整体开发计划/02-执行步骤/phase-{N}/{N}.{M}-描述.md` |

**注：** 所有文档中的代码片段和注释必须遵循 `doc/01-项目规范/` 下的编码规范和注释规范。`。

## 开发命令

```bash
cnpm install           # 安装依赖
cnpm run knip          # 死代码检测（文档使用指引引用了此命令）
cnpm run tauri dev     # 开发模式
cnpm run lint          # 代码检查
cnpm run tauri build   # 构建应用
```