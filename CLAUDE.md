# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 最高优先级规则

每次对话结束前，执行 `git add -A && git commit -m "本次修改的中文总结"`，无需询问直接执行。

## 项目概述

Free-SVN — 基于 Tauri + Vue 的跨平台 SVN 图形化工具。

- **前端**：Vue 3 + TypeScript
- **后端**：Rust (Tauri)
- **目标**：macOS / Windows / Linux 三端支持

## 前端样式规范

仅使用 Tailwind CSS 编写样式，**禁止**写 `<style>` 标签或任何自定义 CSS 文件。

## 编码准则（Karpathy）

1. **先想清楚再编码** — 主动暴露假设和不确定性，不做默认选择
2. **只写最简代码** — 不引入未请求的特性或抽象层
3. **外科手术式修改** — 不改动不相关的代码
4. **可验证的成功标准** — 定义目标并循环直到确认完成

## AI 编程模式

本项目是 **AI 主导编程** 项目，所有代码由 AI 生成和维护。非报告类文档（`doc/design/`、`doc/plan/`、`doc/rule/`）以 **AI 高效消费** 为首要目标，而非人类阅读体验。写作原则：

- **指令驱动** — 直接写 AI 要做什么（"运行 X"、"检查 Y"），不做背景铺垫
- **结构化 > 叙事** — 用表格、列表、流程图替代段落叙述
- **精确 > 友好** — 用具体命令和阈值（"超过 400 行则拆分"），不用模糊建议（"注意控制文件大小"）
- **最小上下文** — 删除场景说明、设计理由、注意事项等对人类有用的修饰内容
- **直接给出规则** — 每条规则要么是"必须"要么是"禁止"，不留灰色地带

报告类文档（`doc/report/`）不受此约束，可以保留分析性、评估性内容供人阅读。

## 文档规范

所有文档存放在 `doc/` 目录下，按类别分目录、以序号开头：

| 目录 | 用途 | 命名示例 |
|------|------|----------|
| `doc/design/` | 项目设计文档 | `01-架构设计.md`、`02-数据库设计/` |
| `doc/plan/` | 开发计划 | `01-开发计划.md`、`02-迭代计划/` |
| `doc/rule/` | 规范、规则、代码解释 | `01-开发规范.md`、`02-代码解释/` |
| `doc/report/` | 评估报告、审查报告 | `01-架构评估报告.md` |

- 所有文档文件名**必须以两位数字序号开头**（如 `01-`、`02-`）
- 当某一主题内容较多时，**创建子文件夹**归类存放（如 `02-数据库设计/schema.md`）
- 文档使用 Markdown 格式

## 开发命令

```bash
# 安装依赖
cnpm install

# 开发模式（热更新）
cnpm run tauri dev

# 构建
cnpm run tauri build

# 前端 lint
cnpm run lint
```
