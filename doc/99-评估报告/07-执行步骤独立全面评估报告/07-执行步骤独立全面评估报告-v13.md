# 07-执行步骤独立全面评估报告 v13

> **评估时间：** 2026-07-01
> **评估对象：** `doc/03-执行计划/01-整体开发计划/02-执行步骤/` 下 Phase-1 ~ Phase-7 全部 **38 个执行步骤**（较 v12 新增 3 个步骤：3.5 认证操作命令、6.10 AuthDialog、7.2 services/svn.ts 虽已存在但之前统计遗漏）
> **评估目标：** AI 编码前最后一次全面审查。确保所有业务需求、视觉设计完整覆盖；确保执行步骤清晰准确、符合开发规范；确保 AI 能够准确编码，不自由发挥、不走样。

---

## 总览

| 项目 | 数量 |
|------|------|
| 阶段总数 | 7 |
| 执行步骤总数 | 38 |
| NPM 依赖安装步骤 | 1 |
| 后端 Rust 步骤 | 17 |
| 前端 Vue/TS 步骤 | 22 |
| 构建验证步骤 | 1 |

**业务设计文档对照版本：** `01-业务功能设计-v5.md`（含 v5 新增认证凭据管理三场景）
**视觉设计文档对照版本：** `02-交互与视觉设计-v5.md`（含 v5 新增认证对话框设计）

---

# 打分表一：业务方案覆盖水平

**评分标准：** 逐项对比 `01-业务功能设计-v5.md` 中的全部业务功能，检查在执行步骤中是否完整覆盖。满分 100 分/项。**扣分说明**列仅在得分 < 100 时出现。

## 1.1 前端功能覆盖

| 业务功能 | 设计文档章节 | 对应执行步骤 | 覆盖情况 | 得分 | 扣分说明 |
|---------|-------------|-------------|---------|:---:|---------|
| 欢迎页空状态（3 种状态） | §5.1 | 6.1、6.2 | 完全覆盖：欢迎页/无变更/搜索无结果三态均已实现 | **100** | |
| 变更文件列表 | §4.4 | 6.1、6.2 | 完全覆盖：FileListTable + FileStatusIcon + FileActionButtons 均已定义 | **100** | |
| 文件状态图标（6 种） | §4.4 | 6.2 | 完全覆盖：M/✚/!/🗑/?/- 六种状态 + 冲突行指示条 | **100** | |
| 文件操作按钮按状态组合 | §4.4 表格 | 6.2 | 完全覆盖：6 种状态各自的操作按钮组合均已定义 | **100** | |
| 文件行右键菜单（4 项） | §4.4 | 6.2 | 完全覆盖：复制路径/复制相对路径/在文件管理器中显示/用外部编辑器打开 | **100** | |
| 搜索与筛选 | §4.4 | 6.1、6.2 | 完全覆盖：300ms 防抖 + 筛选下拉 + 过滤实现 | **100** | |
| 排序（冲突优先） | §4.4 | 6.2 | 完全覆盖：默认排序规则 + 列头排序 | **100** | |
| 多选交互（Shift+Ctrl+Click） | §4.4 | 7.1（useFileSelection.ts） | 已定义 composable 封装，具体实现由 useFileSelection 完成 | **100** | |
| 差异对比面板（diff2html） | §5.3 | 6.3 | 完全覆盖：diff2html 渲染 + 导航 + 搜索 + 拖拽 + 二进制文件处理 | **100** | |
| 提交面板 + 选择状态传递 | §5.4 | 6.4 | 完全覆盖：三条选择规则 + CommitForm + 提交流程 | **100** | |
| 日志视图（分页+展开详情） | §5.5 | 6.5 | 完全覆盖：虚拟滚动 + 搜索 + 分页 + 展开详情 | **100** | |
| 设置页面（4 组 + 关于） | §5.7 | 6.9 | 完全覆盖：SVN/差异合并/界面/通用 + 关于区 + 导出日志 | **100** | |
| 底部状态栏（三段信息） | §4.5 | 5.4 | 完全覆盖：版本号/最新提交/SVN版本 + 版本号点击复制 | **100** | |
| 顶部状态栏（路径+切换+分支+暗色） | §4.2 | 5.2 | 完全覆盖：路径中间省略 + 切换下拉 + 分支名标签 + 暗色切换 + 凭据管理按钮 | **100** | |
| SVN 工具栏（11 按钮 + 溢出） | §4.3 | 5.3 | 完全覆盖：按钮禁用条件 + 溢出折叠 + 设置按钮 | **100** | |
| ProgressOverlay + 取消 | §4.5 | 5.5 | 完全覆盖：全屏遮罩 + 进度条 + 阶段文字 + 取消按钮 | **100** | |
| 空状态：搜索无结果 | §4.4 | 6.1 | 完全覆盖：3 种空状态互斥处理 | **100** | |
| 自动刷新策略（focus 等） | §4.4 | 7.1（useAutoRefresh.ts） | 已定义 composable，但 window.focus 事件在 Tauri 上下文中可能需改用 @tauri-apps/api/window 的 onFocusChanged | **95** | useAutoRefresh 使用 DOM window.focus 事件，在 Tauri WebView 中当应用窗口从后台切回时可能不触发。建议改用 `@tauri-apps/api/window` 的 `onFocusChanged` 或 `listen('tauri://focus')` |
| 系统托盘 | §3.4 | 7.3 | 完全覆盖：托盘图标 + 右键菜单 + 后台操作 + 系统通知 | **100** | |
| 差异对比面板标题栏 | §5.3 | 6.3 | 完全覆盖：文件名 + 版本号 + 作者 + 搜索/外部工具/复制路径/关闭按钮 | **100** | |
| Diff 面板大文件分片加载方案 | §5.3 | 6.3 | 完全覆盖：提供了 splitDiffByFile + requestAnimationFrame 分片渲染备选方案 | **100** | |

## 1.2 SVN 操作功能覆盖

| 业务功能 | 设计文档章节 | 对应执行步骤 | 覆盖情况 | 得分 | 扣分说明 |
|---------|-------------|-------------|---------|:---:|---------|
| status（文件变更列表） | §6.1 | 3.1 | 完全覆盖：get_status 命令 + parser.parse_status | **100** | |
| info（工作副本信息） | §6.1 | 3.1 | 完全覆盖：get_info 命令 + parser.parse_info | **100** | |
| checkout（检出） | §5.6、§6.1 | 3.2、6.6 | 完全覆盖：含 credentials 参数 + 进度推送 + 可取消 | **100** | |
| commit（提交） | §6.1 | 3.2、6.4 | 完全覆盖：原子操作不可取消 + 进度反馈 | **100** | |
| update（更新） | §6.1 | 3.2 | 完全覆盖：含 revision 参数 + 进度推送 + 可取消 | **100** | |
| diff（差异） | §6.1 | 3.1 | 完全覆盖：二进制文件检测 + 非文本处理 | **100** | |
| log（日志） | §6.1 | 3.1 | 完全覆盖：limit/search/revision 参数 + --xml 解析 | **100** | |
| blame（注解） | §6.1 | 3.1 | 完全覆盖：120s 单独超时 | **100** | |
| add（添加） | §6.1 | 3.2 | 完全覆盖：批量路径处理 | **100** | |
| delete（删除） | §6.1 | 3.2 | 完全覆盖：含 keepLocal 参数 | **100** | |
| revert（还原） | §6.1 | 3.2 | 完全覆盖：批量文件还原 | **100** | |
| resolve（冲突解决） | §5.11、§6.1 | 6.8 | 完全覆盖：mine-full/theirs-full/working 三种 resolution + 批量处理 | **100** | |
| switch（切换） | §5.8、§6.1 | 3.3、6.7 | 完全覆盖：长操作可取消 + 进度推送 | **100** | |
| copy（分支/标签） | §5.9、§6.1 | 3.3、6.7 | 完全覆盖：含 URL 校验 + revision 参数 | **100** | |
| merge（合并） | §5.10、§6.1 | 3.3、6.7 | 完全覆盖：4 步向导 + 可取消 + 冲突列表返回 | **100** | |
| cleanup（清理） | §6.1 | 3.3 | 完全覆盖 | **100** | |
| export（导出） | §5.12、§6.1 | 3.3、6.6 | 完全覆盖：可取消 + 进度推送 | **100** | |
| ignore（忽略） | §6.1 | 3.2、6.8 | 完全覆盖：4 种忽略模式 + 读取已有列表去重追加 | **100** | |
| relocate（重定位） | §6.1 | 3.3 | 完全覆盖：URL 格式校验 + check_network | **100** | |
| property（属性） | §6.1 | 3.3 | 完全覆盖：3 种 action（get/set/delete） | **100** | |
| lock/unlock（锁定） | §6.1 | 3.3 | 完全覆盖：批量路径 + message 可选参数 | **100** | |
| cancel_operation（取消） | §2.5 | 3.4 | 完全覆盖：AtomicBool + kill 子进程 + svn cleanup + 事件推送 | **100** | |
| 网络检测 | §2.4 | 2.2、3.4、7.1 | 完全覆盖：服务端操作前检测 + check_network_command | **100** | |
| get_logs/export_logs（日志导出） | §6.1 | 3.4 | 完全覆盖：纯文本返回 + ZIP 打包 + 系统信息 | **100** | |
| status 缓存策略（写操作期间） | §2.4 | 4.2（fileListStore） | 完全覆盖：lastCachedFiles + refresh() 判断 isOperationRunning | **100** | |
| 工作副本路径有效性检测 | §4.4 | 6.1 | 完全覆盖：onMounted 中 Tauri fs exists() 检查，不存在时 ElMessage + 切回欢迎页 | **100** | |
| 外部工具调用（差异/合并） | §5.7 | 2.5 | 完全覆盖：vscode/bcomp/kaleidoscope/自定义四种 + 合并工具三种 + 回退策略 | **100** | |
| svn 版本号检测 | §2.3 | 2.5（resolver.rs） | 部分覆盖：resolver.rs 中定义了 get_svn_version()，但尚未包装为 Tauri command 注册到 generate_handler! | **85** | SettingsPage（6.9）中调用 `invoke('get_svn_version')`，但后端没有对应的 Tauri command 注册。`tools/resolver.rs` 的 `get_svn_version()` 函数未通过 `commands/` 暴露为 `#[tauri::command]`。需在 `commands/settings.rs` 或新建 `commands/version.rs` 中添加 `get_svn_version` 命令并注册到 lib.rs |

## 1.3 认证凭据管理覆盖（v5 新增）

| 业务功能 | 设计文档章节 | 对应执行步骤 | 覆盖情况 | 得分 | 扣分说明 |
|---------|-------------|-------------|---------|:---:|---------|
| 检出对话框认证输入区 | §5.6、§2.2 | 6.6 | 完全覆盖：用户名/密码/保存凭据 + 校验逻辑 | **100** | |
| 认证重试对话框 | §5.14 | 6.10 | 完全覆盖：retry 模式 + 状态提示 + 重试/取消 | **100** | |
| 凭据管理对话框 | §5.15 | 6.10 | 完全覆盖：manage 模式 + 测试连接 + 清除缓存 | **100** | |
| 密码 stdin 管道传递 | §2.2、§6.2 | 2.2（executor.rs） | 完全覆盖：明文禁止 + stdin 管道 + 日志掩码 | **100** | |
| SVN_AUTH_FAILED 错误检测 | §2.2、§6.2 | 2.2（executor.rs） | 完全覆盖：AUTH_ERROR_KEYWORDS 关键词检测 + realm 提取 | **100** | |
| 前端认证失败→AuthDialog 联动 | §8.3 | 4.2（svnStore）+ 6.10 | 完全覆盖：call() 自动捕获 authFailed/authContext + watch 触发 + retryAuth | **100** | |
| 凭据安全规范（日志掩码等） | §2.8 | 2.2（executor.rs） | 完全覆盖：日志中密码掩码为 --password-from-stdin [***] | **100** | |
| credentials 参数贯通 | §2.2 | 3.1~3.5 | 完全覆盖：所有服务端命令均含 credentials 可选参数 | **100** | |
| test_connection 命令 | §5.15、§6.2 | 3.5（auth.rs） | 部分覆盖：功能已实现，但存在编译问题 | **85** | 3.5 auth.rs 中 `test_connection` 函数使用了 `AppError::SvnExecFailed(...)` 构造函数，但 `models/error.rs` 的 AppError 枚举中只有 `SvnCommand(String)` 而没有 `SvnExecFailed` 变体。这会导致编译错误。应改为 `AppError::SvnCommand(...)` |
| 会话凭据池 | §2.8 | 4.2（svnStore） | 完全覆盖：retryAuth 保存 credentials，重启清空 | **100** | |

## 1.4 系统集成覆盖

| 业务功能 | 设计文档章节 | 对应执行步骤 | 覆盖情况 | 得分 | 扣分说明 |
|---------|-------------|-------------|---------|:---:|---------|
| 系统右键菜单（3 平台） | §3.1~§3.3 | 7.4 | 完全覆盖：macOS/Windows/Linux + 菜单结构 + 条件菜单 | **100** | |
| 系统托盘 | §3.4 | 7.3 | 完全覆盖：托盘菜单 + 后台操作 + 系统通知 | **100** | |
| 单实例防护 | §3.5 | 7.3 | 完全覆盖：tauri-plugin-single-instance + --svn-cmd 参数传递 | **100** | |
| 窗口状态持久化 | §3.6 | 7.3 | 完全覆盖：tauri-plugin-window-state | **100** | |
| 开机自启动 | §5.7 | 6.9、7.3 | 完全覆盖：SettingsPage 开关 + set_auto_start 命令（3 平台实现） | **100** | v12 评估中此项为 90 分（缺少 Rust 端实现），v13 中 7.3 已补充完整的 set_auto_start 三平台代码 |
| 键盘快捷键（全） | §4.4 | 7.1、6.3 | 完全覆盖：useKeyboardShortcuts composable + diff 面板快捷键 + AuthDialog Enter | **100** | |
| --svn-cmd 参数跨平台传递 | §3.2 | 7.3、7.4 | 完全覆盖：三平台参数占位符 + 单实例传递 + 前端 listen | **100** | |
| 关闭按钮→最小化到托盘 | §3.4 | 7.3 | 完全覆盖：onCloseRequested → hide() | **100** | |
| 操作中托盘徽章 | §3.4 | 7.3 | 完全覆盖：set_tray_badge 命令 + macOS Dock badge | **100** | |
| 后台操作系统通知 | §3.4 | 7.3 | 完全覆盖：tauri-plugin-notification 后端直接通知方案 | **100** | |

**表一小计：均分 99.1，最低分 85（svn 版本号检测命令未注册、auth.rs 编译错误）**

---

# 打分表二：视觉设计覆盖水平

**评分标准：** 逐项对比 `02-交互与视觉设计-v5.md` 中的全部视觉设计要求。满分 100 分/项。

| 视觉设计要求 | 设计文档章节 | 对应执行步骤 | 覆盖情况 | 得分 | 扣分说明 |
|-------------|-------------|-------------|---------|:---:|---------|
| MainLayout 四层布局 | §2.1 | 5.1 | 完全覆盖：TopBar(48px)+ToolBar(40px)+RouterView+StatusBar(28px) | **100** | |
| 欢迎页模式隐藏 ToolBar/StatusBar | §2.1 | 5.1 | 完全覆盖：isWelcomePage computed 判断（route.name + currentPath） | **100** | |
| TopBar 路径中间省略 + 悬停 tooltip | §2.1 | 5.2 | 完全覆盖：CSS truncate 保留首尾各 25/20 字符 + title | **100** | |
| TopBar 右侧 🔑 凭据管理按钮 | §2.1（v5 新增） | 5.2 | 完全覆盖：KeyRound 图标 16px + p-1.5 + rounded-md | **100** | |
| 暗色切换 Moon/Sun 图标 | §2.1 | 5.2 | 完全覆盖 | **100** | |
| 欢迎页居中布局（精确结构） | §3.1 | 6.1 | 完全覆盖：欢迎标题 + 副标题 + 2 个按钮 + 最近列表 | **100** | |
| 变更文件列表精确定位 | §3.2 | 6.2 | 完全覆盖：搜索/筛选/表格/底栏 | **100** | |
| 文件状态色（亮色/暗色） | §3.2 | 6.2 | 完全覆盖：blue/green/red/amber/slate/violet + 暗色变体 | **100** | |
| 操作按钮颜色 + hover 下划线 | §3.2 交互说明表 | 6.2 | 完全覆盖：各按钮颜色按规范定义 + hover 下划线 + 颜色加深 | **100** | |
| 全选底栏粘性 | §3.2 | 6.2 | 完全覆盖：shrink-0 固定底栏 | **100** | |
| 差异面板标题栏 + 拖拽调整 | §3.3 | 6.3 | 完全覆盖：标题栏(文件名/版本/[外部工具]/复制路径/✕关闭) + 拖拽(120px~70%) | **100** | |
| diff2html 渲染 + 语法高亮 | §3.3 | 6.3、1.3 | 完全覆盖：style.css import diff2html + highlight.js | **100** | |
| 差异面板逐块导航 + Ctrl+F/Up/Down | §3.3 | 6.3、7.1 | 完全覆盖 | **100** | |
| 二进制文件差异提示 | §3.3 | 6.3 | 完全覆盖："该文件为二进制文件，无法显示差异" | **100** | |
| 提交面板选择状态三条规则 | §3.4 | 6.4 | 完全覆盖：继承/独立/清除 | **100** | |
| 提交日志输入框 80px~200px | §3.4 | 6.4 | 完全覆盖 | **100** | |
| 72 字符/行超长警告 | §3.4 | 6.4 | 完全覆盖：amber 边框警告 | **100** | |
| 最近日志下拉（20 条） | §3.4 | 6.4 | 完全覆盖 | **100** | |
| 日志视图虚拟滚动 | §3.5 | 6.5 | 完全覆盖：El-table-v2 + 分页 | **100** | |
| 检出对话框精确布局（v5 含认证区） | §3.7 | 6.6 | 完全覆盖：URL 校验 + 路径选择 + 深度 + 认证区始终展开 | **100** | |
| 认证重试对话框视觉（440px） | §3.7（v5 新增） | 6.10 | 完全覆盖：440px + 状态提示颜色 + 图标 | **100** | |
| 凭据管理对话框视觉（480px） | §3.7（v5 新增） | 6.10 | 完全覆盖：480px + 底部按钮左右分布 + 状态提示 | **100** | |
| 密码👁显示/隐藏按钮交互 | §3.7 | 6.10 | 完全覆盖：aria-label 随状态切换 + 图标切换 | **100** | |
| 合并向导 4 步进度条 | §3.7 | 6.7 | 完全覆盖：①→②→③→④ 步骤条 + ✅ 完成标记 | **100** | |
| 冲突解决界面 | §3.7 | 6.8 | 完全覆盖：差异查看 + 使用我的/他们的 + 标记已解决 + 外部合并 | **100** | |
| 设置页 4 个 El-card 分组 | §3.6 | 6.9 | 完全覆盖 | **100** | |
| 认证操作类 loading 态 | §4.5 | 6.10 | 完全覆盖：按钮 spinner + 内嵌状态提示 + 禁用输入 | **100** | |
| 对话框入场动画 | §1.8 | 6.6~6.10 | 完全覆盖：所有 el-dialog 步骤已添加注释说明内置 200ms scale+fade 动画 | **100** | v12 扣分项（95 分）已修复 |
| dark: 变体暗色适配 | §5 | 全组件 | 完全覆盖：每个组件模板中均有 `dark:` 变体 | **100** | |
| 暗色 Diff 行配色 | §5.3 | 6.3 | 完全覆盖：dark:bg-red-900/30 删除行、dark:bg-green-900/30 新增行 | **100** | |
| 无障碍 aria-label | §6 | 各组件 | 完全覆盖：操作按钮 aria-label + 密码切换 aria-label + role="alert" | **100** | |
| 图标颜色双通道 | §6 | 6.2 | 完全覆盖：FileStatusIcon 使用图标+颜色双通道 | **100** | |
| focus:ring 焦点指示 | §6 | 多步骤 | 完全覆盖：所有交互按钮/输入框已添加 focus:ring 类 | **100** | v12 扣分项（90 分）已修复 |
| prefers-reduced-motion | §6 | 7.5 | 完全覆盖：motion-safe/motion-reduce 变体 + style.css 全局降级 | **100** | |
| 文本缩放（相对单位） | §6 | 全组件 | 完全覆盖：全部使用 Tailwind text-sm/text-xs 等相对单位 | **100** | |

**表二小计：均分 100，最低分 100（无扣分项，v12 中的 3 个扣分项均已修复）**

---

# 打分表三：AI 编程适配水平

**评分标准：** 逐步骤评估 AI 是否可以仅依据此步骤文件完成精确编码。检查字段完整性（涉及文件、操作细节、验收标准）、描述精确度、代码完整度。满分 100 分。

## Phase-1：项目初始化

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 1.1 后端 Rust 骨架 | **✅** 20 个文件明细+操作类型 | **✅** 完整代码块+模板文件 | **✅** 5 条具体标准 | **✅** | **100** | |
| 1.2 前端依赖安装 | **✅** 3 个项 | **✅** shell 命令+版本表 | **✅** 4 条可执行命令验证 | **✅** | **100** | |
| 1.3 前端骨架完善 | **✅** 9 个文件 | **✅** 完整 CSS/JSON/TS 代码 | **✅** 7 条验证 | **✅** | **100** | |
| 1.4 构建验证 | **✅**（验证模式） | **✅** shell 命令+排查指引 | **✅** 4 条 + 可选 tauri dev | **✅** | **100** | |

## Phase-2：Rust 后端核心

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 2.1 数据模型与共享类型 | **✅** 5 个文件 | **✅** 完整 Rust 结构体代码 | **✅** 6 条 | **✅** | **100** | |
| 2.2 SVN 执行核心 | **✅** 1 个文件 | **✅** 完整 executor.rs 代码 | **✅** 10 条详细标准 | **✅** | **100** | |
| 2.3 XML 解析层 | **✅** 1 个文件 | **✅** 完整 serde 反序列化结构体 + 6 组解析函数 | **✅** 7 条 | **✅** | **100** | |
| 2.4 配置持久化与日志系统 | **✅** 4 个文件 | **✅** 完整 store.rs/logger.rs 代码 | **✅** 7 条 | **✅** | **100** | |
| 2.5 外部工具管理模块 | **✅** 4 个文件 | **✅** 完整工具调用封装代码 | **✅** 7 条 | **✅** | **100** | |

## Phase-3：Rust Command 层

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 3.1 只读操作命令 | **✅** 4 个文件 | **✅** 完整代码（status/diff/log/blame） | **✅** 7 条 | **✅** | **100** | |
| 3.2 核心写操作命令 | **✅** 6 个文件 | **✅** 完整代码（checkout/update/commit/file_ops/ignore） | **✅** 9 条 | **✅** | **100** | |
| 3.3 剩余功能操作命令 | **✅** 6 个文件 | **✅** 完整代码（switch/copy/merge/cleanup/lock/relocate/property） | **✅** 8 条 | **✅** | **100** | |
| 3.4 系统操作与 lib.rs 注册 | **✅** 4 个文件 | **✅** 完整代码（cancel/logs/network/settings/branches + generate_handler!） | **✅** 9 条 | **✅** | **95** | 步骤完成后因 mod.rs 含 `pub mod auth;` 但 auth.rs 未创建导致编译错误——已在步骤中注释说明属预期行为，但未包含 auth.rs 后重新编译验证的确认步骤 |
| 3.5 认证操作命令 auth.rs | **✅** 2 个文件 | **❌** test_connection 中使用未定义的 AppError::SvnExecFailed 变体 | **✅** 8 条 | **✅** | **85** | **严重编译错误**：`test_connection` 函数引用了 `AppError::SvnExecFailed`，但 `models/error.rs` 的 AppError 枚举中没有此变体。正确的变体应为 `AppError::SvnCommand(String)`。此错误会导致 cargo build 编译失败。需将代码中所有 `AppError::SvnExecFailed(...)` 替换为 `AppError::SvnCommand(...)` |

## Phase-4：前端基础架构

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 4.1 前端类型定义 | **✅** 4 个 TS 文件 | **✅** 完整 TypeScript 代码 | **✅** 5 条 | **✅** | **100** | |
| 4.2 Pinia 状态管理与服务层 | **✅** 5 个 TS 文件 | **✅** 完整 Pinia Store 代码 | **✅** 11 条详细 | **✅** | **100** | |
| 4.3 路由配置与国际化增强 | **✅** 6 个文件 | **✅** 完整路由 + DiffPage + locale JSON 代码 | **✅** 9 条 | **✅** | **100** | |

## Phase-5：前端布局与通用组件

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 5.1 MainLayout | **✅** 1 个文件 | **✅** 完整 Vue 组件代码 | **✅** 8 条 | **✅** | **100** | |
| 5.2 TopBar | **✅** 1 个文件 | **✅** 完整 Vue 组件代码 + 交互逻辑 | **✅** 11 条 | **✅** | **100** | |
| 5.3 ToolBar | **✅** 1 个文件 | **✅** 完整 Vue 组件代码 + 溢出策略 + 禁用逻辑 | **✅** 12 条 | **✅** | **100** | |
| 5.4 StatusBar | **✅** 1 个文件 | **✅** 完整 Vue 组件代码 | **✅** 5 条 | **✅** | **100** | |
| 5.5 ProgressOverlay | **✅** 1 个文件 | **✅** 完整 Vue 组件代码 | **✅** 7 条 | **✅** | **100** | |

## Phase-6：页面与功能组件

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 6.1 HomePage | **✅** 4 个文件 | **✅** 完整代码（欢迎页/变更列表/空状态/路径检测） | **✅** 11 条 | **✅** | **100** | |
| 6.2 FileListTable 系列 | **✅** 5 个文件 | **✅** 完整代码（表格/状态图标/操作按钮/右键菜单/虚拟滚动） | **✅** 14 条 | **✅** | **100** | |
| 6.3 DiffViewer | **✅** 1 个文件 | **✅** 完整组件代码 + 大文件分片方案 | **✅** 13 条 | **✅** | **100** | |
| 6.4 CommitPage | **✅** 3 个文件 | **✅** 完整组件代码（提交面板/表单/提交流程） | **✅** 11 条 | **✅** | **100** | |
| 6.5 LogPage | **✅** 3 个文件 | **✅** 完整组件代码（日志列表/详情/搜索/分页） | **✅** 10 条 | **✅** | **100** | |
| 6.6 对话框组一 | **✅** 4 个文件 | **✅** 完整代码（Checkout/UpdateRevision/Export/Confirm） | **✅** 7 条 | **✅** | **100** | |
| 6.7 对话框组二 | **✅** 3 个文件 | **✅** 完整代码（Switch/BranchTag/MergeWizard） | **✅** 6 条 | **✅** | **100** | |
| 6.8 对话框组三 | **✅** 2 个文件 | **✅** 完整代码（ResolveConflict/IgnoreDialog） | **✅** 3 条 | **✅** | **100** | |
| 6.9 SettingsPage | **✅** 1 个文件 | **✅** 完整代码（4 组设置/保存校验/关于） | **✅** 7 条 | **✅** | **95** | SettingsPage 中调用 `invoke('get_svn_version')`，但后端没有注册 `get_svn_version` Tauri command（见表一 1.2 扣分项）。需先在 `commands/settings.rs` 或新文件添加 get_svn_version 命令 |
| 6.10 AuthDialog | **✅** 3 个文件 | **✅** 完整代码（retry/manage 双模式 + SVN_AUTH_FAILED 联动） | **✅** 9 条 | **✅** | **100** | |

## Phase-7：系统集成与收尾

| 步骤 | 涉及文件完整 | 操作细节完整 | 验收标准可验证 | 描述无省略 | 总分 | 扣分说明 |
|-----|:----------:|:----------:|:------------:|:---------:|:---:|---------|
| 7.1 Composables | **✅** 5 个文件 | **✅** 完整代码结构 | **✅** 6 条 | **✅** | **95** | useAutoRefresh 使用 DOM `window.focus` 事件。在 Tauri 2 中，当应用窗口从后台切回时，DOM 的 focus 事件可能不会触发。应改用 `@tauri-apps/api/window` 的 `onFocusChanged` 或 `listen('tauri://focus')` |
| 7.2 services/svn.ts 完整封装 | **✅** 1 个文件 | **✅** 完整 invoke 封装 + 31 个方法 + 3 个 event listener | **✅** 6 条 | **✅** | **100** | |
| 7.3 lib-setup 与系统托盘 | **✅** 4 个文件 | **✅** 完整 Rust 代码（插件注册/托盘/单实例/自启动） | **✅** 12 条 | **✅** | **100** | |
| 7.4 Shell 右键菜单 | **✅** 3 个文件 | **✅** 完整三平台实现（macOS workflow / Windows winreg / Linux Nautilus） | **✅** 6 条 | **✅** | **100** | |
| 7.5 暗色模式与无障碍 | **✅** 2 个文件 | **✅** 具体实现说明 + 26 个组件的完整 dark 适配清单 + 无障碍清单 | **✅** 6 条 | **✅** | **100** | |
| 7.6 最终构建验证 | **✅**（验证模式） | **✅** 完整验证命令 + clippy 策略 + 修复指引 | **✅** 7 条 | **✅** | **100** | |

**表三小计：均分 99.2，最低分 85（3.5 auth.rs 编译错误）+ 95（多处）**

---

# 打分表四：多语言适配水平

**评分标准：** 逐步骤评估 i18n 完整度。检查 locale JSON 定义、组件中使用 `t()`、ja/ko 回退策略。满分 100 分。

| 步骤 | i18n key 定义 | 组件使用 t() | ja/ko 处理 | 备注 | 得分 | 扣分说明 |
|-----|:-----------:|:----------:|:---------:|:----|:---:|---------|
| 1.3 前端骨架 | **✅** error(13) + common(10) + app(2) | **✅** t() 使用 | **✅** 英文回退占位 | 4 个语言包 JSON 均已创建 | **100** | |
| 4.1 类型定义 | **✅** ErrorCode 枚举 + ERROR_MESSAGE_KEYS + FALLBACK 中/英 | N/A 纯 TS 类型 | — | 回退链已完善 | **100** | |
| 4.2 Pinia Store | **✅** svnStore 中常量定义 | 错误码翻译通过 t() | — | | **100** | |
| 4.3 国际化增强 | **✅** 5 个分类完整 JSON + langMap + 语言切换联动 | **✅** locale.value 联动 | **✅** ja/ko 英文回退 + Coming Soon 标记 | | **100** | |
| 5.1 MainLayout | **✅** 无直接 i18n | — | — | 纯布局 | **100** | |
| 5.2 TopBar | **✅** common.credentialManagement / common.switchToLightMode / common.switchToDarkMode / common.copyFailed 等 | **✅** t() 用于按钮/aria-label/tooltip | **✅** 回退链 | | **100** | |
| 5.3 ToolBar | **✅** toolbar.* 11 个 key + disabled tooltip 5 个 key | **✅** t() 用于标签/aria-label/tooltip | **✅** 回退链 | | **100** | |
| 5.4 StatusBar | **✅** statusBar.* 5 个 key（含参数插值） | **✅** t() + 参数插值 | **✅** 回退链 | | **100** | |
| 5.5 ProgressOverlay | **✅** progress.* 4 个 key + common.cancel | **✅** t() 用于全部文字 | **✅** 回退链 | | **100** | |
| 6.1 HomePage | **✅** workspace.* + common.* 约 20 个 key | **✅** 全部使用 t() | **✅** 回退链 | | **100** | |
| 6.2 FileListTable 系列 | **✅** file.*（status 6 + action 6）+ workspace.* + 右键菜单 4 项 | **✅** 状态文字/操作按钮/提示均使用 t() | **✅** 回退链 | | **100** | |
| 6.3 DiffViewer | **✅** file.binaryFileHint + file.diffPrev/Next + common.* | **✅** t() 用于全部按钮/提示 | **✅** 回退链 | | **100** | |
| 6.4 CommitPage | **✅** workspace.selectFiles + file.* + common.* + error.* 新 6 个 key | **✅** t() 用于全部文字 | **✅** 回退链 | | **100** | |
| 6.5 LogPage | **✅** log.* 6 个 key + workspace.* 4 个 key | **✅** t() 用于全部文字 | **✅** 回退链 | | **100** | |
| 6.6 对话框组一 | **✅** dialog.* 约 20 个 key + auth.* 4 个 + error.* 2 个 | **✅** t() 用于全部文字/aria-label/tooltip | **✅** 回退链 | | **100** | |
| 6.7 对话框组二 | **✅** dialog.* + workspace.* + common.* + merge.* | **✅** t() 用于全部步骤标题/按钮/说明 | **✅** 回退链 | | **100** | |
| 6.8 对话框组三 | **✅** dialog.* + file.* | **✅** t() 用于全部文字 | **✅** 回退链 | | **100** | |
| 6.9 SettingsPage | **✅** settings.*（约 25 个 key）+ common.* | **✅** 全部使用 t() + langMap | **✅** ja/ko Coming Soon 标记 | | **100** | |
| 6.10 AuthDialog | **✅** auth.*（18 个 key 完整） | **✅** 全部使用 t() + aria-label | **✅** | | **100** | |
| 7.1~7.2 | 主要为 composables/后端/构建验证，无 UI 文案 | — | — | 仅在 7.5 无障碍中提及 aria-label key（已完成） | **100** | |
| 7.3~7.4 | 系统托盘菜单使用英文（操作系统级 UI，不切换语言） | — | — | 设计合理 | **100** | |
| 7.5 暗色与无障碍 | **✅** 18 个 aria-label i18n key 均已列出并在语言包中定义 | **✅** t() 用于全部 aria-label | — | | **100** | |

**表四小计：均分 100，最低分 100**

---

# 打分表五：开发规范适配水平

**评分标准：** 逐步骤评估是否符合 `01-Vue前端开发规范.md` 和 `02-Rust后端开发规范.md`。满分 100 分。

## Phase-1

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 1.1 Rust 骨架 | **✅** 完全符合后端规范：模块结构(§01)、mod.rs 只做声明(§06) | 无 | **100** | |
| 1.2 前端依赖 | **✅** 使用 cnpm、按规范技术栈表安装 | 但缺少 `@tauri-apps/plugin-store` npm 包安装（CommitForm 6.4B 中 `import { Store } from '@tauri-apps/plugin-store'` 需要此包） | **90** | 步骤 6.4B 中 CommitForm 和 CommitPage 从 `@tauri-apps/plugin-store` 导入 `Store` 用于持久化最近提交信息，但步骤 1.2 的 cnpm install 命令中没有包含此 npm 包。需追加 `@tauri-apps/plugin-store@^2.x` |
| 1.3 前端骨架 | **✅** strict:true、仅 Tailwind、App.vue 仅 RouterView、i18n fallbackLocale | 无 | **100** | |
| 1.4 构建验证 | **✅** cnpm run lint + cnpm run build + cargo build | 无 | **100** | |

## Phase-2

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 2.1 数据模型 | **✅** thiserror(§03)、AppError Serialize、serde derive、PascalCase 命名(§06) | 无 | **100** | |
| 2.2 SVN 执行核心 | **✅** spawn_blocking + 60s 超时(§04)、validate_path(§05)、AtomicBool 取消机制 | 无 | **100** | |
| 2.3 XML 解析层 | **✅** serde 反序列化、禁止字符串正则匹配(§04)、解析失败返回 SVN_PARSE_FAILED | 无 | **100** | |
| 2.4 配置/日志 | **✅** tauri-plugin-store、try_deserialize + 损坏恢复(§05.2)、日志轮转 5MB×3(§05.1) | 无 | **100** | |
| 2.5 外部工具 | **✅** 不设超时、不捕获输出、分离进程(工具调用规范)、which crate 路径检测 | 无 | **100** | |

## Phase-3

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 3.1 只读操作 | **✅** async fn(§02)、Deserialize 结构体参数、Result<T,AppError>、仅做参数校验+调用 | 无 | **100** | |
| 3.2 核心写操作 | **✅** queue.try_lock/unlock(§04 并发控制)、长操作 Tauri event 推送(§02)、credentials 参数 | 无 | **100** | |
| 3.3 剩余操作 | **✅** 同 3.2、merge 后读取 status 获取冲突数 | 无 | **100** | |
| 3.4 系统操作 | **✅** cancel_operation 绕过锁(特殊操作)、generate_handler! 注册(§02) | 无 | **100** | |
| 3.5 认证操作 | **❌** 密码通过 stdin(§05 安全规范)符合规范；但 test_connection 使用了未定义的 AppError 变体 | **85** | 使用未定义的 `AppError::SvnExecFailed` 是编译错误，违反 Rust 类型安全规范。需改为 `AppError::SvnCommand(String)` |

## Phase-4

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 4.1 类型定义 | **✅** PascalCase 接口(§03)、camelCase 字段(§06)、types/ 目录按模块分(§03)、禁止 any | 无 | **100** | |
| 4.2 Pinia/Services | **✅** Setup Store 语法(§05)、一个 store 一个 defineStore、分层规范 | 无 | **100** | |
| 4.3 路由/i18n | **✅** 懒加载(§05)、kebab-case 路由 path、vue-i18n + fallbackLocale | 无 | **100** | |

## Phase-5

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 5.1 MainLayout | **✅** `<script setup>`(§02)、仅 Tailwind(§04)、无 `<style>` 标签 | 无 | **100** | |
| 5.2 TopBar | **✅** Composition API、defineProps + defineEmits 类型声明、无 defineExpose、Lucide 图标 | 无 | **100** | |
| 5.3 ToolBar | **✅** Lucide 图标(§01.1)、仅 Tailwind、ResizeObserver 溢出检测、Record<IconKey, Component> | 无 | **100** | |
| 5.4 StatusBar | **✅** 纯展示 props + emits、仅 Tailwind | 无 | **100** | |
| 5.5 ProgressOverlay | **✅** Teleport、El-progress/El-button、仅 Tailwind、focus:ring 焦点指示 | 无 | **100** | |

## Phase-6

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 6.1 HomePage | **✅** v-if/else 链、Tauri dialog/fs API、仅 Tailwind | 无 | **100** | |
| 6.2 FileListTable | **✅** el-table size="small"(§01.2)、仅 Tailwind、多选交互、Teleport 右键菜单 | 无 | **100** | |
| 6.3 DiffViewer | **✅** diff2html + highlight.js、键盘快捷键 composable、Transition 动画 | 无 | **100** | |
| 6.4 CommitPage | **✅** 独立 selectedPaths 副本(§05)、textarea 弹性、无 defineExpose(§01.4) | 无 | **100** | |
| 6.5 LogPage | **✅** el-table 展开行、Transition 动画、搜索 300ms 防抖 | 无 | **100** | |
| 6.6 对话框组一 | **✅** el-dialog + close-on-click-modal 控制(§04.5)、操作中禁止遮罩关闭 | 无 | **100** | |
| 6.7 对话框组二 | **✅** el-dialog + v-show 步骤切换、el-steps 进度条 | 无 | **100** | |
| 6.8 对话框组三 | **✅** el-dialog + Promise.all 批量 resolve | 无 | **100** | |
| 6.9 SettingsPage | **✅** el-card + el-form + el-switch、独立布局 | 无 | **100** | |
| 6.10 AuthDialog | **✅** el-dialog、密码 show/hidden、loading 态、retry 模式遮罩不可关闭 | 无 | **100** | |

## Phase-7

| 步骤 | 规范符合度 | 不符合项 | 得分 | 扣分说明 |
|-----|:---------:|:--------|:---:|---------|
| 7.1 Composables | **✅** Composition API、useXxx 命名(§06)、职责独立 | 无（window.focus 问题是技术选型，非规范违规） | **100** | |
| 7.2 services/svn 封装 | **✅** 无状态(§05 分层规范)、pages 禁止直接 invoke | 无 | **100** | |
| 7.3 lib-setup 与托盘 | **✅** 单实例(§05.3)、tray-icon features、setup 不阻塞 startup | 无 | **100** | |
| 7.4 Shell 右键菜单 | **✅** 三平台分开(§05.3)、注册/清理函数成对、winreg 方案免管理员权限 | 无 | **100** | |
| 7.5 暗色/无障碍 | **✅** 仅 dark:前缀(§04.1)、禁止自定义 CSS、aria-label、role="alert" | 无 | **100** | |
| 7.6 构建验证 | **✅** knip + lint + clippy + build + test 完整流程 | 无 | **100** | |

**表五小计：均分 99.2，最低分 85（3.5 auth.rs 编译错误）+ 90（1.2 缺少 npm 包）**

---

# 打分表六：前后端类型对齐与 IPC 通信正确性

**评分标准：** 检查 Rust 端 struct 与 TypeScript 端 interface 的类型对齐，以及 invoke 参数/返回值匹配。满分 100 分/项。

| 检查项 | 前端定义 | 后端定义 | 对齐情况 | 得分 | 扣分说明 |
|-------|---------|---------|:-------:|:---:|---------|
| SvnCredentials | types/svn.ts | svn/types.rs | **✅** username/password/saveToCache 三字段对齐 | **100** | |
| FileItem | types/svn.ts | models/file.rs | **✅** 13 字段对齐 | **100** | |
| RepoInfo | types/svn.ts | models/repo.rs | **✅** 14 字段 + branchName 对齐 | **100** | |
| OperationProgress | types/svn.ts | svn/types.rs | **✅** operation/percent/stage/fileCount 对齐 | **100** | |
| OperationResult | types/svn.ts | svn/types.rs | **✅** result/detail 对齐 | **100** | |
| LogEntry | types/svn.ts | svn/parser.rs | **✅** revision/author/date/msg/paths 对齐 | **100** | |
| BlameLine | types/svn.ts | svn/parser.rs | **✅** revision/author/date/lineNumber/content 对齐 | **100** | |
| AppSettings | types/settings.ts | config/store.rs | **✅** 11 字段对齐（含 fallbackToBuiltin/darkMode） | **100** | |
| ErrorCode | types/error-codes.ts | models/error.rs | **✅** 13 个错误码对齐（含 TOOL_NOT_FOUND） | **100** | |
| invoke 参数：get_log | stores/svn.ts | commands/log.rs | **❌** store 返回 `{ entries, totalCount }`，Rust 返回 `Vec<LogEntry>` | **80** | TS 侧 svnStore.getLog 以 `{ entries: LogEntry[], totalCount: number }` 格式接收返回值，但 Rust 端的 `get_log` 返回 `Result<Vec<LogEntry>, AppError>`（纯数组）。前端应直接接收 `LogEntry[]`，不需要 `entries` 包装 |
| invoke 参数：checkout_repo | stores/svn.ts | commands/checkout.rs | **❌** store 返回 `{ revision: number }`，Rust 返回 `Result<String, AppError>` | **80** | TS 侧 expects `{ revision: number }`，但 Rust 端返回 `String`（原始 svn stdout）。类型不一致 |
| invoke 参数：merge_branch | stores/svn.ts | commands/branch_ops.rs | **❌** store 返回 `MergeResult`，Rust 返回 `Result<OperationResult, AppError>` | **85** | merge_branch 返回值在 TS 端定义为 `MergeResult { result, conflictCount, conflicts?, detail? }`，但 Rust 端返回 `OperationResult { result, detail }`，缺少 `conflictCount` 字段。Rust 端需要扩展返回类型或 TS 端调整定义 |
| invoke 参数：commit 返回值 | stores/svn.ts | commands/commit.rs | **❌** store 接收 `OperationResult`，但 handleCommit 中 `typeof result === 'number'` 判定 | **85** | `create_commit` 返回 `OperationResult` 对象。但 6.4 CommitPage 的 `handleCommit` 中 `typeof result === 'number'` 分支永远不会进入，因为结果始终是对象。统一的 i18n 处理逻辑应直接使用 `result.result` 和 `result.detail` |

**表六小计：均分 91.7，最低分 80（多个 IPC 返回类型不匹配）**

---

# 汇总统计

## 各打分表平均分

| 打分表 | 平均分 | 最低分项 |
|-------|:-----:|:--------|
| 表一：业务功能覆盖 | **99.1** | svn 版本号检测命令未注册 (85) / auth.rs 编译错误 (85) |
| 表二：视觉设计覆盖 | **100** | 全部满分（v12 的扣分项已全部修复）|
| 表三：AI 编程适配 | **99.2** | auth.rs 编译错误 (85) / window.focus 事件 (95) / get_svn_version 未注册 (95) |
| 表四：多语言适配 | **100** | 全部满分 |
| 表五：开发规范适配 | **99.2** | auth.rs 编译错误 (85) / 缺少 npm 包 (90) |
| 表六：IPC 类型对齐 | **91.7** | 多个 invoke 返回类型不匹配 (80/85) |

## 总评

**整体得分：98.2 / 100**

### 相比 v12 的重大改进（已修复）

1. ✅ **开机自启动 Rust 实现**（原 90→100）：7.3 中补充了完整的 `set_auto_start` 命令（macOS LaunchAgent / Windows winreg / Linux autostart），覆盖三平台
2. ✅ **对话框入场动画显式声明**（原 95→100）：6.6~6.10 所有对话框步骤均添加了注释说明 el-dialog 默认 200ms scale+fade 动画
3. ✅ **焦点指示 focus:ring**（原 90→100）：所有交互式按钮/输入框已补充 `focus:ring-2 focus:ring-blue-400 focus:outline-none`

### 需要修复的关键问题

以下问题必须在编码前修复，否则 AI 编码会编译失败或产生不一致的运行结果：

| 编号 | 问题 | 严重程度 | 涉及步骤 | 修复建议 |
|:---:|------|:-------:|---------|---------|
| **P0** | **auth.rs 使用未定义的 AppError::SvnExecFailed** | **致命（编译失败）** | 3.5（auth.rs） | 将 `AppError::SvnExecFailed(...)` 全部改为 `AppError::SvnCommand(...)`。`models/error.rs` 中没有 `SvnExecFailed` 变体，只有 `SvnCommand(String)`。共 3 处需修改 |
| **P0** | **get_svn_version 未注册为 Tauri command** | **致命（运行时失败）** | 6.9（SettingsPage） | 在 `commands/settings.rs` 中新增 `#[tauri::command] pub async fn get_svn_version() -> Result<String, AppError>`，包装 `crate::tools::resolver::get_svn_version()`，并在 `lib.rs` 的 `generate_handler!` 中注册 |
| **P1** | **getLog 返回类型不匹配** | **高** | 4.2（svnStore） | svnStore.getLog 应返回 `LogEntry[]`（纯数组）而非 `{ entries, totalCount }`。Rust 端返回 `Vec<LogEntry>`，Tauri 自动序列化为 JSON 数组。需移除 `entries` 包装层 |
| **P1** | **checkout_repo 返回类型不匹配** | **高** | 4.2（svnStore） | svnStore.checkoutRepo 的返回值应改为 `string`（Rust 端返回 `Result<String, AppError>`），或 Rust 端改为返回 `Result<CheckoutResult, AppError>` |
| **P1** | **merge_branch 返回类型不匹配** | **高** | 4.2（svnStore）、3.3 | Rust 端 `merge_branch` 返回 `OperationResult` 但 TS 端期望 `MergeResult`（含 conflictCount）。需统一：或在 Rust 端新增 `MergeResult` 结构体含 conflictCount 字段，或 TS 端移除 conflictCount 用后计算 |
| **P1** | **CommitPage 中 handleCommit 的类型分支无效** | **高** | 6.4（CommitPage） | `typeof result === 'number'` 分支永远不会进入（`create_commit` 返回 `OperationResult` 对象）。应直接使用 `result.detail` 提取版本号 |
| **P2** | **缺少 @tauri-apps/plugin-store npm 包** | **中** | 1.2 | CommitForm 6.4B 使用 `import { Store } from '@tauri-apps/plugin-store'` 但步骤 1.2 的 cnpm install 未包含此包。需追加 |
| **P2** | **useAutoRefresh 使用不正确的 window.focus 事件** | **中** | 7.1（useAutoRefresh.ts） | 改用 `@tauri-apps/api/window` 的 `onFocusChanged` 或 `listen('tauri://focus')` 监听 Tauri 窗口焦点事件 |

### 编码前重要提醒

1. **AI 以执行步骤为准，不要自行回读设计文档补充细节。** 遇到步骤描述不够具体时，向用户提问而非自行决定

2. **P0 问题必须先修复再编码：** `auth.rs` 中的编译错误必须修复，`get_svn_version` 命令必须添加注册，否则后面步骤无法编译通过

3. **IPC 类型对齐：** 编码时严格对照 Rust struct 和 TS interface 的字段名和类型。当前表六中标记的对齐问题会影响运行时数据正确性，建议编码前统一修正

4. **认证凭据安全：** 编码时注意密码必须通过 `--password-from-stdin`（stdin 管道）传递，禁止使用 `--password` 命令行参数。日志中密码掩码为 `***`

5. **构建验证建议：** 建议阶段三完成后（3.5 auth.rs 修复后）单独执行一次 `cargo build`，确保所有 Rust 代码可编译。不要在阶段七才集中修复编译错误

6. **组件/文件拆分时机：** 注意 Vue 组件 400 行、Rust command 文件 150 行等行数警戒线，达到后按规范拆分
