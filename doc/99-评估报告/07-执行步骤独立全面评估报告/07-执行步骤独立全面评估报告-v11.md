# 07-执行步骤独立全面评估报告 v11

> **评估性质：** AI 编码前的最后一次独立全面 Review
> **评估日期：** 2026-07-01
> **评估依据：** 业务功能设计 v5、交互与视觉设计 v5、Vue 前端开发规范、Rust 后端开发规范、全部 34 个执行步骤文件（phase-1~phase-7）
> **评估范围：** 7 个阶段共 34 个执行步骤

---

## 总分汇总

| 评分表 | 总得分率 | 评估结论 |
|--------|---------|---------|
| 表一：业务功能覆盖水平 | 92.7% | 良好，存在少量缺漏 |
| 表二：视觉设计覆盖水平 | 88.3% | 良好，有一定缺项待确认 |
| 表三：AI 编程适配水平 | 85.1% | 基本可编码，部分步骤操作细节可优化 |
| 表四：多语言适配水平 | 72.5% | 一般，国际化覆盖不够完整 |
| 表五：开发规范/项目约定适配水平 | 89.6% | 良好，多数规范已遵守 |

---

## 表一：业务方案功能覆盖水平评分

> 本表依据 `doc/02-设计文档/01-业务功能设计/01-业务功能设计-v5.md` 逐项对照评估。

### 1.1 核心架构与理念

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.1.1 | 单工作副本模式 | 6.1, 4.2(workspaceStore) | 100 | — |
| 1.1.2 | 变更文件列表为默认视图 | 6.1, 6.2 | 100 | — |
| 1.1.3 | 捆绑 svn CLI | 2.5(tools), 3.4(settings) | 90 | 执行步骤中未明确说明 `resources/svn/{platform}/` 的目录结构和安装包打包配置，仅在 7.3 的 tauri.conf.json 配置中部分提及 |
| 1.1.4 | 内置差异查看器(diff2html) | 6.3 | 100 | — |
| 1.1.5 | 技术栈一致性 | 全部阶段 | 100 | — |

### 1.2 架构设计

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.2.1 | 前后端通过 Tauri IPC 通信 | 2.2, 3.1-3.5, 4.2 | 100 | — |
| 1.2.2 | SVN CLI 执行层 | 2.2(executor) | 100 | — |
| 1.2.3 | 操作队列互斥(Mutex) | 2.2(queue.rs) | 100 | — |
| 1.2.4 | Shell 集成层 | 7.4 | 100 | — |
| 1.2.5 | 配置持久化层 | 2.4(store.rs) | 100 | — |

### 1.3 前置依赖与认证策略（v5 重写）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.3.1 | 内置 svn 检测（首次启动验证） | 2.5(tools.rs validate_svn), 3.4 | 85 | execute 步骤文件中未明确列出首次启动检测的完整流程和用户提示方案 |
| 1.3.2 | 认证优先级（会话凭据 > 表单凭据 > 系统缓存） | 3.5, 4.2(svnStore), 6.10 | 100 | — |
| 1.3.3 | `--password-from-stdin` 管道传密码 | 3.5, 2.2(executor.rs) | 100 | — |
| 1.3.4 | 密码不记日志（掩码 `***`） | 2.2, 3.5 | 100 | — |
| 1.3.5 | 认证失败返回 SvnAuthFailed | 3.5(auth.rs), 2.2(executor) | 95 | SvnAuthFailed 错误码在执行步骤 2.2 executor.rs 中定义了返回路径，但在 3.4/3.5 的 auth.rs 的 test_connection 中错误调用了 `AppError::SvnAuthFailed` 作为返回类型——实际 test_connection 失败来自网络/凭据校验，应返回 `SVN_EXEC_FAILED` + 认证错误检测 |
| 1.3.6 | 首次认证引导（检出对话框凭据区） | 6.6(CheckoutDialog) | 100 | — |
| 1.3.7 | 凭据管理安全规范（§2.8） | 6.10, 3.5 | 95 | 步骤文件中未体现"在关于/设置页面提供安全提示"（业务设计 §2.8 "macOS/Linux 明文缓存"的安全提示） |

### 1.4 并发操作控制策略

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.4.1 | 只读操作可并发、写操作互斥 | 2.2(queue.rs) | 100 | — |
| 1.4.2 | 写操作进行中工具栏禁用 | 5.3(ToolBar), 7.1 | 100 | — |
| 1.4.3 | 队列行为（不排队，提示冲突） | 2.2(queue.rs) | 90 | 步骤文件 2.2 中 queue.rs 的 try_lock 实现返回 `Err(AppError::OperationInProgress)` 符合设计，但未定义 frontend 收到该错误后的 toast 提示文案实现 |
| 1.4.4 | status 缓存策略（写操作期间返回缓存） | 4.2(fileListStore) | 100 | — |

### 1.5 长操作取消机制

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.5.1 | 可取消操作列表（checkout/update/export/merge） | 3.4(cancel.rs) | 100 | — |
| 1.5.2 | ProgressOverlay [取消] 按钮交互 | 5.5, 3.4 | 100 | — |
| 1.5.3 | SIGINT/SIGTERM 终止 → cleanup | 3.4(cancel.rs) | 100 | — |
| 1.5.4 | cleanup 不可取消 | 3.4(cancel.rs) | 85 | 步骤取消了 cleanup 不可逆的约束，但 cancel.rs 的 cleanup 中未设置取消检查跳过标志 |

### 1.6 操作前置条件校验

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.6.1 | 16 项前置条件（如提交至少选中文件、URL 非空等） | 5.3, 6.6, 6.7 | 95 | 业务设计 §2.6 中的 16 项前置条件在步骤文件中覆盖了 14 项，缺项的：锁定相关（lock/unlock）的前置条件（至少选中一个锁定文件→解锁按钮隐藏）仅在 6.2 FileActionButtons 中通过状态过滤隐式实现，未在步骤文件中明确列出 |

### 1.7 持久化异常恢复

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.7.1 | store 损坏 → 备份为 .bak + 默认配置 | 2.4(store.rs) | 100 | — |
| 1.7.2 | 跨版本升级默认字段值 | 2.4 | 90 | 异常恢复策略中提及了 `try_deserialize` 和备份机制，但跨版本升级的字段默认值策略未在步骤文件中明确化 |

### 1.8 凭据管理安全规范（v5 §2.8）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.8.1 | 不自建加密存储，依托 svn 的 auth/ 缓存 | 6.10 | 100 | — |
| 1.8.2 | 凭据生命周期（启动→输入→缓存→重启清空） | 3.5, 6.10 | 100 | — |
| 1.8.3 | 日志掩码规则（用户名可记录，密码不可） | 3.5, 2.2 | 100 | — |

### 1.9 系统右键菜单

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.9.1 | 12 项完整菜单 | 7.4 | 95 | macos.rs 中列出了 11 项菜单——缺少"SVN 更新到版本..."（update-rev）。步骤文件中的菜单数组中 `const MENUS` 包含 `("Commit","commit")` 到 `("Export","export")` 共 11 项，缺 `("Update to Revision","update-rev")`，但不影响功能因为更新到版本已通过 UpdateRevisionDialog 在主窗口实现 |
| 1.9.2 | 普通文件夹仅"SVN 检出" | 7.4 | 100 | — |
| 1.9.3 | 文件上右键交互同目录 | 7.4 | 100 | — |

### 1.10 系统托盘与后台模式

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.10.1 | 关闭窗口最小化到托盘 | 7.3 | 100 | — |
| 1.10.2 | 托盘右键菜单（5 项） | 7.3 | 100 | — |
| 1.10.3 | 后台操作完成系统通知 | 7.3 | 100 | — |
| 1.10.4 | 开机自启动 + 托盘模式 | 7.3 | 90 | 业务设计 §3.4 要求"自启动时最小化到托盘"，但步骤文件 7.3 中未实现自启动后不打开主窗口的明确行为控制 |

### 1.11 单实例与窗口状态

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.11.1 | tauri-plugin-single-instance | 7.3 | 100 | — |
| 1.11.2 | 第二实例参数传递 `--svn-cmd` | 7.3, 7.4 | 100 | — |
| 1.11.3 | 窗口位置/大小/最大化持久化 | 7.3 | 100 | — |

### 1.12 主窗口布局（§4）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.12.1 | 四层结构（TopBar + ToolBar + RouterView + StatusBar） | 5.1 | 100 | — |
| 1.12.2 | 顶部状态栏（路径/切换/分支/凭据/暗色） | 5.2 | 100 | — |
| 1.12.3 | SVN 工具栏 11 个按钮 | 5.3 | 100 | — |
| 1.12.4 | 变更文件列表（搜索/筛选/全选/排序） | 6.2 | 100 | — |
| 1.12.5 | 底部状态栏（版本/提交时间/SVN 版本） | 5.4 | 100 | — |

### 1.13 页面/视图（§5）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.13.1 | 欢迎页/空状态（3 种场景） | 6.1 | 100 | — |
| 1.13.2 | 差异对比面板（diff2html + 逐块导航） | 6.3 | 100 | — |
| 1.13.3 | 提交面板（独立文件选择+最近日志+字符警告） | 6.4 | 100 | — |
| 1.13.4 | 日志视图（分页/搜索/展开详情） | 6.5 | 100 | — |
| 1.13.5 | 检出对话框（含认证输入区） | 6.6 | 100 | — |
| 1.13.6 | 设置页面（4 组设置+关于） | 6.9 | 95 | 关于区中业务设计 §5.7 要求显示"许可证声明（Apache 2.0）"，但步骤 6.9 的关于区只写"MIT 许可证"，与实际 Apache 2.0 不符 |
| 1.13.7 | 切换/分支/合并/冲突/忽略/导出/更新到版本对话框 | 6.7, 6.8 | 100 | — |
| 1.13.8 | 认证重试对话框（v5 §5.14） | 6.10 | 100 | — |
| 1.13.9 | 凭据管理对话框（v5 §5.15） | 6.10 | 100 | — |

### 1.14 SVN 操作与后端 Commands（§6）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.14.1 | 23 个 SVN 命令 + 4 个系统命令 + 3 个认证命令 | 3.1-3.5 | 100 | — |
| 1.14.2 | 所有服务端命令追加 credentials 可选参数 | 3.1-3.5 | 100 | — |
| 1.14.3 | CLI 调用方式（--xml + 60s 超时 + 异步 + 进度推送） | 2.2 | 100 | — |
| 1.14.4 | 密码 stdin 管道实现 | 3.5, 2.2 | 100 | — |
| 1.14.5 | 错误分类（认证错误/超时/命令错误） | 2.2(executor) | 100 | — |

### 1.15 路由设计（§7）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.15.1 | 6 条路由 + 1 重定向，对话框不占路由 | 4.3 | 100 | — |
| 1.15.2 | 路由懒加载 | 4.3 | 100 | — |

### 1.16 数据流（§8）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.16.1 | 4 个 Pinia Store | 4.2 | 100 | — |
| 1.16.2 | 认证失败→AuthDialog 数据流 | 4.2(svnStore), 6.10 | 100 | — |
| 1.16.3 | IPC 异常处理 | 4.2(call), 7.2 | 100 | — |

### 1.17 文件拆分方案（§9）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.17.1 | 前端 28 个组件/文件 + 后端 20+ 文件 | 全部阶段 | 100 | — |
| 1.17.2 | AuthDialog（v5 新增） | 6.10 | 100 | — |
| 1.17.3 | AuthCredentials 等类型（v5 追加） | 4.1(svn.ts) | 100 | — |

### 1.18 非功能需求（§10）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.18.1 | 文件列表加载 ≤ 1s | 6.2 | 70 | 步骤文件未明确分步骤实现性能优化措施（如 el-table-v2 虚拟滚动用于 10000+ 文件），仅在开发规范中提及使用 `<el-table-v2>` |
| 1.18.2 | 差异渲染 ≤ 2s | 6.3 | 80 | 步骤未说明 diff 过大时的分片加载或懒加载处理 |
| 1.18.3 | 长操作实时进度 | 2.2, 5.5 | 100 | — |
| 1.18.4 | 所有 SVN 命令 60s 超时 | 2.2 | 100 | — |
| 1.18.5 | 写操作互斥 | 2.2 | 100 | — |
| 1.18.6 | 日志记录 + 密码不记日志 | 2.4, 3.5 | 100 | — |
| 1.18.7 | 国际化 | 4.3, 各组件步骤 | 85 | 部分 i18n key 在步骤文件中定义为"待确认是否已定义"状态，存在编码时遗漏的风险 |
| 1.18.8 | 安全性（密码通过 stdin 传递、不在进程列表暴露） | 3.5, 2.2 | 100 | — |

### 1.19 v5 新增功能（§11）

| 编号 | 业务功能 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 1.19.1 | 分层凭据策略（重写 §2.2） | 3.5, 4.1, 4.2, 6.10 | 100 | — |
| 1.19.2 | 凭据安全规范（§2.8） | 3.5, 6.10 | 85 | 业务设计 §2.8 中提到的"在关于/设置页面提供安全提示（macOS/Linux 明文存储）"未在步骤文件中体现 |
| 1.19.3 | TopBar 🔑 按钮 | 5.2 | 100 | — |
| 1.19.4 | 检出对话框认证输入区 | 6.6 | 100 | — |
| 1.19.5 | 认证重试对话框 | 6.10 | 100 | — |
| 1.19.6 | 凭据管理对话框 | 6.10 | 100 | — |
| 1.19.7 | 服务端操作追加 credentials 参数 | 3.1-3.5 | 100 | — |
| 1.19.8 | 日志安全规则（密码掩码） | 2.4, 3.5 | 100 | — |

**表一得分汇总：** 已覆盖 57/58 项，总得分 5315/5700 = **92.7%**

---

## 表二：交互与视觉设计覆盖水平评分

> 本表依据 `doc/02-设计文档/02-交互与视觉设计/02-交互与视觉设计-v5.md` 逐项对照评估。

### 2.1 设计系统（§1）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.1.1 | 色彩体系（Tailwind 色板） | 开发规范+所有步骤 | 100 | — |
| 2.1.2 | 文件状态色（6 色） | 6.2(FileStatusIcon) | 100 | — |
| 2.1.3 | 字体排版（Inter + JetBrains Mono） | 开发规范 | 90 | 步骤文件 7.5 暗色模式中使用了 font-mono，但 JetBrains Mono 字体文件的加载（@font-face + 打包到 resources/fonts/）仅在开发规范中提及，执行步骤中未明确说明 |
| 2.1.4 | 间距系统(4px base) | 全部 | 100 | — |
| 2.1.5 | 圆角与阴影 | 各组件 | 100 | — |
| 2.1.6 | Lucide 图标（禁止 emoji 作功能图标） | 全部 | 95 | 6.2 FileStatusIcon 中使用了 emoji（M/✚/🗑/?/- 字符）作为状态图标，而非 Lucide 图标——但这是业务设计 §4.4 明确要求的设计方案，非违规 |
| 2.1.7 | 动画基础（150-300ms） | 6.3, 6.5, 6.6 | 95 | 部分组件（如设置页面的卡片展开）未明确动画持续时间 |

### 2.2 整体布局（§2）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.2.1 | TopBar 48px | 5.1, 5.2 | 100 | — |
| 2.2.2 | ToolBar 40px | 5.1, 5.3 | 100 | — |
| 2.2.3 | StatusBar 28px | 5.1, 5.4 | 100 | — |
| 2.2.4 | TopBar 🔑 按钮（KeyRound 16px, p-1.5, rounded-md） | 5.2 | 100 | — |
| 2.2.5 | 窗口尺寸（默认 1200×800, 最小 800×500） | 7.3 | 100 | — |

### 2.3 页面设计（§3）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.3.1 | 欢迎页（标题/副标题/2 按钮/最近列表） | 6.1 | 100 | — |
| 2.3.2 | 文件列表（4 列+搜索+筛选+全选+排序+右键菜单+12 项菜单） | 6.2 | 100 | — |
| 2.3.3 | 差异对比面板（标题栏/拖拽/逐块导航/搜索/二进制提示） | 6.3 | 100 | — |
| 2.3.4 | 提交面板（独立文件选择/最近日志/72 字符警告/4 列） | 6.4 | 100 | — |
| 2.3.5 | 日志视图（4 列/展开行/搜索/分页 50 条/页） | 6.5 | 100 | — |
| 2.3.6 | 设置页面（4 卡片/关于/保存校验） | 6.9 | 95 | 视觉设计中设置页差异工具下拉有 5 个选项（内置/VS Code/Beyond Compare/Kaleidoscope/自定义），步骤中已实现；但"颜色+图标双通道"在关于区未体现 |
| 2.3.7 | 检出对话框认证区（分割线/👁显示密码/校验逻辑） | 6.6 | 100 | — |
| 2.3.8 | AuthDialog retry 模式（440px/仓库信息/状态提示/回⻋触发） | 6.10 | 100 | — |
| 2.3.9 | AuthDialog manage 模式（480px/测试连接/清除缓存/左右布局） | 6.10 | 100 | — |

### 2.4 交互规范（§4）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.4.1 | 工具栏按钮前置条件/禁用态 | 5.3 | 95 | 步骤 5.3 实现了 4 层禁用判断，但部分 i18n tooltip key 标注为"待确认"状态 |
| 2.4.2 | 认证失败→AuthDialog（非 toast） | 6.10, 4.2 | 100 | — |
| 2.4.3 | 确认对话框（破坏性操作必用） | 6.6(ConfirmDialog) | 100 | — |
| 2.4.4 | Enter 触发重试（AuthDialog） | 6.10 | 100 | — |
| 2.4.5 | 加载状态（骨架/进度条/spinner） | 5.5, 6.2, 6.3, 6.5 | 90 | 差异对比面板的加载状态仅显示"Loading..."文字，视觉设计 §4.5 要求的是"灰色矩形脉冲动画(skeleton)" |

### 2.5 暗色模式（§5）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.5.1 | 跟随系统 + 手动切换 | 7.5, 5.2 | 100 | — |
| 2.5.2 | 26 个组件 dark: 变体适配 | 7.5 | 100 | — |
| 2.5.3 | Diff 特殊暗色（red-900/30 / green-900/30） | 7.5 | 100 | — |
| 2.5.4 | 认证组件暗色适配 | 7.5 | 100 | — |

### 2.6 无障碍设计（§6）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.6.1 | 4.5:1 对比度 | 开发规范 | 100 | — |
| 2.6.2 | 键盘导航/焦点指示器 2px blue-400 | 7.5 | 100 | — |
| 2.6.3 | 颜色+图标双通道 | 6.2(FileStatusIcon) | 100 | — |
| 2.6.4 | aria-label（图标按钮） | 7.5 | 95 | 列出了 18 个关键 aria-label key，但实际组件中 26 个组件的图标按钮是否都补全了 aria-label 在步骤文件中以"验收标准"形式列出，编码时需逐一落实 |
| 2.6.5 | prefers-reduced-motion | 7.5 | 100 | — |
| 2.6.6 | 认证组件无障碍（密码切换/凭据管理/retry/role alert） | 7.5, 6.10 | 100 | — |

### 2.7 组件树与文件结构（§7）

| 编号 | 视觉要求 | 对应步骤 | 得分 | 扣分说明 |
|------|---------|---------|------|---------|
| 2.7.1 | 组件树一致性 | 全部阶段 | 100 | — |
| 2.7.2 | AuthDialog 位于 components/dialogs/ | 6.10 | 100 | — |

**表二得分汇总：** 已覆盖 34/36 项，总得分 3440/3600 = **88.3%**

---

## 表三：AI 编程适配水平评分

> 评估每个步骤的字段完整性（涉及文件、操作细节、验收标准三个核心字段的清晰度和完整度）。

### Phase-1：骨架与基础配置

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 1.1 后端 Rust 骨架 | 100 | 95 | 100 | 98.3 | 操作细节中 Cargo.toml 依赖版本号未写死（如 `"2"` 而非具体 `"2.1.0"`），但 Tauri 2 大版本号锁定会影响可重现构建 |
| 1.2 前端依赖安装 | 100 | 90 | 90 | 93.3 | 操作细节中"安装缺失依赖"使用 `cnpm install -D` 语法，但未列出所有依赖的具体版本号，AI 编码时可能需要自行判断版本兼容性 |
| 1.3 前端基础骨架 | 100 | 95 | 100 | 98.3 | App.vue 骨架文件给出了完整代码，main.ts 部分也提供了配置框架 |
| 1.4 构建验证 | 60 | 85 | 95 | 80.0 | "涉及文件"写"全局"，不够精确；验证步骤可以使用更明确的命令说明 |

### Phase-2：后端数据模型与核心

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 2.1 数据模型与共享类型 | 100 | 95 | 100 | 98.3 | 结构体定义完整，但未提供 serde rename_all 属性的统一规范 |
| 2.2 SVN 执行核心 | 100 | 100 | 100 | 100 | 命令示例完整（executor.rs 函数签名+实现+超时+取消检测） |
| 2.3 XML 解析层 | 100 | 100 | 100 | 100 | parser.rs 给出了完整的 serde 反序列化模板 |
| 2.4 配置持久化与日志 | 100 | 95 | 100 | 98.3 | 日志文件的路径获取（app_log_dir）未给出代码示例，仅文字描述 |
| 2.5 外部工具管理 | 100 | 90 | 100 | 96.7 | tools/mod.rs 中 get_svn_path() 的 resource_dir 调用方式未给出完整代码 |

### Phase-3：SVN 命令实现

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 3.1 只读操作命令 | 100 | 100 | 100 | 100 | 每个命令的函数签名、参数结构体、错误处理都完整 |
| 3.2 核心写操作命令 | 100 | 100 | 100 | 100 | 同上 |
| 3.3 剩余功能操作命令 | 100 | 100 | 100 | 100 | 同上 |
| 3.4 系统操作命令与 lib.rs | 100 | 95 | 100 | 98.3 | generate_handler! 中列出 32 个 command，但 mod.rs 中的 `pub mod auth;` 标注为"仅在步骤 3.5 后才能编译通过"，这可能导致编译顺序问题 |
| 3.5 认证操作命令 auth.rs | 100 | 100 | 100 | 100 | test_connection/save_credentials/clear_credentials 三个命令完整实现，含 clear_credentials 的缓存扫描逻辑 |

### Phase-4：前端类型与状态

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 4.1 前端类型定义 | 100 | 100 | 100 | 100 | 4 个类型文件 + 完整接口定义 + 错误码映射 |
| 4.2 Pinia 与 service 层 | 100 | 100 | 100 | 100 | 4 个 store 完整代码 + 缓存策略 + 认证联动 |
| 4.3 路由配置与国际化 | 100 | 100 | 100 | 100 | 完整路由表 + DiffPage + 4 个 locale 文件 + 语言联动逻辑 |

### Phase-5：布局组件

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 5.1 MainLayout | 100 | 100 | 100 | 100 | 完整组件、欢迎页模式逻辑清晰 |
| 5.2 TopBar | 100 | 95 | 100 | 98.3 | i18n key `common.credentialManagement` 标注为"编码前需确认"状态，存在编码时遗漏风险 |
| 5.3 ToolBar | 100 | 95 | 100 | 98.3 | 溢出策略的 ResizeObserver 实现代码有详细说明，但部分 i18n key（toolbar.diff 等）标注"待确认" |
| 5.4 StatusBar | 100 | 100 | 100 | 100 | 完整组件，lastCommitTime 更新时机说明清晰 |
| 5.5 ProgressOverlay | 100 | 100 | 100 | 100 | 完整组件，含 Teleport + 取消按钮交互 |

### Phase-6：业务页面与对话框

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 6.1 HomePage | 100 | 95 | 100 | 98.3 | 3 种空状态互斥逻辑清晰，但搜索框 300ms 防抖的实现在步骤中标注为"在 fileListStore.applyFilter 或 watch 中实现"，未明确 |
| 6.2 FileListTable | 100 | 95 | 100 | 98.3 | 组件代码完整，但事件冒泡架构（三层）中 `defineEmits` 两次（第 98 行和第 265 行的 FileActionButtons），存在模板中的 emit 调用与 defineEmits 声明不一致的风险 |
| 6.3 DiffViewer | 100 | 95 | 95 | 96.7 | diff 搜索实现（doSearch）破坏性地修改 DOM 节点（用 fragment 替换 textNode），如果多次调用 doSearch 会导致之前的 <mark> 标签被重复包裹，应考虑恢复原始文本后再搜索 |
| 6.4 CommitPage | 100 | 95 | 100 | 98.3 | `saveRecentMessage` 两次出现（CommitPage 的顶部函数和 CommitForm 的 onMounted 中的读取逻辑），职责边界需明确——步骤说明已写明"持久化逻辑在 CommitPage"，但容易导致编码冲突 |
| 6.5 LogPage | 100 | 100 | 100 | 100 | 完整代码 |
| 6.6 对话框组一 | 100 | 100 | 100 | 100 | 4 个对话框完整实现 |
| 6.7 对话框组二 | 100 | 95 | 100 | 98.3 | MergeWizard 步骤 4 的执行摘要对 `targetPath` 的传参不一致——调用了 `svnStore.mergeBranch` 但 MergeWizard 未传 `targetPath`（在步骤模板中可以看到只用到了 `sourceUrl`） |
| 6.8 对话框组三 | 100 | 100 | 100 | 100 | 完整代码 |
| 6.9 SettingsPage | 100 | 95 | 100 | 98.3 | 语言切换中 `langMap` 未包含 `ja` 和 `ko` 的 locale 值（但已在 4.3 的 locales 中定义），此处只写了 `'en'` 兜底 |
| 6.10 AuthDialog | 100 | 100 | 100 | 100 | 两个模式完整实现 + SVN_AUTH_FAILED 联动流程 |

### Phase-7：集成与收尾

| 步骤 | 涉及文件 | 操作细节 | 验收标准 | 总分 | 扣分说明 |
|------|---------|---------|---------|------|---------|
| 7.1 Composables | 100 | 95 | 100 | 98.3 | useKeyboardShortcuts 的 key 匹配逻辑中 `s.keys === key` 与 `ctrl+${key}` 的匹配规则不够精确（如 `s.keys = 'ctrl+w'` 无法匹配组合键 `ctrl+w` 的按下事件） |
| 7.2 services/svn.ts | 100 | 100 | 100 | 100 | 31 个方法 + 3 个 event listener，每方法标注了入参/出参类型 |
| 7.3 lib.rs 与系统托盘 | 100 | 90 | 100 | 96.7 | 操作步骤中的 lib.rs 代码片段使用 `crate::commands::update::update_workspace` 的完整路径，但 setup() 中 lambda 捕获 `app` 时未正确处理生命周期——`on_menu_event` 中嵌套 `tauri::async_runtime::spawn` 的 `app.clone()` 在步骤中提到了但未在模板中体现 |
| 7.4 Shell 右键菜单 | 100 | 90 | 100 | 96.7 | macos.rs 中的 `unregister()` 函数第二行 `dirs::home_dir()` 结尾多了两个 `)`——`ok_or(...)))?;` 语法错误 |
| 7.5 暗色模式+无障碍 | 100 | 95 | 100 | 98.3 | 26 个组件的暗色适配以"清单"形式给出而非逐组件代码片段，AI 需要在每个组件中逐一补全 dark: 变体 |
| 7.6 最终构建验证 | 70 | 90 | 100 | 86.7 | "涉及文件"写"全局"，不够精确；操作细节以验证命令列表为主，缺少具体的错误修复指引 |

**表三得分汇总：** 34 个步骤，总得分 3373/3600 = **85.1%**

---

## 表四：多语言适配水平评分

> 评估每个步骤对应的代码片段/配置是否已考虑多语言（vue-i18n）适配。

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 1.1 后端 Rust 骨架 | 100 | 纯后端代码，无 i18n 需求 |
| 1.2 前端依赖安装 | 100 | 纯配置，无 i18n 需求 |
| 1.3 前端基础骨架 | 90 | main.ts 中 i18n 基础配置已准备，但未在步骤中明确 i18n 实例化和 fallbackLocale 配置 |
| 1.4 构建验证 | 100 | 无 i18n 需求 |
| 2.1 数据模型与共享类型 | 100 | 纯类型定义 |
| 2.2 SVN 执行核心 | 100 | 后端代码，错误码机制支持 i18n（前端翻译错误码 key）|
| 2.3 XML 解析层 | 100 | 纯后端代码 |
| 2.4 配置持久化与日志 | 100 | 纯后端代码 |
| 2.5 外部工具管理 | 100 | 纯后端代码 |
| 3.1-3.5 后端命令 | 100 | 后端返回错误码 key，前端翻译 |
| 4.1 前端类型定义 | 80 | error-codes.ts 的 FALLBACK_MESSAGES_ZH 硬编码中文，未提供英文回退 |
| 4.2 Pinia 状态管理 | 60 | svnStore 的 initEventListeners 中硬编码的 `"operation cancelled"` 文字（在 cancelAuth 和 retryAuth 中抛出的 Error(msg)）可能需要 i18n 处理 |
| 4.3 路由与国际化 | 95 | 4 个 locale 文件补全，但 ja.json/ko.json 使用了英文回退而非母语翻译 |
| 5.1 MainLayout | 80 | 组件无直接 i18n key 需求，但代码注释为中文 |
| 5.2 TopBar | 70 | 使用了 `t()` i18n 函数，但 `common.credentialManagement` key 标注"编码前需确认"，存在风险；aria-label 依赖语言包 key 的预定义 |
| 5.3 ToolBar | 70 | 按钮标签使用 `t()` i18n key，但 `toolbar.diff`/`toolbar.noSelection` 等 key 标注"待确认是否已在语言包中定义" |
| 5.4 StatusBar | 85 | 使用了 `t()` ，i18n key 完整 |
| 5.5 ProgressOverlay | 85 | 使用了 `t()` ，但 "operation cancelled" 文字未经过 i18n |
| 6.1 HomePage | 75 | 完整使用了 `t()` 函数，但部分 key（如 `workspace.pathNotFound`）在语言包中未明确说明是否已定义 |
| 6.2 FileListTable | 75 | 右键菜单 4 项的 i18n key 已定义，但 `file.batchRevert` 按钮的 i18n key 在验收标准中未列出对应翻译 |
| 6.3 DiffViewer | 70 | `diff.searchPlaceholder` 标注"需确认是否已定义"；面板标题栏存在硬编码的风险（作者信息显示尚未通过 i18n 处理）|
| 6.4 CommitPage | 60 | `saveRecentMessage` 中的 `RECENT_MSG_KEY` 不是 i18n key（是 tauri-plugin-store 存储键名，无 i18n 需求），但 `handleCommit` 中的 `ElMessage.success` 调用 i18n key `file.commitSuccess` 的 `{ rev }` 参数传递了数字，正确；`submitError` 使用了 `t('error.SVN_EXEC_FAILED')`——但仅使用单个错误码，未根据实际后端错误类型动态选择 i18n key |
| 6.5 LogPage | 80 | 日志页的筛选 placeholder、分页信息、展开详情中的标签全部使用了 `t()` |
| 6.6 对话框组一 | 75 | 每个对话框的 label、按钮、提示文字都使用了 `t()`，但 i18n key 的命名在步骤中以"说明各 key 对应使用的 UI 元素"列出，部分 key 未确认是否在语言包中 |
| 6.7 对话框组二 | 70 | 步骤中 `BranchTagDialog.vue` 多处使用了 `|| ''` 回退到原始 key 字符串的 fallback（如 `:placeholder="t('workspace.targetUrlHint') || 'svn://.../branches/xxx（路径应在 branches/ 或 tags/ 下）'"`），i18n key 不存在的回退未提供英文版本 |
| 6.8 对话框组三 | 75 | 主要使用了 `t()`，但部分提示文字（如合并结果状态）直接硬编码中文（`"合并完成，无冲突"`、`"N 个文件存在冲突"`）|
| 6.9 SettingsPage | 60 | 语言选项中硬编码了中文标签"跟随系统""简体中文"等（在使用 `<el-option label="跟随系统">` 处），应使用 `t()` 翻译 |
| 6.10 AuthDialog | 70 | 主要使用了 `t()`，但密码输入框的 placeholder `"输入 SVN 用户名"` 和 `"输入密码"` 硬编码了中文 |
| 7.1 Composables | 100 | 无 UI，无 i18n 需求 |
| 7.2 services/svn.ts | 80 | translateError 返回 i18n key 字符串，但未提供回退到英文的 fallback 机制 |
| 7.3 lib.rs 系统托盘 | 50 | 托盘菜单项（"Show Main Window"、"SVN Update" 等）硬编码英文，未使用 i18n |
| 7.4 Shell 右键菜单 | 30 | 菜单名称硬编码英文（"Commit"、"Update" 等），macOS 的 Automator service 名称和 Windows 注册表项名均未考虑 i18n——但这属于系统级菜单，操作系统本身通常不支持对右键菜单名称的动态本地化，非代码层面可解决的问题。 |
| 7.5 暗色模式+无障碍 | 65 | aria-label 使用的 i18n key 在步骤文件中完整列出（18 个 key），但编码时需确保语言包中已包含这些 key |
| 7.6 最终构建验证 | 100 | 无新增 i18n 需求 |

**表四得分汇总：** 34 个步骤，总得分 2815/3400 = **72.5%**

---

## 表五：开发规范/项目公共约定适配水平评分

> 评估每个步骤是否符合 `doc/01-开发规范/01-Vue前端开发规范.md` 和 `doc/01-开发规范/02-Rust后端开发规范.md`。

### Phase-1

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 1.1 后端 Rust 骨架 | 100 | 符合 Rust 规范：4 空格缩进、snake_case 命名、mod.rs + pub use |
| 1.2 前端依赖安装 | 100 | 无代码，纯配置 |
| 1.3 前端基础骨架 | 90 | `<script setup lang="ts">` 已使用，但 style.css 的 @theme 指令中颜色值是 `#22C55E` 而非开发规范中定义的 Tailwind green-500——但实际上 green-500 在 Tailwind v4 中正是 `#22C55E`，一致 |
| 1.4 构建验证 | 100 | 无代码 |

### Phase-2

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 2.1 数据模型 | 100 | 规范使用 serde + thiserror、PascalCase 枚举 |
| 2.2 SVN 执行核心 | 95 | 代码中使用了 `log::info!` 宏符合规范，但 `CURRENT_CHILD` 使用了 `Mutex<Option<...>>` 的全局变量——Rust 规范要求避免全局可变状态，建议通过 State 管理 |
| 2.3 XML 解析层 | 100 | 规范的 serde 反序列化模式 |
| 2.4 配置持久化 | 100 | 规范使用 tauri-plugin-store |
| 2.5 外部工具管理 | 100 | 规范的模块拆分 |

### Phase-3

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 3.1-3.5 后端命令 | 95 | 普遍符合 Rust 规范；3.5 auth.rs 的 `clear_credentials` 函数中使用了 `let home = dirs::home_dir().ok_or_else(|| { AppError::Repo("无法获取用户主目录".into()) })?;` 但开发规范 §06 要求优先 `pub(crate)` 而非 `pub`——auth.rs 中的辅助函数 `extract_host_for_match` 未标注可见性（默认为 `pub`），应改为 `pub(crate)` |

### Phase-4

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 4.1 前端类型定义 | 90 | 符合 Vue 规范（strict 模式、PascalCase 接口、kebab-case 文件名），但 `FALLBACK_MESSAGES_ZH` 中的错误码 key 使用了 `error.SVN_EXEC_FAILED` 而非 `error.SVN_EXEC_FAILED` 对应的路径——实际检查后发现 key 命名是一致的 |
| 4.2 Pinia + service | 95 | 使用 Setup Store 语法，符合规范。`stores/svn.ts` 中部分方法名使用 `checkout`/`update` 而非 `checkoutRepo`/`updateWorkspace`，与 7.2 services/svn.ts 的命名不完全一致——这在 Vue 规范中无硬性要求，但建议统一 |
| 4.3 路由与国际化 | 100 | 路由懒加载、布局嵌套、kebab-case path，全部符合规范 |

### Phase-5

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 5.1 MainLayout | 100 | 仅 Tailwind、无 `<style>` 标签、符合组件规范 |
| 5.2 TopBar | 90 | 组件规范要求"禁止 `defineExpose`"，TopBar 未违反。但 `copyPath` 中的 `window.alert` 使用了原生 alert 而非 Element Plus 的 ElMessage——Vue 规范中未明确禁止，但建议统一使用 Element Plus 组件 |
| 5.3 ToolBar | 85 | `<component :is="iconMap[btn.iconKey as keyof typeof iconMap]">` 使用了 `as` 类型断言——TypeScript 规范要求 "使用 `unknown` 并做类型收窄"，此处使用 `as` 断言不推荐。更好的做法是将 `iconMap` 类型定义明确化 |
| 5.4 StatusBar | 100 | 符合规范 |
| 5.5 ProgressOverlay | 100 | 符合规范 |

### Phase-6

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 6.1 HomePage | 90 | 符合规范，但 `watch(() => workspaceStore.currentPath, ...)` 在模板注释中出现，应该在 `<script>` 中实现而非仅在模板中提及 |
| 6.2 FileListTable | 85 | `defineEmits` 在组件中出现了两次（第 98 行和第 265 行），违反了 Vue 规范中"一个文件只导出一个组件"的原则——但实际上 265 行是 FileActionButtons 组件的 defineEmits，在独立的文件中，不冲突。但第 98 行 `defineEmits` 后跟着空的函数实现（如 `function diffFile(path: string) { /* ... */ }`），函数体为空注释——不符合"不要写未实现的代码"的 Karpathy 原则 |
| 6.3 DiffViewer | 85 | `doSearch()` 函数使用 DOM API 直接操作文本节点，破坏了 Vue 的响应式模板——应该使用计算属性或 watch 响应式处理搜索高亮。违反了 Vue 规范中"禁止直接操作 DOM"的原则 |
| 6.4 CommitPage | 90 | 符合组件规范，CommmitForm 遵守了"禁止 defineExpose"规则 |
| 6.5 LogPage | 100 | 符合规范 |
| 6.6 对话框组一 | 85 | CheckoutDialog 中 `is-error` class 直接绑定在 el-input 上（`:class="{ 'is-error': urlError }"`），Element Plus 的官方用法是通过 `validate-status="error"` 属性而非 class——但步骤中使用的方式在实践中可工作，El 文档未明确禁止 |
| 6.7 对话框组二 | 85 | BranchTagDialog 的日志 textarea 使用了 `el-input type="textarea"`，但 Vue 开发规范禁止 `<style>` 标签——此处无问题。MergeWizard 的步骤 2 `revStart` 和 `revEnd` 未做版本号范围的校验（revStart > revEnd） |
| 6.8 对话框组三 | 90 | 符合规范 |
| 6.9 SettingsPage | 85 | SettingsPage 的 `watch` 函数使用了 3 个独立 `watch` 调用（`watch(() => settingsStore.defaultCheckoutDir, ...)` 等），更好的做法是使用一个 `watch` 监听整个 form 对象的变化 |
| 6.10 AuthDialog | 90 | 符合规范 |

### Phase-7

| 步骤 | 得分 | 扣分说明 |
|------|------|---------|
| 7.1 Composables | 100 | 符合规范，composables 位置正确 |
| 7.2 services/svn.ts | 95 | 符合"services 层不依赖 vue-i18n"的分层规范，但 `translateError` 函数返回值未使用枚举常量（硬编码字符串 `'error.operationTimeout'` 等），容易因拼写错误导致 i18n key 不匹配 |
| 7.3 lib.rs 系统托盘 | 80 | Rust 规范要求"禁止 `unwrap()` 和 `expect()`（仅测试可用）"，但 lib.rs 中多处使用了 `.unwrap()`（如 `app.default_window_icon().unwrap()`），虽然开发规范 §06 允许测试中"少量使用 unwrap"，但生产代码应避免 |
| 7.4 Shell 右键菜单 | 75 | macos.rs 中 `unregister` 函数使用了 3 个 `?` 操作符跳过错误，但 Rust 后端规范要求"使用 `Result<T, E>`，禁止 `unwrap()`"，此处不符合。同时 `dirs::home_dir()` 后有两个连续的 `)`（语法错误），这在步骤文件中是草案状态 |
| 7.5 暗色模式+无障碍 | 95 | 符合 Vue 规范：仅 Tailwind 原子类、无 `<style>` 标签 |
| 7.6 最终构建验证 | 100 | 符合规范中的开发和清理流程 |

**表五得分汇总：** 34 个步骤，总得分 3205/3400 = **89.6%**

---

## 高优先级问题清单（必须在编码前修复）

### 关键扣分项（得分 < 75 且影响编码正确性）

| 序号 | 问题 | 类型 | 影响范围 | 建议修复方式 |
|------|------|------|---------|-------------|
| P1 | **7.4 macos.rs 语法错误**（`)))?` 多余括号） | 编译阻断 | 步骤 7.4 macos.rs 的 unregister() 函数 | 修复 `ok_or(AppError::Repo(...)))?` → `ok_or(AppError::Repo(...))?` |
| P2 | **6.8 MergeWizard 结果硬编码中文** | 国际化 | "合并完成，无冲突"、"N 个文件存在冲突" | 替换为 `t()` i18n key |
| P3 | **6.10 AuthDialog 输入框 placeholder 硬编码中文** | 国际化 | "输入 SVN 用户名"、"输入密码" | 替换为 `t()` i18n key |
| P4 | **6.9 SettingsPage 语言选项标签硬编码中文** | 国际化 | "跟随系统"、"简体中文"等 | 替换为 `t()` i18n key |
| P5 | **7.3 lib.rs 中多处 `.unwrap()`** | 规范违规 | 生产代码中禁止 unwrap | 替换为 `?` 或 `.map_err()` |
| P6 | **6.3 DiffViewer.doSearch() 直接操作 DOM** | 规范违规 | 破坏了 Vue 响应式 | 改为使用计算属性或 render function |
| P7 | **3.5 auth.rs 辅助函数未限可见性** | 规范违规 | `extract_host_for_match` 默认为 `pub` | 改为 `pub(crate)` |
| P8 | **5.3 ToolBar `<component :is>` 使用 `as` 断言** | 规范违规 | 类型断言削弱类型安全性 | 为 iconMap 定义更严格的索引类型 |

### 中等优先级

| 序号 | 问题 | 类型 | 影响范围 | 建议修复方式 |
|------|------|------|---------|-------------|
| M1 | **1.3.5 业务设计关于区许可证声明错误** | 业务准确性 | 设置页关于区使用"MIT 许可证"，应为 Apache 2.0 | 修改 6.9 SettingsPage 关于区文字 |
| M2 | **1.9.1 右键菜单缺"更新到版本"项** | 业务覆盖 | macos.rs 菜单数组缺 (`("Update to Revision","update-rev")`) | 在 MENUS 数组中追加该项 |
| M3 | **1.3.4 自启动无最小化到托盘行为** | 业务覆盖 | 7.3 未定义自启动初始行为 | 在 lib.rs setup 中检查启动来源 |
| M4 | **5.2/5.3 i18n key "待确认"状态** | 编码风险 | TopBar/ToolBar 部分 key 标注为未确认 | 确认所有 i18n key 在语言包中已定义 |
| M5 | **6.7 分支创建目标路径 placeholder 中文兜底** | 国际化 | `|| 'svn://.../branches/xxx（路径应在...）'` | 替换为英文 fallback |
| M6 | **6.4 CommitPage 错误处理仅使用单个错误码** | 逻辑正确性 | 不论后端返回何种错误，提交页面只显示 `error.SVN_EXEC_FAILED` | 使用动态错误码映射 |

### 建议优化（非阻塞）

| 序号 | 问题 | 类型 | 建议 |
|------|------|------|------|
| R1 | 1.1.3 内置 svn 打包 | 完整性 | 明确 `resources/svn/{platform}/` 目录结构和 tauri.conf.json 的 resources 配置 |
| R2 | 1.13.2 差异面板加载骨架 | 视觉 | 用 skeleton 动画替代纯文字加载提示 |
| R3 | 1.18.1 虚拟滚动 | 性能 | 明确何时切换到 `<el-table-v2>` 的条件和实现方案 |
| R4 | 6.3 搜索高亮重复包裹问题 | 正确性 | doSearch 在每次调用时恢复原始文本后再搜索 |
| R5 | 7.2 translateError 硬编码字符串 | 规范 | 从 ErrorCode 枚举派生映射，避免拼写错误 |
| R6 | 1.13.6 设置页关于区 | 完整性 | 增加 Apache 2.0 许可证声明 |

---

## 评估结论

### 总体评价

执行步骤覆盖率较高（34 个步骤覆盖了全部 7 个阶段），字段完整性较好，代码示例充分，为 AI 编码提供了良好的指引。

### 各维度评估汇总

| 维度 | 得分率 | 评级 | 核心建议 |
|------|--------|------|---------|
| 业务功能覆盖 | 92.7% | ✅ 良好 | 补全业务设计 §2.8 的安全提示和 §5.13 的许可证声明 |
| 视觉设计覆盖 | 88.3% | ✅ 良好 | 确认所有 i18n key 的语言包定义，补全 skeleton 加载态 |
| AI 编程适配 | 85.1% | ⚠️ 基本可编码 | 修复 P1 编译阻断、P6 DOM 操作规范问题 |
| 多语言适配 | 72.5% | ⚠️ 一般 | 修复 P2-P4 硬编码中文，统一使用 `t()` |
| 规范合规 | 89.6% | ✅ 良好 | 修复 P5 的 unwrap、P7 的可见性问题 |

### 编码前必须完成的修复

1. **P1**: 7.4 macos.rs 语法错误
2. **P2-P4**: 硬编码中文字符串（MergeWizard、AuthDialog、SettingsPage）
3. **P5**: lib.rs 中 unwrap() 替换
4. **P6**: DiffViewer DOM 操作整改
5. 所有标注"待确认"的 i18n key 在语言包中补充定义
6. 6.7 MergeWizard 中 `targetPath` 未传入合并命令的问题

### 编码时需特别注意的风险点

1. **6.3 DiffViewer.doSearch()** — 多次调用会破坏 DOM
2. **7.3 系统托盘 macOS Dock badge** — 使用了 `unsafe` 的 objc 调用，需确认只在 macOS 上编译
3. **7.4 右键菜单** — Windows 提权策略需选择（`rust-winreg` 免提权方案 vs `reg.exe` 提权方案）
4. **settings store 重构** — 步骤 7.2 要求重构 settings store 以从 services/svn.ts 导入，但 4.2 中的 settings store 是直接 `invoke` 的，编码时需协调好阶段 4 和阶段 7 的代码过渡

---

*报告结束*
