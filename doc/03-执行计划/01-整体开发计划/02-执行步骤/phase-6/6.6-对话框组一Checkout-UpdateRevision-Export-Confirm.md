# 6.6 对话框组一：Checkout / UpdateRevision / Export / Confirm

> 对应开发计划 v2 步骤 6D.1 + 6D.8 + 6D.9 + 6D.7
> 前置状态：阶段五已完成，svnStore/workspaceStore 可用

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src/components/dialogs/CheckoutDialog.vue` | **新建** |
| `src/components/dialogs/UpdateRevisionDialog.vue` | **新建** |
| `src/components/dialogs/ExportDialog.vue` | **新建** |
| `src/components/dialogs/ConfirmDialog.vue` | **修改** — 从骨架完善 |

## 操作细节

### 6.6A — CheckoutDialog

> **对话框入场动画：** 所有 `el-dialog` 默认内置 200ms scale(0.95→1) + fade 动画（`transition="el-dialog-fade"`），无需自定义 CSS。操作进行中（loading）时设置 `:close-on-click-modal="false"` 禁止遮罩关闭。

el-dialog 480px。仓库 URL 输入 + 格式校验（svn:// / https:// / svn+ssh:// 正则），目标路径输入+浏览按钮，深度下拉（4 选项，映射：infinity->--depth infinity / immediates->--depth immediates / children->--depth children / empty->--depth empty），ignoreExternals 复选框，"仅此目录（不包括子目录）"复选框（对应 `--depth empty`），选中时忽略深度下拉。检出中按钮 loading，禁止遮罩关闭。

认证信息区域（可选，留空走系统缓存凭据）（v5 新增）：始终展开显示不可折叠（视觉设计 §3.7 要求），用户名输入框、密码输入框（使用 Lucide Eye/EyeOff 图标按钮显示/隐藏切换）、"保存凭据到 ~/.subversion/auth/"复选框。`checkout()` 调用时当用户名不为空时附带 `credentials` 参数（`{ username, password, saveToCache }`）。

**完整代码：**

```vue
<script setup lang="ts">
import { ref, reactive, computed } from 'vue'
import { useSvnStore } from '@/stores/svn'
import { useWorkspaceStore } from '@/stores/workspace'
import { useI18n } from 'vue-i18n'
import { open } from '@tauri-apps/plugin-dialog'

const { t } = useI18n()
const svnStore = useSvnStore()
const workspaceStore = useWorkspaceStore()

const emit = defineEmits<{ close: [] }>()

// ── 表单状态 ──
const repoUrl = ref('')
const targetPath = ref('')
const depth = ref('infinity')
const ignoreExternals = ref(false)
const emptyOnly = ref(false)
const isCheckingOut = ref(false)

// URL 校验
const urlValid = computed(() =>
  /^svn:\/\/|^https:\/\/|^svn\+ssh:\/\//.test(repoUrl.value)
)
const urlError = computed(() => {
  if (!repoUrl.value) return ''
  if (!urlValid.value) return t('dialog.urlInvalid')
  return ''
})

// 路径浏览
async function browseTarget() {
  const selected = await open({ directory: true, title: t('dialog.selectTargetDir') })
  if (selected) targetPath.value = selected
}

// ── 认证信息区域（v5 新增，可选） ──
const authForm = reactive({
  username: '',
  password: '',
  saveToCache: false,
  showPassword: false,
})
const usernameError = ref('')
const passwordError = ref('')

function validateAuth(): boolean {
  let valid = true
  usernameError.value = ''
  passwordError.value = ''
  if (authForm.username && !authForm.username.trim()) {
    usernameError.value = t('error.usernameWhitespace')
    valid = false
  }
  if (authForm.username && !authForm.password) {
    passwordError.value = t('error.passwordEmpty')
    valid = false
  }
  return valid
}

// ── 检出操作 ──
async function handleCheckout() {
  if (!urlValid.value || !validateAuth()) return
  isCheckingOut.value = true
  try {
    const actualDepth = emptyOnly.value ? 'empty' : depth.value
    const result = await svnStore.checkout({
      url: repoUrl.value,
      targetPath: targetPath.value,
      depth: actualDepth,
      ignoreExternals: ignoreExternals.value,
      credentials: authForm.username ? {
        username: authForm.username,
        password: authForm.password,
        saveToCache: authForm.saveToCache,
      } : undefined,
    })
    if (result && typeof result.revision === 'number') {
      await workspaceStore.switchWorkspace(targetPath.value)
      emit('close')
    }
  } finally {
    isCheckingOut.value = false
  }
}
</script>

<template>
  <el-dialog :model-value="true" :title="t('dialog.checkout')" width="480px"
    :close-on-click-modal="false" :close-on-press-escape="!isCheckingOut"
    @close="emit('close')">
    <div class="flex flex-col gap-3">
      <!-- 仓库 URL -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.repoUrl') }}
        </label>
        <el-form-item :validate-status="urlError ? 'error' : ''" class="mb-0"
          :error="urlError || undefined">
          <el-input v-model="repoUrl" size="small" :placeholder="t('dialog.repoUrlPlaceholder')" />
        </el-form-item>
        <p v-if="urlError" class="text-xs text-red-500 mt-1">{{ urlError }}</p>
      </div>

      <!-- 目标路径 -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.targetPath') }}
        </label>
        <div class="flex gap-2">
          <el-input v-model="targetPath" size="small" :placeholder="t('dialog.selectTargetDir')" />
          <el-button size="small" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="browseTarget">{{ t('dialog.browse') }}</el-button>
        </div>
      </div>

      <!-- 深度选项 -->
      <div class="flex gap-3 items-center">
        <div class="flex-1">
          <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
            {{ t('dialog.depth') }}
          </label>
          <el-select v-model="depth" size="small" :disabled="emptyOnly">
            <el-option value="infinity" :label="t('dialog.depthInfinity')" />
            <el-option value="immediates" :label="t('dialog.depthImmediates')" />
            <el-option value="children" :label="t('dialog.depthChildren')" />
            <el-option value="empty" :label="t('dialog.depthEmpty')" />
          </el-select>
        </div>
      </div>

      <el-checkbox v-model="ignoreExternals" size="small">
        {{ t('dialog.ignoreExternals') }}
      </el-checkbox>
      <el-checkbox v-model="emptyOnly" size="small">
        {{ t('dialog.currentDirOnly') }}
      </el-checkbox>

      <!-- 认证区域（可选，v5 新增）——始终展开，不可折叠（视觉设计 §3.7 要求） -->
      <div class="border-t border-slate-200 dark:border-slate-700 pt-3">
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-2 block">
          {{ t('dialog.authOptional') }}
        </label>
        <div class="flex flex-col gap-2">
          <el-input v-model="authForm.username" size="small"
            :placeholder="t('auth.username')" />
          <div class="relative">
            <el-input v-model="authForm.password" size="small"
              :type="authForm.showPassword ? 'text' : 'password'"
              :placeholder="t('auth.password')" />
            <button @click="authForm.showPassword = !authForm.showPassword"
              class="absolute right-2 top-1/2 -translate-y-1/2
                text-slate-400 dark:text-slate-500 hover:text-slate-600
                focus:ring-2 focus:ring-blue-400 focus:outline-none rounded"
              :aria-label="t('auth.togglePassword')">
              <EyeOff v-if="authForm.showPassword" class="w-4 h-4" />
              <Eye v-else class="w-4 h-4" />
            </button>
          </div>
          <el-checkbox v-model="authForm.saveToCache" size="small">
            {{ t('auth.saveToCache') }}
          </el-checkbox>
        </div>
      </div>
    </div>

    <template #footer>
      <el-button size="default" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="emit('close')" :disabled="isCheckingOut">
        {{ t('dialog.cancel') }}
      </el-button>
      <el-button size="default" type="primary" :loading="isCheckingOut"
        :disabled="!urlValid || !targetPath" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="handleCheckout">
        {{ t('dialog.checkout') }}
      </el-button>
    </template>
  </el-dialog>
</template>
```

**CheckoutDialog i18n key 说明：**

| i18n key | 用途 | 对应 UI 元素 |
|---------|------|-------------|
| `dialog.checkout` | 弹窗标题 | el-dialog title |
| `dialog.repoUrl` | 仓库 URL 标签 | label |
| `dialog.repoUrlPlaceholder` | URL 输入框占位文字 | el-input placeholder |
| `dialog.targetPath` | 目标路径标签 | label |
| `dialog.selectTargetDir` | 浏览按钮弹窗标题 + 路径输入框占位 | open() title + el-input placeholder |
| `dialog.browse` | 浏览按钮文字 | el-button |
| `dialog.depth` | 深度标签 | label |
| `dialog.depthInfinity` / `dialog.depthImmediates` / `dialog.depthChildren` / `dialog.depthEmpty` | 深度下拉各选项 | el-option label |
| `dialog.ignoreExternals` | 忽略外部引用复选框 | el-checkbox |
| `dialog.currentDirOnly` | 仅此目录复选框 | el-checkbox |
| `dialog.authOptional` | 认证区域标签（可选） | label |
| `auth.username` | 用户名输入框占位 | el-input placeholder |
| `auth.password` | 密码输入框占位 | el-input placeholder |
| `auth.togglePassword` | 密码显示切换按钮 aria-label | button aria-label |
| `auth.saveToCache` | 保存凭据复选框 | el-checkbox |
| `dialog.urlInvalid` | URL 格式校验错误提示 | p 标签 |
| `error.usernameWhitespace` | 用户名纯空白校验 | validateAuth() 校验 |
| `error.passwordEmpty` | 用户名非空但密码为空校验 | validateAuth() 校验 |
| `dialog.cancel` | 取消按钮 | el-button |

注：认证区域的 `is-error` class 已改为 Element Plus 标准 `validate-status="error"` 属性，通过 `<el-form-item :validate-status>` 实现错误状态样式，无需手动添加 class。

### 6.6B — UpdateRevisionDialog

> **对话框入场动画：** 所有 `el-dialog` 默认内置 200ms scale(0.95→1) + fade 动画（`transition="el-dialog-fade"`），无需自定义 CSS。操作进行中（loading）时设置 `:close-on-click-modal="false"` 禁止遮罩关闭。

el-dialog 420px。当前版本只读显示，目标版本 ElInputNumber（正整数校验，留空=HEAD），深度下拉（5 选项，默认"不修改"）。更新中按钮 loading。

**完整代码：**

```vue
<script setup lang="ts">
import { ref, reactive, computed } from 'vue'
import { useSvnStore } from '@/stores/svn'
import { useWorkspaceStore } from '@/stores/workspace'
import { useI18n } from 'vue-i18n'

const { t } = useI18n()
const svnStore = useSvnStore()
const workspaceStore = useWorkspaceStore()

const emit = defineEmits<{ close: [] }>()

// ── 表单状态 ──
const currentRevision = ref(workspaceStore.currentRevision)
const targetRevision = ref<number | undefined>(undefined)
const updateDepth = ref('') // '' = 不修改
const isUpdating = ref(false)

const SUPPORTED_DEPTHS = [
  { value: '', label: 'dialog.depthUnchanged' },
  { value: 'infinity', label: 'dialog.depthInfinity' },
  { value: 'immediates', label: 'dialog.depthImmediates' },
  { value: 'children', label: 'dialog.depthChildren' },
  { value: 'empty', label: 'dialog.depthEmpty' },
]

async function handleUpdate() {
  if (!workspaceStore.currentPath) return
  isUpdating.value = true
  try {
    await svnStore.update({
      path: workspaceStore.currentPath,
      revision: targetRevision.value,
      depth: updateDepth.value || undefined,
    })
    emit('close')
  } finally {
    isUpdating.value = false
  }
}
</script>

<template>
  <el-dialog :model-value="true" :title="t('dialog.updateRevision')" width="420px"
    :close-on-click-modal="false" @close="emit('close')">
    <div class="flex flex-col gap-4">
      <!-- 当前版本（只读） -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.currentRevision') }}
        </label>
        <el-input :model-value="String(currentRevision)" size="small" disabled />
      </div>

      <!-- 目标版本 -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.targetRevision') }}
        </label>
        <el-input-number v-model="targetRevision" :min="1" size="small"
          :placeholder="t('common.optional')" class="!w-32" controls-position="right" />
        <p class="text-xs text-slate-400 dark:text-slate-500 mt-1">
          {{ t('dialog.revisionOptional') }}
        </p>
      </div>

      <!-- 深度下拉 -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.depth') }}
        </label>
        <el-select v-model="updateDepth" size="small" class="!w-full">
          <el-option v-for="opt in SUPPORTED_DEPTHS" :key="opt.value"
            :value="opt.value" :label="t(opt.label)" />
        </el-select>
      </div>
    </div>

    <template #footer>
      <el-button size="default" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="emit('close')" :disabled="isUpdating">
        {{ t('dialog.cancel') }}
      </el-button>
      <el-button size="default" type="primary" :loading="isUpdating"
        class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="handleUpdate">
        {{ t('dialog.update') }}
      </el-button>
    </template>
  </el-dialog>
</template>
```

### 6.6C — ExportDialog

> **对话框入场动画：** 所有 `el-dialog` 默认内置 200ms scale(0.95→1) + fade 动画（`transition="el-dialog-fade"`），无需自定义 CSS。操作进行中（loading）时设置 `:close-on-click-modal="false"` 禁止遮罩关闭。

el-dialog 480px。工作副本路径只读，导出目标路径+浏览，版本号可选 ElInputNumber，仅导出未版本化复选框。导出中按钮 loading。

**完整代码：**

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useSvnStore } from '@/stores/svn'
import { useWorkspaceStore } from '@/stores/workspace'
import { useI18n } from 'vue-i18n'
import { open } from '@tauri-apps/plugin-dialog'

const { t } = useI18n()
const svnStore = useSvnStore()
const workspaceStore = useWorkspaceStore()

const emit = defineEmits<{ close: [] }>()

// ── 表单状态 ──
const targetDir = ref('')
const revision = ref<number | undefined>(undefined)
const ignoreExternals = ref(false)
const isExporting = ref(false)

const canExport = computed(() => !!workspaceStore.currentPath && !!targetDir.value)

// 路径浏览
async function browseTarget() {
  const selected = await open({ directory: true, title: t('dialog.selectExportDir') })
  if (selected) targetDir.value = selected
}

async function handleExport() {
  if (!canExport.value) return
  isExporting.value = true
  try {
    // 统一为对象参数（与 7.2 services/svn.ts exportWorkspace 签名一致）
    await svnStore.exportWorkspace({
      path: workspaceStore.currentPath,
      targetDir: targetDir.value,
      revision: revision.value,
      ignoreExternals: ignoreExternals.value,
    })
    emit('close')
  } finally {
    isExporting.value = false
  }
}
</script>

<template>
  <el-dialog :model-value="true" :title="t('dialog.exportDialog')" width="480px"
    :close-on-click-modal="false" @close="emit('close')">
    <div class="flex flex-col gap-4">
      <!-- 工作副本路径（只读） -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.workspacePath') }}
        </label>
        <el-input :model-value="workspaceStore.currentPath" size="small" disabled />
      </div>

      <!-- 导出目标路径 -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('dialog.exportTargetPath') }}
        </label>
        <div class="flex gap-2">
          <el-input v-model="targetDir" size="small" :placeholder="t('dialog.selectExportDir')" />
          <el-button size="small" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="browseTarget">{{ t('dialog.browse') }}</el-button>
        </div>
      </div>

      <!-- 版本号（可选） -->
      <div>
        <label class="text-xs font-medium text-slate-700 dark:text-slate-300 mb-1 block">
          {{ t('common.revision') }}
        </label>
        <el-input-number v-model="revision" :min="1" size="small"
          :placeholder="t('common.optional')" class="!w-32" controls-position="right" />
        <p class="text-xs text-slate-400 dark:text-slate-500 mt-1">
          {{ t('dialog.revisionOptional') }}
        </p>
      </div>

      <!-- 忽略外部引用 -->
      <el-checkbox v-model="ignoreExternals" size="small">
        {{ t('dialog.ignoreExternals') }}
      </el-checkbox>
    </div>

    <template #footer>
      <el-button size="default" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="emit('close')" :disabled="isExporting">
        {{ t('dialog.cancel') }}
      </el-button>
      <el-button size="default" type="primary" :loading="isExporting"
        :disabled="!canExport" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="handleExport">
        {{ t('dialog.export') }}
      </el-button>
    </template>
  </el-dialog>
</template>
```

### 6.6D — ConfirmDialog（通用）

> **对话框入场动画：** 所有 `el-dialog` 默认内置 200ms scale(0.95→1) + fade 动画（`transition="el-dialog-fade"`），无需自定义 CSS。

```vue
<script setup lang="ts">
const { t } = useI18n()
const props = withDefaults(defineProps<{ title: string; message: string; confirmText?: string; cancelText?: string; fileList?: string[]; type?: 'danger' | 'warning' | 'info' }>(), { confirmText: '', cancelText: '', type: 'danger' })
const emit = defineEmits<{ confirm: []; cancel: [] }>()
</script>
<template>
  <el-dialog :model-value="true" :title="title" width="420px" @close="emit('cancel')">
    <p class="text-sm text-slate-600 dark:text-slate-400">{{ message }}</p>
    <div v-if="fileList?.length" class="mt-3 max-h-32 overflow-y-auto">
      <div v-for="f in fileList" :key="f" class="text-xs font-mono text-slate-600 py-0.5">{{ f }}</div>
    </div>
    <p v-if="type === 'danger'" class="text-xs text-red-500 mt-2">{{ t('file.irreversibleAction') }}</p>
    <template #footer>
      <el-button size="default" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="emit('cancel')">{{ cancelText }}</el-button>
      <el-button size="default" :type="type === 'danger' ? 'danger' : 'primary'" class="focus:ring-2 focus:ring-blue-400 focus:outline-none" @click="emit('confirm')">{{ confirmText }}</el-button>
    </template>
  </el-dialog>
</template>
```

## 验收标准

- [ ] 类型检查通过
- [ ] CheckoutDialog URL 校验，深度 4 选项，ignoreExternals，"仅此目录"复选框，认证信息区域（v5 新增）+ validateAuth() 校验逻辑，loading 状态
- [ ] ConfirmDialog 使用 withDefaults(defineProps<...>()) 单次调用（无重复 defineProps）
- [ ] UpdateRevisionDialog 版本号数字校验（el-input-number min=1），深度 5 选项（含"不修改"），当前版本只读，更新中按钮 loading
- [ ] ExportDialog 路径只读+浏览，版本可选 ElInputNumber，仅未版本化复选框，导出目标路径浏览按钮
- [ ] UpdateRevisionDialog 和 ExportDialog 补充 i18n key，说明各 key 对应使用的 UI 元素：
      - **UpdateRevisionDialog** — `dialog.updateRevision`（弹窗标题）/ `dialog.currentRevision`（当前版本标签）/ `dialog.targetRevision`（目标版本标签）/ `dialog.revisionOptional`（目标版本提示文字）/ `dialog.depthUnchanged`（深度下拉-不修改选项）/ `dialog.depthInfinity`（深度下拉-无限）/ `dialog.depthImmediates`（深度下拉-立即子项）/ `dialog.depthChildren`（深度下拉-子项）/ `dialog.depthEmpty`（深度下拉-仅此目录）/ `dialog.update`（确认按钮）/ `dialog.cancel`（取消按钮）
      - **ExportDialog** — `dialog.exportDialog`（弹窗标题）/ `dialog.workspacePath`（工作副本路径标签）/ `dialog.exportTargetPath`（导出目标路径标签）/ `dialog.selectExportDir`（浏览按钮 tooltip）/ `dialog.browse`（浏览按钮文字）/ `common.revision`（版本号标签）/ `common.optional`（可选占位）/ `dialog.ignoreExternals`（忽略外部引用复选框）/ `dialog.export`（确认按钮）/ `dialog.cancel`（取消按钮）
- [ ] ConfirmDialog title/message/fileList 可配，确认按钮 danger
- [ ] 全部 el-dialog 200ms 入场动画（transition="el-dialog-fade"），操作中禁止遮罩关闭；所有交互元素（button/a/input）追加 focus:ring-2 focus:ring-blue-400 focus:outline-none 焦点指示器；对话框必填字段使用 el-form :rules 或 class="is-error" 标记
- [ ] 无 `<style>` 标签
