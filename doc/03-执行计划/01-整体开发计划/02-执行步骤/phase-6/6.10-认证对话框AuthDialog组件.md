# 6.10 AuthDialog 认证对话框组件

> 对应设计文档 v5 §5.14 认证重试对话框 + §5.15 凭据管理对话框
> 前置状态：步骤 4.1~4.3 已完成（类型定义、Stores、Router、i18n），6.6（CheckoutDialog 含认证输入区）已完成

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src/components/dialogs/AuthDialog.vue` | **新建** — 认证对话框（retry 重试模式 / manage 凭据管理模式）|
| `src/locales/zh-CN.json` | **修改** — 追加认证相关翻译 |
| `src/locales/en.json` | **修改** — 追加认证相关翻译 |

## 操作细节

### 6.10A — 新建 `src/components/dialogs/AuthDialog.vue`

AuthDialog 支持两种使用模式，通过 `mode` prop 控制：

**模式 1：retry（认证重试）** — 服务端操作认证失败后弹出
- 显示失败操作的信息（仓库 URL、操作类型）
- 用户输入凭据 → [重试] → 通过 emit 回调执行重试
- 取消 → 关闭对话框，不执行操作

**模式 2：manage（凭据管理）** — 通过 TopBar [🔑] 按钮打开
- 显示当前工作副本的仓库 URL
- 用户输入凭据 → [{{ t('auth.testConnection') }}] → [{{ t('auth.clearCache') }}] → [关闭]
- 凭据存储在会话凭据池中

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useI18n } from 'vue-i18n'
import { KeyRound, Eye, EyeOff, CheckCircle, XCircle, Loader2 } from 'lucide-vue-next'
import { testConnection, clearCredentials } from '@/services/svn'

const { t } = useI18n()

type AuthMode = 'retry' | 'manage'

interface Props {
  mode: AuthMode
  /** retry 模式下：失败操作的描述信息 */
  operationLabel?: string
  /** 仓库 URL */
  repoUrl?: string
  /** retry 模式下：要重新执行的函数（接收 credentials） */
  onRetry?: (username: string, password: string, saveToCache: boolean) => Promise<boolean>
}

const props = withDefaults(defineProps<Props>(), {
  mode: 'retry',
  operationLabel: '',
  repoUrl: '',
})

const emit = defineEmits<{
  close: []
  success: []
}>()

// ── 表单状态 ──
const username = ref('')
const password = ref('')
const saveToCache = ref(false)
const showPassword = ref(false)
const isProcessing = ref(false)
const statusMessage = ref('')
const statusType = ref<'success' | 'error' | ''>('')

const canRetry = computed(() => username.value.trim().length > 0 && password.value.length > 0)
const dialogTitle = computed(() =>
  props.mode === 'retry' ? t('auth.authFailedTitle') : t('auth.credentialManagement')
)

// ── 重试（retry 模式） ──
async function handleRetry() {
  if (!canRetry.value || !props.onRetry) return
  isProcessing.value = true
  statusMessage.value = ''
  statusType.value = ''

  try {
    const ok = await props.onRetry(username.value, password.value, saveToCache.value)
    if (ok) {
      statusMessage.value = t('auth.operationSuccess')
      statusType.value = 'success'
      setTimeout(() => emit('success'), 800)
    } else {
      statusMessage.value = t('auth.authFailedMessage')
      statusType.value = 'error'
    }
  } catch (err: any) {
    statusMessage.value = err?.message || t('auth.operationFailed')
    statusType.value = 'error'
  } finally {
    isProcessing.value = false
  }
}

// ── {{ t('auth.testConnection') }}（manage 模式） ──
async function handleTestConnection() {
  if (!canRetry.value) return
  isProcessing.value = true
  statusMessage.value = ''
  statusType.value = ''

  try {
    const msg = await testConnection(props.repoUrl || '', username.value, password.value)
    statusMessage.value = msg
    statusType.value = 'success'
  } catch (err: any) {
    statusMessage.value = err?.message || t('auth.connectionFailed')
    statusType.value = 'error'
  } finally {
    isProcessing.value = false
  }
}

// ── {{ t('auth.clearCache') }}（manage 模式） ──
async function handleClearCache() {
  isProcessing.value = true
  statusMessage.value = ''
  statusType.value = ''

  try {
    const msg = await clearCredentials(props.repoUrl || '')
    statusMessage.value = msg
    statusType.value = 'success'
  } catch (err: any) {
    statusMessage.value = err?.message || t('auth.clearFailed')
    statusType.value = 'error'
  } finally {
    isProcessing.value = false
  }
}
</script>

<template>
  <el-dialog
    :model-value="true"
    :title="dialogTitle"
    :width="mode === 'retry' ? '440px' : '480px'"
    :close-on-click-modal="false"
    :close-on-press-escape="true"
    @close="emit('close')"
  >
    <div class="space-y-4">
      <div v-if="repoUrl" class="text-sm text-slate-600 dark:text-slate-400">
        <div class="font-medium text-slate-800 dark:text-slate-200">SVN 服务器需要认证</div>
        <div class="mt-1 font-mono text-xs">{{ repoUrl }}</div>
        <div v-if="mode === 'retry' && operationLabel" class="text-xs text-slate-500 mt-0.5">
          操作: {{ operationLabel }}
        </div>
      </div>

      <div>
        <label class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">{{ t('auth.username') }}</label>
        <el-input
          v-model="username"
          placeholder="输入 SVN 用户名"
          :disabled="isProcessing"
          size="default"
          @keyup.enter="mode === 'retry' ? handleRetry() : emit('close')"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1">{{ t('auth.password') }}</label>
        <div class="relative">
          <el-input
            v-model="password"
            :type="showPassword ? 'text' : 'password'"
            placeholder="输入密码"
            :disabled="isProcessing"
            size="default"
            @keyup.enter="mode === 'retry' ? handleRetry() : emit('close')"
          />
          <button
            class="absolute right-2 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600"
            @click="showPassword = !showPassword"
            :aria-label="showPassword ? t('auth.hidePassword') : t('auth.showPassword')"
          >
            <EyeOff v-if="showPassword" class="w-4 h-4" />
            <Eye v-else class="w-4 h-4" />
          </button>
        </div>
      </div>

      <el-checkbox v-model="saveToCache" :disabled="isProcessing">
        <span class="text-sm">{{ t('auth.saveToCache') }}</span>
      </el-checkbox>

      <div
        v-if="statusMessage"
        class="flex items-center gap-2 text-sm px-3 py-2 rounded-md"
        :class="statusType === 'success'
          ? 'bg-green-50 dark:bg-green-900/20 text-green-700 dark:text-green-400'
          : 'bg-red-50 dark:bg-red-900/20 text-red-600 dark:text-red-400'"
      >
        <CheckCircle v-if="statusType === 'success'" class="w-4 h-4 shrink-0" />
        <XCircle v-else class="w-4 h-4 shrink-0" />
        <span>{{ statusMessage }}</span>
      </div>
    </div>

    <template #footer>
      <div class="flex items-center gap-2" :class="mode === 'manage' ? 'justify-between' : 'justify-end'">
        <div v-if="mode === 'manage'" class="flex items-center gap-2">
          <el-button size="small" :disabled="isProcessing || !canRetry" @click="handleTestConnection">
            <Loader2 v-if="isProcessing" class="w-4 h-4 mr-1 animate-spin" />
            {{ t('auth.testConnection') }}
          </el-button>
          <el-button size="small" :disabled="isProcessing" @click="handleClearCache">
            {{ t('auth.clearCache') }}
          </el-button>
        </div>

        <div class="flex items-center gap-2">
          <el-button @click="emit('close')" :disabled="isProcessing">
            {{ mode === 'retry' ? t('auth.cancelOperation') : t('common.close') }}
          </el-button>
          <el-button
            v-if="mode === 'retry'"
            type="primary"
            :loading="isProcessing"
            :disabled="!canRetry"
            @click="handleRetry"
          >
            {{ isProcessing ? t('auth.retrying') : t('auth.retry') }}
          </el-button>
        </div>
      </div>
    </template>
  </el-dialog>
</template>
```

**关键交互逻辑：**

| 场景 | 触发 | emit | 行为 |
|------|------|------|------|
| retry + 重试成功 | `handleRetry()` → `onRetry()` 返回 true | `success` | 显示成功提示 0.8s 后关闭 |
| retry + 重试失败 | `handleRetry()` → `onRetry()` 返回 false | 无 | 保留对话框，显示错误信息 |
| manage + {{ t('auth.testConnection') }} | `handleTestConnection()` | 无 | 调用 services/svn.ts 的 `testConnection` |
| manage + {{ t('auth.clearCache') }} | `handleClearCache()` | 无 | 调用 services/svn.ts 的 `clearCredentials` |
| 点击取消/关闭 | — | `close` | 关闭对话框 |

**SVN_AUTH_FAILED 自动弹出 AuthDialog 的完整联动流程（业务功能 §8.3 + 视觉设计 §4.2）：**

AuthDialog 本身不包含自动触发逻辑。触发链路通过 svnStore（步骤 4.2）的状态驱动：

```
后端 command 返回 AppError::SvnAuthFailed
       ↓
svnStore.call() 捕获到 SVN_AUTH_FAILED / E170001
  → authFailed.value = true
  → authContext.value = { command, args, errorMessage }
  → 错误继续 throw（组件自行处理 catch 或全局 error handler）
       ↓
App.vue watch(authFailed, (v) => { if (v) showAuthDialog = true })
       ↓
渲染 <AuthDialog mode=retry :onRetry=svnStore.retryAuth />
       ↓
用户在对话框中输入凭据 → 点击重试
  → AuthDialog 调用 onRetry(username, password, saveToCache)
  → svnStore.retryAuth() 将原 command + args + credentials 重新 invoke
  → retry 成功：authFailed=false, AuthDialog 关闭, 返回 success
  → retry 失败：保留对话框, 显示错误信息
       ↓
用户点击取消 → svnStore.cancelAuth() → authFailed=false, AuthDialog 关闭
```

**要点说明：**
1. `svnStore.call()` 覆盖所有 invoke 方法，任一 command 返回 SVN_AUTH_FAILED 都会触发认证流程
2. 前端 watch(authFailed) 在 App.vue 的 setup 中注册，确保全局生效
3. `retryAuth()` 在重试时将 credentials 注入到原 args 中，后端使用 --password-from-stdin 传递密码
4. AuthDialog 的 onRetry prop 签名与 svnStore.retryAuth() 完全匹配
5. 遇到认证失败时不自动弹出 toast（由 AuthDialog 处理而非 ElMessage），避免重复通知

## 验收标准

- [ ] `cnpm run lint` 类型检查通过
- [ ] AuthDialog 支持 retry 和 manage 两种模式
- [ ] retry 模式显示仓库 URL、操作类型、用户名/密码输入、保存凭据复选框
- [ ] manage 模式包含[{{ t('auth.testConnection') }}][{{ t('auth.clearCache') }}]按钮
- [ ] 密码输入框支持 👁 显示/隐藏
- [ ] 用户名和密码均非空时操作按钮才启用
- [ ] SVN_AUTH_FAILED 时 svnStore.call() 自动设置 authFailed/authContext，前端 watch 触发 AuthDialog
- [ ] retryAuth() 将原 command + args + credentials 重新 invoke，成功后清除认证状态
- [ ] 无 `<style>` 标签，仅使用 Tailwind 和 Element Plus 组件
