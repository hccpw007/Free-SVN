# 6.1 HomePage 欢迎页与空状态

> 对应开发计划 v2 步骤 6A.1 + 6A.7：HomePage 欢迎页状态 + 三种空状态
> 前置状态：阶段五已完成（MainLayout/TopBar/ToolBar/StatusBar），workspaceStore/fileListStore 已可用

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src/pages/HomePage.vue` | **修改** — 从占位组件替换为完整欢迎页 + 空状态逻辑 |
| `src/components/svn/FileListTable.vue` | **新建** — 文件表格组件骨架（6.2 完善） |
| `src/locales/zh-CN.json` | **修改** — 补充欢迎页/空状态翻译 key |
| `src/locales/en.json` | **修改** — 同上 |

## 操作细节

### 6.1A — 修改 `src/pages/HomePage.vue`

完整 HomePage 包含欢迎页状态和变更列表状态两种模式，通过 `workspaceStore.currentPath` 切换。

**双模式切换逻辑：**

```vue
<script setup lang="ts">
import { useRouter } from 'vue-router'
import { useWorkspaceStore } from '@/stores/workspace'
import { useFileListStore } from '@/stores/fileList'
import { computed, onMounted, watch } from 'vue'
import { useI18n } from 'vue-i18n'
import { ElMessage } from 'element-plus'
import { RefreshCw, X } from 'lucide-vue-next'
import FileListTable from '@/components/svn/FileListTable.vue'

const router = useRouter()
const { t } = useI18n()
const workspaceStore = useWorkspaceStore()
const fileListStore = useFileListStore()

const isWelcomePage = computed(() => !workspaceStore.currentPath)
const isEmptyChanges = computed(() => !!workspaceStore.currentPath && fileListStore.files.length === 0)

// 搜索/筛选后 filteredFiles 为空但 files 不为空
const isSearchEmpty = computed(() => {
  return !!workspaceStore.currentPath
    && fileListStore.files.length > 0
    && fileListStore.filteredFiles.length === 0
})

onMounted(async () => {
  if (workspaceStore.currentPath) {
    // 检查工作副本路径是否存在（Tauri fs API）
    try {
      const { exists } = await import('@tauri-apps/plugin-fs')
      const pathExists = await exists(workspaceStore.currentPath)
      if (!pathExists) {
        ElMessage.warning(t('workspace.pathNotFound'))
        await workspaceStore.switchWorkspace('')
        return
      }
    } catch {
      // fallback: 路径检查失败时继续加载
    }
    await fileListStore.refresh()
  }
})

function handleCheckout() {
  // 打开检出对话框（阶段六后续实现）
}

async function handleOpenWorkspace() {
  // 调用 Tauri dialog.open 选择目录（需 @tauri-apps/plugin-dialog）
  try {
    const { open } = await import('@tauri-apps/plugin-dialog')
    const selected = await open({ directory: true })
    if (selected) {
      await workspaceStore.switchWorkspace(selected as string)
    }
  } catch {
    // fallback
  }
}
</script>

<template>
  <!-- 欢迎页（无工作副本） -->
  <div v-if="isWelcomePage" class="h-full flex items-center justify-center">
    <div class="text-center max-w-md px-8">
      <h1 class="text-3xl font-bold text-slate-800 dark:text-slate-100">
        {{ t('app.welcomeTitle') }}
      </h1>
      <p class="mt-2 text-sm text-slate-500 dark:text-slate-400">
        {{ t('app.welcomeSubtitle') }}
      </p>
      <div class="mt-8 flex flex-col items-center gap-3">
        <button class="w-56 px-4 py-2.5 rounded-md bg-green-500 hover:bg-green-600 text-white text-sm font-medium transition-colors duration-150" @click="handleCheckout">
          {{ t('common.checkout') }}...
        </button>
        <button class="w-56 px-4 py-2 rounded-md border border-slate-300 dark:border-slate-600 text-slate-700 dark:text-slate-300 text-sm hover:bg-slate-50 dark:hover:bg-slate-800 transition-colors duration-150" @click="handleOpenWorkspace">
          {{ t('workspace.openExisting') }}
        </button>
      </div>
      <div class="mt-8 text-left">
        <p class="text-xs font-medium text-slate-400 dark:text-slate-500 mb-2 uppercase tracking-wider">
          {{ t('workspace.recentWorkspaces') }}
        </p>
        <div v-if="workspaceStore.recentWorkspaces.length === 0" class="text-xs text-slate-400 dark:text-slate-500 italic">
          {{ t('workspace.noRecentWorkspaces') }}
        </div>
        <div v-else class="space-y-1">
          <div v-for="wp in workspaceStore.recentWorkspaces" :key="wp"
            class="flex items-center justify-between px-3 py-1.5 rounded-md text-xs text-slate-600 dark:text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800 cursor-pointer group transition-colors duration-150 font-mono"
            @click="workspaceStore.switchWorkspace(wp)"
          >
            <span class="truncate">{{ wp }}</span>
            <button class="opacity-0 group-hover:opacity-100 text-slate-400 hover:text-red-500 transition-opacity duration-150" :title="t('common.remove')" @click.stop="workspaceStore.removeRecentWorkspace(wp)">
              <X class="w-3.5 h-3.5" />
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- 有工作副本：变更列表视图 -->
  <div v-else class="h-full flex flex-col">
    <!-- 搜索栏 + 筛选 + 刷新 -->
    <div class="px-4 py-2 flex items-center gap-3 bg-white dark:bg-slate-800 border-b border-slate-200 dark:border-slate-700">
      <el-input v-model="fileListStore.searchQuery" :placeholder="t('workspace.searchPlaceholder')" size="default" clearable class="max-w-xs" @input="fileListStore.applyFilter()" />
      <!--
        搜索框 300ms 防抖实现在 fileListStore.applyFilter() 内部
        或通过 watch searchQuery + setTimeout(300) 实现：
        watch(() => fileListStore.searchQuery, () => { clearTimeout(timer); timer = setTimeout(() => fileListStore.applyFilter(), 300) })
      -->
      <el-select v-model="fileListStore.filterStatus" size="default" class="!w-36" @change="fileListStore.applyFilter()">
        <el-option :label="t('file.filterAll')" value="all" />
        <el-option :label="t('file.statusModified')" value="modified" />
        <el-option :label="t('file.statusAdded')" value="added" />
        <el-option :label="t('file.statusConflicted')" value="conflicted" />
        <el-option :label="t('file.statusDeleted')" value="deleted" />
        <el-option :label="t('file.statusUnversioned')" value="unversioned" />
      </el-select>
      <button class="p-1.5 rounded-md text-slate-400 hover:text-slate-600 dark:hover:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors duration-150" :title="t('common.refresh')" aria-label="刷新" @click="fileListStore.refresh()">
        <RefreshCw class="w-4 h-4" />
      </button>
    </div>

    <!-- 空变更状态 -->
    <div v-if="isEmptyChanges" class="flex-1 flex items-center justify-center">
      <div class="text-center">
        <p class="text-sm text-slate-500 dark:text-slate-400">✅ {{ t('workspace.noChanges') }}</p>
        <p class="text-xs text-slate-400 dark:text-slate-500 mt-1 font-mono">{{ t('workspace.latestVersion') }}: {{ workspaceStore.currentRevision }}</p>
        <p class="text-xs text-slate-400 dark:text-slate-500 font-mono">{{ t('workspace.lastCommit') }}: {{ workspaceStore.lastCommitTime }}</p>
        <div class="mt-4 flex gap-3 justify-center">
          <button class="text-xs text-blue-600 hover:underline" @click="fileListStore.refresh()">{{ t('common.refresh') }}</button>
          <button class="text-xs text-blue-600 hover:underline" @click="router.push('/workspace/log')">{{ t('workspace.viewLog') }}</button>
        </div>
      </div>
    </div>

    <!-- 搜索/筛选空结果 -->
    <div v-else-if="isSearchEmpty" class="flex-1 flex items-center justify-center">
      <div class="text-center">
        <p class="text-sm text-slate-500 dark:text-slate-400">🔍 {{ t('workspace.noSearchResult') }}</p>
        <p class="text-xs text-slate-400 dark:text-slate-500 mt-1">{{ t('workspace.searchFilterHint') }}</p>
        <button class="mt-2 text-xs text-blue-600 hover:underline" @click="fileListStore.clearFilter()">{{ t('workspace.clearFilter') }}</button>
      </div>
    </div>

    <!-- 文件列表表格 -->
    <FileListTable v-else class="flex-1" />
  </div>
</template>
```

**三种空状态互斥逻辑：**
- `isWelcomePage`（优先最高）：`!workspaceStore.currentPath` → 欢迎页
- `isEmptyChanges`：`files.length === 0` → "工作副本无变更"
- `isSearchEmpty`：`files.length > 0 && filteredFiles.length === 0` → "未找到匹配的文件"
- 其他 → FileListTable 正常渲染

**i18n 补充：**

在 `zh-CN.json` / `en.json` 的 `workspace` 和 `file` 分类追加：

```json
{
  "workspace": {
    "openExisting": "打开已有工作副本",
    "recentWorkspaces": "最近打开的工作副本",
    "searchPlaceholder": "按文件名搜索...",
    "viewLog": "查看日志",
    "remove": "移除",
    "searchFilterHint": "尝试修改搜索条件或筛选类型",
    "pathNotFound": "工作副本路径不存在，请重新选择"
  },
  "file": {
    "filterAll": "全部文件"
  }
}
```

### 6.1B — 创建 `src/components/svn/FileListTable.vue` 骨架

```vue
<script setup lang="ts">
</script>
<template>
  <div class="h-full">
    <!-- 6.2 完整实现 -->
  </div>
</template>
```

## 验收标准

- [ ] 类型检查通过
- [ ] 无工作副本时显示欢迎页（标题 + 副标题 + 检出按钮 + 打开按钮 + 最近列表）
- [ ] 检出按钮 green-500 填充，打开按钮 outline 样式
- [ ] 最近列表为空显示占位文字，有记录时每行 hover 显示移除 X 按钮
- [ ] 三种空状态通过 `v-if`/`v-else-if`/`v-else` 互斥
- [ ] 搜索框 placeholder 为"按文件名搜索..."
- [ ] 无 `<style>` 标签
- [ ] onMounted 中检查工作副本路径有效性，不存在时弹出警告并切回欢迎页
- [ ] 搜索框 300ms 防抖（在 fileListStore.applyFilter 或 watch 中实现）
- [ ] i18n 包含 pathNotFound
