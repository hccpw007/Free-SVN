# 6.4 CommitPage 提交面板与 CommitForm 组件

> 对应开发计划 v2 步骤 6B.1~6B.3：提交页面 + 提交日志输入 + 提交流程
> 前置状态：步骤 6.1~6.2 已完成，svnStore 可用

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src/pages/CommitPage.vue` | **新建** — 提交面板页面 |
| `src/components/svn/CommitForm.vue` | **新建** — 提交信息输入组件 |
| `src/components/dialogs/ConfirmDialog.vue` | **新建** — 通用确认对话框骨架 |

## 操作细节

### 6.4A — 新建 `src/pages/CommitPage.vue`

路由 `/workspace/commit`，继承 MainLayout。四列文件列表（复选框/状态/文件/大小）+ CommitForm + 底部操作栏。选择状态独立副本规则：进入时复制 fileListStore.selectedPaths，修改不影响主视图，提交后双清。

```vue
<script setup lang="ts">
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'
import { useFileListStore } from '@/stores/fileList'
import { useSvnStore } from '@/stores/svn'
import { useI18n } from 'vue-i18n'
import { ArrowLeft } from 'lucide-vue-next'
import { ElMessage, ElMessageBox } from 'element-plus'
import CommitForm from '@/components/svn/CommitForm.vue'

const router = useRouter()
const { t } = useI18n()
const fileListStore = useFileListStore()
const svnStore = useSvnStore()

const commitSelected = ref<Set<string>>(new Set())
const commitMessage = ref('')
const isSubmitting = ref(false)
const submitError = ref('')
const commitFormRef = ref<InstanceType<typeof CommitForm> | null>(null)

onMounted(() => { commitSelected.value = new Set(fileListStore.selectedPaths) })

const commitFiles = computed(() => fileListStore.files)
const selectedCount = computed(() => commitSelected.value.size)

function toggleFile(path: string) {
  const s = new Set(commitSelected.value)
  s.has(path) ? s.delete(path) : s.add(path)
  commitSelected.value = s
}

async function handleCommit() {
  if (selectedCount.value === 0) return
  if (!commitMessage.value.trim()) {
    // 空信息确认（使用 ElMessageBox.confirm）
    try {
      await ElMessageBox.confirm(t('file.emptyCommitConfirm'), t('common.confirm'), { confirmButtonText: t('common.submit'), cancelButtonText: t('common.cancel'), type: 'warning' })
    } catch { return } // 用户取消
  }
  isSubmitting.value = true
  submitError.value = ''
  try {
    const result = await svnStore.commit({ paths: Array.from(commitSelected.value), message: commitMessage.value, keepLocks: false })
    fileListStore.selectedPaths = new Set()
    commitSelected.value = new Set()
    // 提交成功后持久化保存 message（上限 20 条）
    commitFormRef.value?.saveRecentMessage(commitMessage.value)
    ElMessage.success(t('file.commitSuccess', { rev: result.detail || result.result }))
    router.push('/workspace')
  } catch (e: any) {
    submitError.value = e.message || t('error.SVN_EXEC_FAILED')
  } finally { isSubmitting.value = false }
}
</script>

<template>
  <div class="h-full flex flex-col">
    <div class="h-10 px-4 flex items-center gap-2 border-b border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 shrink-0">
      <button class="flex items-center gap-1 text-xs text-slate-500 hover:text-slate-700" @click="router.push('/workspace')">
        <ArrowLeft class="w-4 h-4" />{{ t('common.back') }}
      </button>
    </div>
    <div class="flex-1 min-h-0 overflow-auto p-4">
      <el-table :data="commitFiles" size="small" style="width:100%" row-key="path" @row-click="(r:any) => toggleFile(r.path)">
        <el-table-column width="40">
          <template #default="{ row }"><el-checkbox :model-value="commitSelected.has(row.path)" @change="() => toggleFile(row.path)" /></template>
        </el-table-column>
        <el-table-column :label="t('file.status')" width="48">
          <template #default="{ row }"><span class="text-xs font-mono">{{ row.status }}</span></template>
        </el-table-column>
        <el-table-column :label="t('file.fileName')" min-width="200">
          <template #default="{ row }"><span class="text-xs font-mono truncate block">{{ row.path }}</span></template>
        </el-table-column>
        <el-table-column :label="t('file.fileSize')" width="80">
          <template #default="{ row }"><span class="text-xs font-mono text-slate-400">{{ row.size || '-' }}</span></template>
        </el-table-column>
      </el-table>
    </div>
    <CommitForm v-model="commitMessage" ref="commitFormRef" />
    <div v-if="submitError" class="px-4 py-2 text-xs text-red-600 bg-red-50 dark:bg-red-900/20 border-t border-red-200 dark:border-red-800">{{ submitError }}</div>
    <div class="h-12 px-4 flex items-center justify-between shrink-0 border-t border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800">
      <span class="text-xs text-slate-500">{{ t('workspace.selectedCount', { count: selectedCount }) }}</span>
      <div class="flex items-center gap-2">
        <button class="px-4 py-1.5 text-xs rounded-md border border-slate-300 dark:border-slate-600 text-slate-600 dark:text-slate-400 hover:bg-slate-50 dark:hover:bg-slate-700" @click="router.push('/workspace')">{{ t('common.cancel') }}</button>
        <el-tooltip :content="t('workspace.selectFiles')" :disabled="selectedCount > 0" effect="dark">
          <button class="px-4 py-1.5 text-xs rounded-md font-medium text-white bg-green-500 hover:bg-green-600 disabled:opacity-50 disabled:cursor-not-allowed transition-colors" :disabled="selectedCount === 0 || isSubmitting" @click="handleCommit">
            {{ isSubmitting ? t('common.submitting') : t('common.submit') }}
          </button>
        </el-tooltip>
      </div>
    </div>
  </div>
</template>
```

### 6.4B — 新建 `src/components/svn/CommitForm.vue`

```vue
<script setup lang="ts">
import { ref, watch, onMounted } from 'vue'
import { Store } from '@tauri-apps/plugin-store'
import { useI18n } from 'vue-i18n'

const model = defineModel<string>({ required: true })
const { t } = useI18n()
const charCount = ref(0)
const recentMessages = ref<string[]>([])
const selectedRecentMsg = ref('')

watch(model, (v) => { charCount.value = v.length })

// 从 tauri-plugin-store 读取本地持久化的最近提交日志（上限 20 条）
const RECENT_MSG_KEY = 'recentCommitMessages'
onMounted(async () => {
  try {
    const store = await Store.load('settings.json')
    const saved = await store.get<string[]>(RECENT_MSG_KEY)
    recentMessages.value = saved || []
  } catch { /* 读取失败不影响提交流程 */ }
})

// 被外部调用：提交成功后保存 message 到持久化存储
async function saveRecentMessage(msg: string) {
  if (!msg.trim()) return
  try {
    const store = await Store.load('settings.json')
    const saved = (await store.get<string[]>(RECENT_MSG_KEY)) || []
    // 去重：如果已存在则移除旧位置再添加到最前
    const filtered = saved.filter(m => m !== msg)
    const updated = [msg, ...filtered].slice(0, 20)
    recentMessages.value = updated
    await store.set(RECENT_MSG_KEY, updated)
    await store.save()
  } catch { /* 持久化失败不影响提交流程 */ }
}

function selectRecent(msg: string) { model.value = msg }

// 暴露 saveRecentMessage 给父组件 CommitPage 调用
defineExpose({ saveRecentMessage })
</script>
<template>
  <div class="shrink-0 px-4 py-3 border-t border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800">
    <!-- 最近使用日志下拉 -->
    <div v-if="recentMessages.length > 0" class="mb-2">
      <el-select v-model="selectedRecentMsg" :placeholder="t('file.recentMessages')" size="small" class="!w-full" @change="selectRecent">
        <el-option v-for="msg in recentMessages" :key="msg" :label="msg.slice(0, 60) + (msg.length > 60 ? '...' : '')" :value="msg" />
      </el-select>
    </div>
    <textarea :value="model" @input="model = ($event.target as HTMLTextAreaElement).value"
      :placeholder="t('file.commitPlaceholder')"
      class="w-full min-h-[80px] max-h-[200px] p-3 text-xs font-mono rounded-md border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-800 text-slate-800 dark:text-slate-200 resize-y focus:outline-none focus:ring-2 focus:ring-green-500/30 focus:border-green-500 transition-colors"
      :class="model.split('\n').some(l => l.length > 72) ? '!border-amber-400' : ''" />
    <div class="mt-1 flex items-center justify-between text-xs">
      <span class="text-slate-400">{{ t('file.charCount', { count: charCount }) }}</span>
    </div>
  </div>
</template>
```

**提交流程（CommitPage.handleCommit）：**
1. 选中文件数 > 0 检查 → 否则 return
2. 提交信息为空 → ElMessageBox.confirm 二次确认
   > **设计分歧标注：** 业务设计 §5.4 要求"提交信息为空时【提交】按钮禁用"，而交互设计 §4.3 要求"空信息弹出确认弹窗"。本步骤按照交互设计的确认方案处理（ElMessageBox.confirm）。如果后续需要改为禁用方案，需将此处二次确认逻辑移除，并在 CommitPage 模板的提交按钮 `:disabled` 条件中增加 `!commitMessage.value.trim()` 判断。
3. svnStore.commit() → 成功：清除双选 + ElMessage.success + 路由跳转
4. 失败：红色错误提示条

**i18n 补充：**
```json
{ "file": { "emptyCommitConfirm": "提交信息为空，是否继续？", "commitSuccess": "提交成功 (r{rev})" }, "common": { "submitting": "提交中..." } }
```

## 验收标准

- [ ] 类型检查通过，路由正常
- [ ] 四列（复选框/状态/文件/大小），进入时继承选中态
- [ ] 面板内修改不影响主视图，提交后双清
- [ ] 无文件选中时[提交]禁用 + tooltip
- [ ] 空信息时弹出确认
- [ ] textarea 80px~200px 可拉伸，72字符超长 amber 警告
- [ ] 提交中按钮 loading，失败红色提示条
- [ ] 支持最近使用日志下拉（onMounted 从 tauri-plugin-store 读取最近 20 条已持久化的 message，点击填充 textarea；提交成功后通过 saveRecentMessage 将当前 message 写入 tauri-plugin-store）
- [ ] 无 `<style>` 标签
