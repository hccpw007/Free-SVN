# 3.2 核心写操作命令（checkout/update/commit/file_ops/ignore）

> 对应开发计划 v2 批次 3B：核心写操作——checkout(检出)、update(更新)、commit(提交)、file_ops(增删改解)、ignore(忽略)
> 前置状态：阶段二已完成，步骤 3.1 已完成（只读命令可作为参考模式）

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src-tauri/src/commands/checkout.rs` | **新建** — checkout_repo（长操作可取消）|
| `src-tauri/src/commands/update.rs` | **新建** — update_workspace（长操作可取消）|
| `src-tauri/src/commands/commit.rs` | **新建** — create_commit（原子不可取消）|
| `src-tauri/src/commands/file_ops.rs` | **新建** — add_files/delete_files/revert_files/resolve_conflict |
| `src-tauri/src/commands/ignore.rs` | **新建** — set_ignore |
| `src-tauri/src/commands/mod.rs` | **修改** — 追加 5 个模块声明 |

## 操作细节

### 3.2A — 新建 `src-tauri/src/commands/checkout.rs`

```rust
use serde::Deserialize;
use crate::models::error::AppError;
use crate::svn;
use tauri::AppHandle;
use tauri::Emitter;

#[derive(Debug, Deserialize)]
pub struct CheckoutParams {
    pub url: String,
    pub target_path: String,
    pub depth: Option<String>,
    pub ignore_externals: Option<bool>,
    /// v5 新增：认证凭据（可选，不传则走 --non-interactive 系统缓存）
    pub credentials: Option<crate::svn::types::SvnCredentials>,
}

/// 从仓库检出（长操作，可取消，推送进度事件）
#[tauri::command]
pub async fn checkout_repo(
    app_handle: AppHandle,
    params: CheckoutParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    if params.url.is_empty() {
        return Err(AppError::InvalidInput("repository URL cannot be empty".into()));
    }
    if params.target_path.is_empty() {
        return Err(AppError::InvalidInput("target path cannot be empty".into()));
    }
    let valid = ["svn://", "https://", "svn+ssh://", "http://"];
    if !valid.iter().any(|s| params.url.starts_with(s)) {
        return Err(AppError::InvalidInput("URL must start with svn:// https:// or svn+ssh://".into()));
    }

    svn::executor::check_network(&params.url).await?;
    state.try_lock()?;

    app_handle.emit("operation:started", serde_json::json!({
        "operation": "checkout"
    })).ok();

    // BASE_SVN_ARGS: 所有服务端操作统一追加 --non-interactive 和 --trust-server-cert-failures=unknown-ca
    // --non-interactive 在 --trust-server-cert-failures 之前传递，确保 svn CLI 不弹出交互式提示
    // 参数传递时序：--non-interactive 必须在 --trust-server-cert-failures 之前，否则 svn 会忽略证书信任策略
    let mut args = vec![
        "checkout".to_string(),
        "--non-interactive".to_string(),
        "--trust-server-cert-failures=unknown-ca".to_string(),
    ];
    args.push(params.url.clone());
    args.push(params.target_path.clone());
    if let Some(ref depth) = params.depth {
        if !depth.is_empty() {
            args.push("--depth".to_string()); args.push(depth.clone());
        }
    }
    if params.ignore_externals.unwrap_or(false) {
        args.push("--ignore-externals".to_string());
    }

    let result = svn::executor::run_svn(
        &args.iter().map(String::as_str).collect::<Vec<&str>>(),
        ".",
        params.credentials.as_ref(),
    ).await;

    match &result {
        Ok(output) => {
            let rev = output.lines()
                .find(|l| l.contains("revision"))
                .and_then(|l| l.split_whitespace().last())
                .and_then(|s| s.trim_end_matches('.').parse::<u64>().ok())
                .unwrap_or(0);
            app_handle.emit("operation:completed", serde_json::json!({
                "result": "success", "detail": format!("Checked out revision {}", rev)
            })).ok();
        }
        Err(e) => {
            app_handle.emit("operation:error", serde_json::json!({
                "errorCode": format!("{}", e), "message": format!("{}", e)
            })).ok();
        }
    }

    state.unlock();
    result
}
```

### 3.2B — 新建 `src-tauri/src/commands/update.rs`

```rust
use serde::Deserialize;
use crate::models::error::AppError;
use crate::svn;
use tauri::AppHandle;
use tauri::Emitter;

#[derive(Debug, Deserialize)]
pub struct UpdateParams {
    pub path: String,
    pub revision: Option<u64>,
    pub depth: Option<String>,
    pub ignore_externals: Option<bool>,
    /// v5 新增：认证凭据（可选）
    pub credentials: Option<crate::svn::types::SvnCredentials>,
}

/// 更新工作副本（长操作，可取消，推送进度事件）
#[tauri::command]
pub async fn update_workspace(
    app_handle: AppHandle,
    params: UpdateParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    svn::executor::validate_path(&params.path)?;

    // 先获取远程 URL 检测网络（info 只读操作不需要写锁）
    let info_xml = svn::executor::run_svn(&["info", "--xml"], &params.path, None).await?;
    let info = svn::parser::parse_info(&info_xml)?;
    svn::executor::check_network(&info.url).await?;

    state.try_lock()?;

    app_handle.emit("operation:started", serde_json::json!({
        "operation": "update"
    })).ok();

    // BASE_SVN_ARGS: 追加 --non-interactive（在 --trust-server-cert-failures 之前）和证书信任参数
    // 参数传递时序必须确保 --non-interactive 在前，--trust-server-cert-failures 在后
    let mut args = vec![
        "update".to_string(),
        "--xml".to_string(),
        "--non-interactive".to_string(),
        "--trust-server-cert-failures=unknown-ca".to_string(),
    ];
    if let Some(rev) = params.revision {
        args.push("-r".to_string()); args.push(rev.to_string());
    }
    if let Some(ref depth) = params.depth {
        if !depth.is_empty() {
            args.push("--depth".to_string()); args.push(depth.clone());
        }
    }
    if params.ignore_externals.unwrap_or(false) {
        args.push("--ignore-externals".to_string());
    }
    args.push(params.path.clone());

    let result = svn::executor::run_svn(
        &args.iter().map(String::as_str).collect::<Vec<&str>>(),
        &params.path,
        params.credentials.as_ref(),
    ).await;

    match &result {
        Ok(xml) => {
            let rev = extract_update_revision(xml);
            app_handle.emit("operation:completed", serde_json::json!({
                "result": "success", "detail": format!("Updated to revision {}", rev)
            })).ok();
        }
        Err(e) => {
            app_handle.emit("operation:error", serde_json::json!({
                "errorCode": format!("{}", e), "message": format!("{}", e)
            })).ok();
        }
    }

    state.unlock();
    result
}

fn extract_update_revision(xml: &str) -> u64 {
    xml.lines()
        .find(|l| l.contains("revision=\""))
        .and_then(|l| l.split("revision=\"").nth(1))
        .and_then(|s| s.split('"').next())
        .and_then(|s| s.parse::<u64>().ok())
        .unwrap_or(0)
}
```

### 3.2C — 新建 `src-tauri/src/commands/commit.rs`

```rust
use serde::Deserialize;
use crate::models::error::AppError;
use crate::svn;
use crate::svn::types::OperationResult;

/// 提交参数（v5 新增 credentials）
#[derive(Debug, Deserialize)]
pub struct CommitParams {
    pub paths: Vec<String>,
    pub message: String,
    pub keep_locks: Option<bool>,
    pub credentials: Option<crate::svn::types::SvnCredentials>,
}

/// 提交变更（原子操作，不可取消）
#[tauri::command]
pub async fn create_commit(
    params: CommitParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<OperationResult, AppError> {
    if params.paths.is_empty() {
        return Err(AppError::InvalidInput("至少选择一个文件".into()));
    }
    svn::executor::validate_path(&params.paths[0])?;
    state.try_lock()?;

    let mut args = vec!["commit".to_string(), "--xml".to_string()];
    args.push("-m".to_string()); args.push(params.message.clone());
    if params.keep_locks.unwrap_or(false) {
        args.push("--no-unlock".to_string());
    }
    args.extend(params.paths.clone());

    let result = svn::executor::run_svn(
        &args.iter().map(String::as_str).collect::<Vec<&str>>(),
        &get_working_copy_root(&params.paths[0]),
        params.credentials.as_ref(),
    ).await;

    state.unlock();

    match result {
        Ok(xml) => {
            let rev = extract_commit_revision(&xml);
            Ok(OperationResult {
                result: "success".to_string(),
                detail: Some(format!("Committed revision {}", rev)),
            })
        }
        Err(e) => Err(e),
    }
}

/// 获取工作副本根目录（从 paths[0] 逐级向上查找 .svn 目录）
/// 避免直接使用 paths[0] 作为 cwd 导致子目录下执行 svn commit 失败
fn get_working_copy_root(path: &str) -> String {
    let mut p = std::path::PathBuf::from(path);
    loop {
        if p.join(".svn").exists() {
            return p.to_string_lossy().to_string();
        }
        if !p.pop() {
            return path.to_string();
        }
    }
}

fn extract_commit_revision(xml: &str) -> u64 {
    xml.lines()
        .find(|l| l.contains("revision=\""))
        .and_then(|l| l.split("revision=\"").nth(1))
        .and_then(|s| s.split('"').next())
        .and_then(|s| s.parse::<u64>().ok())
        .unwrap_or(0)
}
```

### 3.2D — 新建 `src-tauri/src/commands/file_ops.rs`

```rust
use serde::Deserialize;
use crate::models::error::AppError;
use crate::svn;

#[derive(Debug, Deserialize)]
pub struct FileOpsParams {
    pub paths: Vec<String>,
    pub keep_local: Option<bool>,
}

#[derive(Debug, Deserialize)]
pub struct ResolveParams {
    pub path: String,
    pub resolution: String,
}

#[tauri::command]
pub async fn add_files(
    params: FileOpsParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    if params.paths.is_empty() { return Err(AppError::InvalidInput("至少指定一个文件".into())); }
    svn::executor::validate_path(&params.paths[0])?;
    state.try_lock()?;
    let mut args = vec!["add".to_string()];
    args.extend(params.paths.clone());
    let r = svn::executor::run_svn(&args.iter().map(String::as_str).collect::<Vec<&str>>(), &params.paths[0], None).await;
    state.unlock(); r
}

#[tauri::command]
pub async fn delete_files(
    params: FileOpsParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    if params.paths.is_empty() { return Err(AppError::InvalidInput("至少指定一个文件".into())); }
    svn::executor::validate_path(&params.paths[0])?;
    state.try_lock()?;
    let mut args = vec!["delete".to_string()];
    if params.keep_local.unwrap_or(false) { args.push("--keep-local".to_string()); }
    args.extend(params.paths.clone());
    let r = svn::executor::run_svn(&args.iter().map(String::as_str).collect::<Vec<&str>>(), &params.paths[0], None).await;
    state.unlock(); r
}

#[tauri::command]
pub async fn revert_files(
    params: FileOpsParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    if params.paths.is_empty() { return Err(AppError::InvalidInput("至少指定一个文件".into())); }
    svn::executor::validate_path(&params.paths[0])?;
    state.try_lock()?;
    let mut args = vec!["revert".to_string()];
    args.extend(params.paths.clone());
    let r = svn::executor::run_svn(&args.iter().map(String::as_str).collect::<Vec<&str>>(), &params.paths[0], None).await;
    state.unlock(); r
}

#[tauri::command]
pub async fn resolve_conflict(
    params: ResolveParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    svn::executor::validate_path(&params.path)?;
    // resolution 参数校验：仅允许 mine-full/theirs-full/working 三种取值
    // mine-full   — 使用本地文件全量覆盖冲突
    // theirs-full — 使用远端文件全量覆盖冲突
    // working     — 标记已手动编辑完成冲突解决
    match params.resolution.as_str() {
        "mine-full" | "theirs-full" | "working" => {}
        _ => return Err(AppError::InvalidInput("resolution 须为 mine-full/theirs-full/working".into())),
    }
    state.try_lock()?;
    let r = svn::executor::run_svn(
        &["resolve", "--accept", &params.resolution, "--xml", &params.path],
        &params.path,
        None,
    ).await;
    state.unlock(); r
}
```

### 3.2E — 新建 `src-tauri/src/commands/ignore.rs`

```rust
use serde::Deserialize;
use crate::models::error::AppError;
use crate::svn;

#[derive(Debug, Deserialize)]
pub struct IgnoreParams {
    pub path: String,
    pub pattern: String,
}

/// 添加 svn:ignore 规则：先读取现有列表，追加去重后写回
#[tauri::command]
pub async fn set_ignore(
    params: IgnoreParams,
    state: tauri::State<'_, crate::svn::queue::SvnQueue>,
) -> Result<String, AppError> {
    svn::executor::validate_path(&params.path)?;
    state.try_lock()?;

    // 读取现有 svn:ignore
    let current = svn::executor::run_svn(
        &["propget", "svn:ignore", &params.path],
        &params.path,
        None,
    ).await.unwrap_or_default();

    // 去重后追加
    let mut lines: Vec<&str> = current.lines().collect();
    let pt = params.pattern.trim();
    if !lines.iter().any(|l| l.trim() == pt) {
        lines.push(pt);
    }

    // propset 写回（通过 stdin 传递内容）
    let result = svn::executor::run_svn(
        &["propset", "svn:ignore", &lines.join("\n"), &params.path],
        &params.path,
        None,
    ).await;

    state.unlock();
    result
}
```

### 3.2F — 修改 `src-tauri/src/commands/mod.rs`

```rust
pub mod status;
pub mod diff;
pub mod log;
pub mod checkout;
pub mod update;
pub mod commit;
pub mod file_ops;
pub mod ignore;
```

**写操作规范对照表：**

| 操作 | 长操作 | 可取消 | try_lock | check_network | 事件 |
|------|--------|--------|----------|---------------|------|
| checkout | ✅ | ✅ | ✅ | ✅ URL直检 | started/progress/completed/error |
| update | ✅ | ✅ | ✅ | ✅ info获取URL | started/progress/completed/error |
| commit | ❌ | ❌ | ✅ | ❌ executor超时处理 | ❌ |
| file_ops | ❌ | ❌ | ✅ | ❌ | ❌ |
| ignore | ❌ | ❌ | ✅ | ❌ | ❌ |

**要点：**
- 写操作统一模式：`try_lock()` → 执行 → `unlock()`。unlock 必须始终被调用（Result 的两条路径都执行 unlock）
- `state` 参数注入：通过 `tauri::State<'_, crate::svn::queue::SvnQueue>` 获取模块级单例
- `app_handle` 参数注入：通过 `tauri::AppHandle` 获取，用于 `emit()` 推送事件
- **长操作进度推送（v5 新增四事件协议）：**
  - `operation:started` — 操作开始时推送 `{"operation": "checkout|update"}`
  - `operation:progress` — 操作执行期间通过逐行读取 svn stdout（每行包含百分比前缀，如 ` svn: E...` 或 `A    file.txt`），使用 `BufReader` 逐行读取并通过 `emit("operation:progress")` 推送到前端。**MVP 阶段进度推送策略：** 当前 checkout 和 update 的实现暂返回完整的操作结果，通过单次 `operation:completed` 替代逐行推送，不实现 `operation:progress` 事件。后续增强时通过 `std::process::Command` 的 `stdout` 管道逐行读取 + 正则提取百分比（`^\s*(\d+)\s*%` 模式匹配百分比前缀），每 200ms 节流推送一次，避免高频 emit 导致前端性能问题。推送详情包含 `{"operation": "checkout", "percent": 45, "stage": "exporting", "file_count": 12}`
  - `operation:completed` — 操作成功完成时推送 `{"result": "success", "detail": "..."}`
  - `operation:error` — 操作失败时推送 `{"errorCode": "...", "message": "..."}`
  当前 checkout 和 update 的实现已包含 started/completed/error 三个事件，`progress` 事件暂不推送。MVP 阶段后端仅通过 started/completed/error 三个事件报告操作状态，`operation:progress` 逐行推送留待后续增强。后续通过 `std::process::Command` 的 `stdout` 管道 + `BufReader::read_line()` 逐行读取 + 正则提取百分比后 `emit` 推送，每 200ms 节流控制频率
- **commit cwd 增强**：使用 `get_working_copy_root()` 替代直接使用 `params.paths[0]` 作为 cwd。该函数从第一个文件路径逐级向上查找 `.svn` 目录，具体实现逻辑：从 `path` 参数开始，逐级调用 `PathBuf::pop()` 去掉最后一个路径分量，每步检查当前级目录下是否含有 `.svn` 子目录。如果找到 `.svn` 则返回该级路径作为工作副本根目录；如果一直追溯到文件系统根仍未找到，则兜底返回原始 `path` 参数。确保 `svn commit` 在工作副本根目录执行，避免子目录 cwd 导致的提交路径错误

## 测试要求

每个 command 函数必须包含 `#[cfg(test)]` 模块进行单元测试：

| 测试对象 | 测试场景 | 说明 |
|---------|---------|------|
| checkout_repo | 正常检出 / URL 格式不合法 / 网络不可用 | 验证 URL scheme 校验逻辑 |
| update_workspace | 正常更新 / 无工作副本 / 超时 | 验证 try_lock/unlock 配对 |
| create_commit | 正常提交 / 空路径列表 / 消息为空 | 验证 paths[] 和 message 参数校验 |
| file_ops 四个函数 | add / delete / revert / resolve | 验证输入参数校验和 try_lock |
| set_ignore | 新增 ignore / 追加到已有 ignore | 验证 svn:ignore 读取+去重+写入 |
| try_lock | 获取成功 / 已占用返回 OperationInProgress | 验证 SvnQueue 互斥行为 |

**测试方法：** 通过构造参数直接调用 command 函数，验证返回类型和错误处理。args 构造逻辑通过单元测试验证 `Result` 类型正确性。不依赖模拟 Tauri 运行时——纯逻辑测试。

## 验收标准

- [ ] `cargo build` 编译通过
- [ ] `checkout_repo()` 校验 URL 格式（4 种 scheme）+ check_network + try_lock + 3 种事件
- [ ] `update_workspace()` 先 info 再 check_network + try_lock + 3 种事件 + xml 解析版本号
- [ ] `create_commit()` 接收 paths[] + message + keep_locks，返回 OperationResult
- [ ] `add_files/delete_files/revert_files` 接收 paths[]，delete 支持 keep_local
- [ ] `resolve_conflict()` 校验 resolution 参数（mine-full/theirs-full/working）
- [ ] `set_ignore()` 读取现有 svn:ignore → 去重追加 → propset 写回
- [ ] 所有写操作调用 try_lock + unlock
- [ ] `commands/mod.rs` 包含全部 8 个 pub mod 声明
