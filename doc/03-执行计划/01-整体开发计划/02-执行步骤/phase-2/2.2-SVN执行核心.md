# 2.2 SVN CLI 执行核心

> 对应开发计划 v2 步骤 2.2：SVN CLI 执行核心——run_svn()、取消检测、网络检测、认证检测、路径校验
> 前置状态：步骤 2.1 已完成（AppError/RepoInfo/FileItem/SvnQueue/types 已实现）

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src-tauri/src/svn/executor.rs` | **修改** — todo!() 替换为完整实现 |

## 操作细节

### 修改 `src-tauri/src/svn/executor.rs`

将 todo!() 骨架替换为完整实现。完整代码如下：

```rust
use crate::models::error::AppError;
use log;
use std::path::PathBuf;
use std::process::{Command, Stdio};
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Mutex;
use std::time::Instant;
use tokio::task::spawn_blocking;
use tokio::time::{timeout, Duration};

/// 全局取消标志
pub static CANCELLED: AtomicBool = AtomicBool::new(false);

/// 当前 svn 子进程句柄
pub static CURRENT_CHILD: Mutex<Option<std::process::Child>> = Mutex::new(None);

const DEFAULT_TIMEOUT_SECS: u64 = 60;
const BLAME_TIMEOUT_SECS: u64 = 120;

/// 认证提示关键词
const AUTH_KEYWORDS: &[&str] = &[
    "Authentication realm",
    "Password:",
    "Username:",
    "认证领域",
    "密码:",
    "用户名:",
];

/// 基础 SVN 参数（防止交互式认证对话框弹出）
const BASE_SVN_ARGS: &[&str] = &[
    "--non-interactive",
    "--trust-server-cert-failures",
    "unknown-ca,cn-mismatch,expired,not-yet-valid,other",
];

/// 认证错误检测关键词（v5 新增）
const AUTH_ERROR_KEYWORDS: &[&str] = &[
    "Authentication realm",
    "认证领域",
    "authorization failed",
    "认证失败",
    "access denied",
    "No credentials",
    "密码不正确",
    "Password incorrect",
    "E215004",
    "E170001",
    "E175002",
];

// ── 路径与版本 ──────────────────────────────────────────

pub fn get_svn_path() -> PathBuf {
    // 开发阶段统一使用系统 svn（PATH 中的 svn 命令）
    // 阶段七打包时改为内置路径（通过全局 OnceCell 注入 resources/ 路径）
    PathBuf::from("svn")
}

pub fn get_timeout_secs(args: &[&str]) -> u64 {
    if args.iter().any(|a| *a == "blame") {
        BLAME_TIMEOUT_SECS
    } else {
        DEFAULT_TIMEOUT_SECS
    }
}

pub fn validate_path(path: &str) -> Result<&str, AppError> {
    if path.is_empty() {
        return Err(AppError::InvalidInput("路径不能为空".into()));
    }
    if path.contains('\0') {
        return Err(AppError::InvalidInput("路径包含非法字符".into()));
    }
    Ok(path)
}

// ── 取消检测 ──────────────────────────────────────────

pub fn is_cancelled() -> bool {
    CANCELLED.load(Ordering::SeqCst)
}

pub fn set_cancelled(val: bool) {
    CANCELLED.store(val, Ordering::SeqCst);
}

// ── 认证检测 ──────────────────────────────────────────

fn check_auth_hint(stderr: &str) -> bool {
    AUTH_KEYWORDS.iter().any(|kw| stderr.contains(kw))
}

// ── 网络检测 ──────────────────────────────────────────

fn extract_host(url: &str) -> Result<String, AppError> {
    let without_scheme = url
        .strip_prefix("svn://")
        .or_else(|| url.strip_prefix("svn+ssh://"))
        .or_else(|| url.strip_prefix("https://"))
        .or_else(|| url.strip_prefix("http://"))
        .ok_or_else(|| AppError::InvalidInput(format!("无法识别的 URL 格式: {}", url)))?;

    let host = without_scheme
        .split('/')
        .next()
        .ok_or_else(|| AppError::InvalidInput(format!("URL 中未找到主机名: {}", url)))?;

    Ok(host.to_string())
}

/// 检测网络是否可达（5s 超时 TcpStream 连接尝试）
pub async fn check_network(server_url: &str) -> Result<(), AppError> {
    let host = extract_host(server_url)?;

    let ports: &[u16] = if server_url.starts_with("https://") {
        &[443]
    } else if server_url.starts_with("svn+ssh://") {
        &[22]
    } else {
        &[3690, 443]
    };

    for &port in ports {
        let addr = format!("{}:{}", host, port);
        match timeout(Duration::from_secs(5), tokio::net::TcpStream::connect(&addr)).await {
            Ok(Ok(_)) => return Ok(()),
            _ => continue,
        }
    }

    log::warn!("check_network 失败: host={}", host);
    Err(AppError::NetworkUnreachable)
}

// ── 认证检测（v5 新增） ─────────────────────────────

/// 判断 stderr 是否为认证错误
fn is_auth_error(stderr: &str) -> bool {
    AUTH_ERROR_KEYWORDS.iter().any(|kw| stderr.contains(kw))
}

/// 提取 realm 信息（用于前端显示）
fn extract_realm(stderr: &str) -> String {
    for line in stderr.lines() {
        if line.contains("Authentication realm") || line.contains("认证领域") {
            return line.trim().to_string();
        }
    }
    stderr.lines().next().unwrap_or("未知认证错误").to_string()
}

// ── 核心执行（v5 增加 credentials 支持） ────────────

fn run_svn_sync(
    svn_path: &PathBuf,
    args: &[String],
    cwd: &str,
    credentials: Option<&crate::svn::types::SvnCredentials>,
) -> Result<String, AppError> {
    use std::io::Write;
    let start = Instant::now();

    // 日志记录（密码掩码）
    let log_args = if credentials.is_some() {
        let masked: Vec<String> = args.iter().map(|a| {
            if a.contains(&crate::svn::types::SvnCredentials { username: String::new(), password: String::new(), save_to_cache: false }.username) && false {
                a.clone()
            } else if a.starts_with("--password") {
                "--password [***]".to_string()
            } else if a == crate::svn::types::SvnCredentials { username: String::new(), password: String::new(), save_to_cache: false }.password {
                "[***]".to_string()
            } else {
                a.clone()
            }
        }).collect();
        masked.join(" ")
    } else {
        args.join(" ")
    };
    log::info!("svn {} (cwd: {})", log_args, cwd);

    let mut cmd = Command::new(svn_path);
    cmd.args(args).current_dir(cwd);

    let mut child = if let Some(creds) = credentials {
        // 有凭据：通过 stdin 传递密码，不捕获 stdout（--password-from-stdin 管道）
        cmd.stdin(Stdio::piped()).stdout(Stdio::piped()).stderr(Stdio::piped());
        let mut c = cmd.spawn().map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                AppError::SvnNotFound
            } else {
                AppError::Io(e)
            }
        })?;
        // 写入密码到 stdin
        if let Some(mut stdin) = c.stdin.take() {
            stdin.write_all(creds.password.as_bytes()).map_err(AppError::Io)?;
            stdin.flush().map_err(AppError::Io)?;
        }
        c
    } else {
        // 无凭据：正常执行
        cmd.stdout(Stdio::piped()).stderr(Stdio::piped());
        cmd.spawn().map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                AppError::SvnNotFound
            } else {
                AppError::Io(e)
            }
        })?
    };

    let output = child.wait_with_output().map_err(AppError::Io)?;
    let elapsed = start.elapsed();
    log::info!("svn 完成 (耗时: {:?}, 退出码: {:?})", elapsed, output.status.code());

    if output.status.success() {
        Ok(String::from_utf8_lossy(&output.stdout).to_string())
    } else {
        let stderr = String::from_utf8_lossy(&output.stderr);
        log::warn!("svn stderr: {}", stderr);

        // v5 新增：检测是否为认证错误
        if is_auth_error(&stderr) {
            let realm = extract_realm(&stderr);
            return Err(AppError::SvnAuthFailed(realm));
        }

        Err(AppError::SvnCommand(stderr.to_string()))
    }
}

/// 异步执行 SVN 命令（带超时控制 + 取消检测 + 认证检测 + 凭据支持）
/// credentials: None = --non-interactive 模式（走系统缓存），Some = 使用输入凭据
pub async fn run_svn(
    args: &[&str],
    cwd: &str,
    credentials: Option<&crate::svn::types::SvnCredentials>,
) -> Result<String, AppError> {
    if is_cancelled() {
        return Err(AppError::Cancelled);
    }

    let svn_path = get_svn_path();
    let timeout_secs = get_timeout_secs(args);

    // 构造参数：有凭据时移除 --non-interactive 并追加 --username
    let mut all_args: Vec<String> = Vec::new();

    if let Some(creds) = credentials {
        // 有凭据：移除 --non-interactive（让 svn 接受 stdin 密码并写入缓存）
        // 保留 --trust-server-cert-failures
        for base_arg in BASE_SVN_ARGS {
            if *base_arg != "--non-interactive" {
                all_args.push(base_arg.to_string());
            }
        }
        all_args.push("--username".to_string());
        all_args.push(creds.username.clone());
    } else {
        // 无凭据：保持 --non-interactive
        all_args.extend(BASE_SVN_ARGS.iter().map(|s| s.to_string()));
    }

    // 追加操作特定参数
    // 注意：--password-from-stdin 不在此处添加，由 run_svn_sync 在检测到
    // credentials 时通过 stdin 管道直接写入密码
    all_args.extend(args.iter().map(|s| s.to_string()));

    let cwd = cwd.to_string();
    let has_creds = credentials.is_some();

    let result = timeout(
        Duration::from_secs(timeout_secs),
        spawn_blocking(move || {
            run_svn_sync(&svn_path, &all_args, &cwd, None)
        }),
    )
    .await;

    match result {
        Ok(inner) => inner,
        Err(_elapsed) => {
            kill_current_process();
            log::warn!("SVN 命令超时 ({}s)", timeout_secs);
            Err(AppError::Timeout(format!("SVN 命令执行超时 ({}s)", timeout_secs)))
        }
    }
}

// ── 进程管理 ──────────────────────────────────────────

/// 终止当前 svn 子进程
pub fn kill_current_process() {
    if let Ok(mut guard) = CURRENT_CHILD.lock() {
        if let Some(mut child) = guard.take() {
            log::info!("终止 SVN 进程 pid={:?}", child.id());
            let _ = child.kill();
            let _ = child.wait();
        }
    }
}

/// 终止当前 svn 子进程并执行 cleanup
pub async fn kill_and_cleanup(cwd: &str) {
    kill_current_process();
    log::info!("执行 svn cleanup (cwd: {})", cwd);
    let _ = run_svn(&["cleanup"], cwd).await;
    set_cancelled(false);
}
```

**要点说明：**

1. **`--non-interactive`**：所有命令追加此参数防止 svn 弹出交互式认证对话框
2. **认证检测**（业务设计 §2.2）：svn 命令返回非零退出码时，`run_svn_sync` 从 stderr 中提取错误信息返回 `SvnCommand`。超时场景下不做额外认证检测（无法同时持有 child 所有权等待输出和存储在 `CURRENT_CHILD` 供取消用），统一返回 `Timeout`。如果是认证问题，svn CLI 会在超时前的 stderr 中输出认证提示，通过常规错误路径捕获
3. **网络检测**：`check_network()` 通过 `TcpStream` 5s 超时尝试连接，`status` 等本地操作不调用
4. **取消流程**：设标志 → kill 子进程 → svn cleanup → 重置标志。进程终止通过 `CURRENT_CHILD` 全局句柄实现（由 cancel command 或超时处理调用 `kill_current_process()`）
5. **路径策略**：统一使用系统 `svn`（`PathBuf::from("svn")`），内置路径延迟到阶段七打包时处理
6. **`CURRENT_CHILD` 使用说明**：当前阶段仅在 `cancel_operation` command 中用于 kill 子进程。`run_svn_sync()` 不保存 child（`Child` 未实现 Clone）。长操作的取消机制后续可在 executor 外部保存 child 句柄后调用 `kill_current_process()`

## 验收标准

- [ ] `cargo build` 编译通过，零 error
- [ ] `run_svn()` 异步执行，支持超时（默认 60s / blame 120s）
- [ ] `run_svn()` 支持可选 `credentials` 参数：有凭据时移除 `--non-interactive` 并追加 `--username`，通过 stdin 传密码
- [ ] 无凭据时保持 `--non-interactive` 走系统缓存
- [ ] svn 失败时自动检测认证错误（匹配 AUTH_ERROR_KEYWORDS），返回 `AppError::SvnAuthFailed` + realm 信息
- [ ] 超时场景统一返回 Timeout
- [ ] `check_network()` 通过 TcpStream 检测（5s 超时）
- [ ] `validate_path()` 空字符检测正确
- [ ] 取消流程完整：kill_current_process → cleanup
- [ ] `get_svn_path()` 返回 `PathBuf::from("svn")`（阶段七改为内置路径）
