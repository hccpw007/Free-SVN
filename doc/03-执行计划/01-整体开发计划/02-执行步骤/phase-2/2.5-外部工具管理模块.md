# 2.5 外部工具管理模块

> 对应开发计划 v2 步骤 2.8：外部工具管理——差异工具/合并工具调用封装、工具路径检测与回退、内置 svn 版本检测
> 前置状态：步骤 2.1 已完成（AppError 已包含 ToolNotFound 变体）

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src-tauri/src/tools/differ.rs` | **修改** — 替换 todo!() 为完整实现 |
| `src-tauri/src/tools/merger.rs` | **修改** — 替换 todo!() 为完整实现 |
| `src-tauri/src/tools/resolver.rs` | **修改** — 替换 todo!() 为完整实现 |
| `src-tauri/Cargo.toml` | **修改** — 追加 `which = "6"`（若阶段一未添加）|

## 操作细节

### 2.5A — 修改 `src-tauri/src/tools/differ.rs`

替换 todo!() 为完整实现：

```rust
use crate::models::error::AppError;
use std::process::Command;

/// 外部差异工具参数
pub struct ExternalDiffParams {
    pub file1: String,
    pub file2: String,
}

/// 打开外部差异工具。
/// 不设超时、不捕获输出、不等待进程（分离模式）
pub fn open_external_diff(tool: &str, params: ExternalDiffParams) -> Result<(), AppError> {
    let (cmd, args) = match tool {
        "vscode" => ("code", vec!["--diff", &params.file1, &params.file2]),
        "bcomp" | "beyond-compare" => ("bcomp", vec![&params.file1, &params.file2]),
        "kaleidoscope" => ("ksdiff", vec![&params.file1, &params.file2]),
        custom => (custom, vec![&params.file1, &params.file2]),
    };

    if which::which(cmd).is_err() {
        return Err(AppError::ToolNotFound(
            format!("差异工具 '{}' 未找到，请检查设置", cmd),
        ));
    }

    let child = Command::new(cmd)
        .args(&args)
        .spawn()
        .map_err(|e| AppError::ToolNotFound(format!("启动失败: {}", e)))?;

    log::info!("已打开外部差异工具: {} (pid={})", cmd, child.id());
    Ok(())
}
```

### 2.5B — 修改 `src-tauri/src/tools/merger.rs`

替换 todo!() 为完整实现：

```rust
use crate::models::error::AppError;
use std::process::Command;

/// 外部合并工具参数（mine/base/theirs/output）
pub struct ExternalMergeParams {
    pub mine: String,
    pub base: String,
    pub theirs: String,
    pub output: String,
}

/// 打开外部合并工具，传入 <mine> <base> <theirs> <output> 参数。
/// 不设超时、不捕获输出、不等待进程。
pub fn open_external_merge(tool: &str, params: ExternalMergeParams) -> Result<(), AppError> {
    let (cmd, args): (&str, Vec<&str>) = match tool {
        "vscode" => ("code", vec![&params.output]),
        "bcomp" | "beyond-compare" => {
            ("bcomp", vec![&params.mine, &params.base, &params.theirs, &params.output])
        }
        "kaleidoscope" => {
            return Err(AppError::ToolNotFound(
                "Kaleidoscope 合并模式未实现，请使用 Beyond Compare 或自定义工具".into(),
            ));
        }
        custom => (custom, vec![&params.mine, &params.base, &params.theirs, &params.output]),
    };

    if which::which(cmd).is_err() {
        return Err(AppError::ToolNotFound(
            format!("合并工具 '{}' 未找到，请检查设置", cmd),
        ));
    }

    let child = Command::new(cmd)
        .args(&args)
        .spawn()
        .map_err(|e| AppError::ToolNotFound(format!("启动失败: {}", e)))?;

    log::info!("已打开外部合并工具: {} (pid={})", cmd, child.id());
    Ok(())
}
```

### 2.5C — 修改 `src-tauri/src/tools/resolver.rs`

替换 todo!() 为完整实现：

```rust
use crate::models::error::AppError;
use std::path::PathBuf;
use std::process::Command;

/// 按名称在 PATH 中查找工具路径
pub fn resolve_tool_path(tool_name: &str) -> Option<PathBuf> {
    which::which(tool_name).ok()
}

/// 检测内置 svn 是否可执行。
///
/// ## 开发环境与生产环境行为差异
///
/// `validate_svn()` 和 `get_svn_version()` 的检测结果依赖于
/// `crate::svn::executor::get_svn_path()` 的返回值，该函数在不同阶段行为不同：
///
/// ### 开发阶段（阶段七之前）
/// `get_svn_path()` 返回 `PathBuf::from("svn")`，即从系统 PATH 中解析 svn。
/// 验证结果是"系统是否安装了 svn"，而非"内置 svn 是否可用"。
/// - macOS/Linux：通常 PATH 中有系统 svn 或 Homebrew 安装的 svn
/// - Windows：需用户手动安装 svn 客户端或通过 Scoop/Chocolatey 安装
/// - 验证通过仅表示 PATH 中有 svn，不代表内置 svn 可用
///
/// ### 生产阶段（阶段七打包后）
/// `get_svn_path()` 返回内置 svn 路径（如 `resources/svn/bin/svn`），
/// 验证结果是"内置 svn 是否可执行且版本正确"。
/// - 内置 svn 与应用一同分发，不依赖系统安装
/// - 验证通过表示用户无需手动安装 svn
/// - 验证失败（如资源文件损坏）时应提示重新安装应用
pub fn validate_svn() -> Result<(), AppError> {
    let svn_path = crate::svn::executor::get_svn_path();
    if !svn_path.exists() {
        return Err(AppError::SvnNotFound);
    }
    Command::new(&svn_path)
        .arg("--version")
        .output()
        .map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                AppError::SvnNotFound
            } else {
                AppError::Io(e)
            }
        })
        .and_then(|output| {
            if output.status.success() {
                Ok(())
            } else {
                Err(AppError::SvnNotFound)
            }
        })
}

/// 获取 svn 版本号（svn --version --quiet）。
///
/// 开发阶段返回系统 PATH 中 svn 的版本号，生产阶段返回内置 svn 的版本号。
/// 行为差异同 `validate_svn()`。详见该函数的"开发环境与生产环境行为差异"说明。
pub fn get_svn_version() -> Result<String, AppError> {
    let svn_path = crate::svn::executor::get_svn_path();
    Command::new(&svn_path)
        .args(["--version", "--quiet"])
        .output()
        .map_err(|e| {
            if e.kind() == std::io::ErrorKind::NotFound {
                AppError::SvnNotFound
            } else {
                AppError::Io(e)
            }
        })
        .and_then(|output| {
            if output.status.success() {
                Ok(String::from_utf8_lossy(&output.stdout).trim().to_string())
            } else {
                Err(AppError::SvnNotFound)
            }
        })
}
```

**要点说明：**
- 各工具模式映射与业务设计 §5.7 外部工具命令行参数表一致
- 差异/合并工具执行规范：不设超时、不捕获输出、不等待进程（分离模式）
- 工具不可用返回 `ToolNotFound`，前端显示提示并回退到内置 diff2html
- 依赖 `which = "6"` crate（确认已在 Cargo.toml 追加）

### 2.5A — 内置 svn 许可证合规声明

内置 svn（subversion）使用 Apache 2.0 许可证。分发时需在 `src-tauri/resources/` 目录中包含许可证文件：

```text
// src-tauri/resources/LICENSE-svn.txt
Subversion (svn) — Apache License, Version 2.0
http://www.apache.org/licenses/LICENSE-2.0
```

该文件在 tauri.conf.json 中通过 resources 配置包含：
```json
{
  "bundle": {
    "resources": ["resources/LICENSE-svn.txt"]
  }
}
```

首次启动时在 About 页面显示许可证信息，或通过 `--license` 命令行参数在终端输出。

## 验收标准

- [ ] `cargo build` 编译通过，零 error
- [ ] `tools/differ.rs` 支持 vscode/bcomp/kaleidoscope/自定义 四种差异工具
- [ ] `tools/merger.rs` 支持 vscode/bcomp/自定义 三种合并工具
- [ ] 外部工具调用满足：不设超时、不捕获输出、不等待进程
- [ ] 工具不可用时返回 AppError::ToolNotFound（不是 panic 或静默失败）
- [ ] `tools/resolver.rs` 实现 resolve_tool_path/validate_svn/get_svn_version
- [ ] `validate_svn()` 在 svn 不可执行时返回 SvnNotFound
- [ ] `get_svn_version()` 返回精简版本号（如 "1.14.3"）
