# 8.1-实现后端进度推送核心函数

## 涉及文件

| 操作 | 文件 |
|------|------|
| 新建 | `src-tauri/src/svn/progress.rs` |
| 修改 | `src-tauri/src/svn/mod.rs`（注册 `pub mod progress;`）|

## 操作细节

> **实际代码状态：** `run_svn_with_progress` 已实现在 `src-tauri/src/svn/progress.rs`（403 行完整实现），`svn/mod.rs` 已注册 `pub mod progress;`。以下操作细节用于**验证/补充/修改现有实现**，而非从零添加。

### 函数签名

`progress.rs` 中已存在的异步函数 `run_svn_with_progress`：

```rust
pub async fn run_svn_with_progress(
    args: &[&str],
    cwd: &str,
    credentials: Option<&crate::svn::types::SvnCredentials>,
    app_handle: tauri::AppHandle,
    operation: &str,
) -> Result<String, AppError>
```

### 双管道线程模型

在 `tokio::task::spawn_blocking` 闭包内使用两个 `std::thread::spawn` 线程分别读取 stdout 和 stderr，通过 `std::sync::mpsc::channel` 发送行数据到主处理循环：

```
spawn_blocking 外层（Tauri 异步兼容）
  ├─ stdout 线程（thread::spawn）→ mpsc::Sender<String> → 主循环（解析文件行 + emit operation:line）
  ├─ stderr 线程（thread::spawn）→ mpsc::Sender<String> → 主循环（解析百分比 + emit operation:progress，带 throttle）
  └─ 主循环（在 spawn_blocking 闭包内同步运行）
```

说明：`spawn_blocking` 确保 Tauri 异步运行时不被阻塞，内部 `thread::spawn` 实现真正的并行 I/O 读取。

### 线程逐行读取

每个线程内：
1. 获取 pipe reader（`BufReader::new(stdout.take().unwrap())`）
2. 每次 `read_line` 前检查 `CANCELLED` 标志（`Ordering::SeqCst`）
3. `catch_unwind` 包裹读取逻辑
4. 读取到行后通过 channel 发送

### 后端 Throttle（200ms）

- 首条进度行立即发送
- 后续 200ms 窗口内最多发送一条
- 使用 `Instant::now()` 时间戳比较

### 文件行解析

根据 operation 类型（checkout/update/commit/switch/merge/export）选择不同的正则：

| 操作 | 匹配模式 | 文件路径提取 |
|------|---------|------------|
| checkout | `^(A)\s+(.+)$` | 第 2 组 |
| update | `^(U|A|D)\s+(.+)$` | 第 2 组 |
| commit | `^Sending\s+(.+)$` | 第 1 组 |
| switch | `^(U|A|D)\s+(.+)$` | 第 2 组 |
| merge | `^(U|A|D|C)\s+(.+)$` | 第 2 组 |
| export | `^(A)\s+(.+)$` | 第 2 组 |

解析出 filePath 后 emit `operation:line` 事件。

> **实现说明：** 上述正则用于文档描述操作匹配逻辑。实际代码中采用更具效率的 `line.starts_with(...)` 字符前缀匹配识别操作类型，二者语义等价。

### 进度行解析

#### 百分比提取

stderr 行匹配两种模式：
- 标准进度：`^\s*(\d+)%\s` — 组 1 为百分比数值
- commit Transmitting 阶段：`^\s*\.\.\.\s(\d+)%\s` — 组 1 为百分比数值

提取百分比后填入 `OperationProgress.percent` 字段。

#### 速度数据提取

SVN 进度行完整格式示例：`45%   1234K   1.2MB/s   00:12`

在已匹配百分比行的基础上，使用以下完整正则进一步提取速度数据：

```
\s+(\d+K?)\s+([0-9.]+ (KB/s|MB/s))\s+(\S+)
```

| 分组 | 含义 | 示例 |
|------|------|------|
| 组 1 | 已传输数据量（带 K/M 后缀） | `1234K` |
| 组 2 + 组 3 | 传输速度（数值+单元） | `1.2 MB/s` |
| 组 4 | 剩余时间估算 | `00:12` |

速度数据填入 `OperationProgress.speed` 字段（格式如 `"1.2 MB/s"`），无可用数据时设为 `None`。

> **⚠️ 待修复：** 当前实际代码（progress.rs 第 279-280 行）在 emit 中使用 `"speed": ""` 和 `"elapsed": ""` 空字符串，而非 `null`。types.rs 定义 `speed: Option<String>`、`elapsed: Option<String>`，emit 应使用 `None`/`null`。需要在发送前统一处理为 Option 类型。

#### 速度数据提取实现要求

当前实际代码（progress.rs 的 `extract_percentage` 函数，第 192-210 行）**仅提取百分比**，未提取速度数据。需要在 stderr 行处理循环中补充速度提取逻辑：

```rust
/// 从 stderr 行提取速度数据的示例函数
fn extract_speed(line: &str) -> (Option<String>, Option<String>) {
    // 匹配格式: "  45%   1234K   1.2MB/s   00:12"
    // 组 1: 传输字节数 (1234K), 组 2+3: 速度 (1.2 MB/s), 组 4: 剩余时间 (00:12)
    let speed_re = regex::Regex::new(
        r"\s+(\d+K?)\s+([0-9.]+ (?:KB/s|MB/s))\s+(\S+)"
    ).ok()?;
    // ... 解析逻辑
    (Some(speed), Some(elapsed))
}
```

该函数需要在 stderr 行匹配到百分比后调用，将解析结果填入 `operation:progress` 事件 payload。

#### OperationProgress 事件载荷

`operation:progress` 事件的 payload 使用 `OperationProgress` 结构体（定义在 `src-tauri/src/svn/types.rs`），完整字段如下：

```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct OperationProgress {
    pub operation: String,            // 操作名（如 "checkout"）
    pub percent: u8,                  // 进度百分比（0 ~ 100）
    pub stage: String,                // 当前阶段（如 "Sending"、"Transmitting"）
    pub file_count: u32,              // 已处理的文件数
    pub completed_count: u32,         // 已完成文件数
    pub pending_count: u32,           // 待传输文件数
    pub speed: Option<String>,        // 传输速度（如 Some("2.3 MB/s")），无数据时为 None
    pub elapsed: Option<String>,      // 已耗时间（如 Some("00:12")），无数据时为 None
    pub current_lines: Vec<String>,   // 当前正在处理的文件路径列表
}
```

> **字段说明：** `percent` 为 `u8`（0~100 整数），`speed` 和 `elapsed` 为 `Option<String>`（无可用数据时为 `None`，前端应做空值处理）。结构体挂载 `#[serde(rename_all = "camelCase")]`，Rust 端源代码中为 `snake_case` 命名，序列化到前端时自动转为 `camelCase`（如 `completed_count` → `completedCount`）。

字段数据来源：percent + speed 来自 stderr 行解析；completed_count、pending_count 通过统计 `operation:line` 事件行数推算；elapsed 使用 `Instant::now() - start_time` 计算。

### 事件发射要求（typed struct）

> **⚠️ 待修复：** 当前实际代码（progress.rs 第 272-281 行）使用 `serde_json::json!` 手动构造 JSON payload 发射 `operation:progress` 事件，而非使用 `types.rs` 的 `OperationProgress` 结构体。

必须将 emit 改为使用 typed struct 序列化，确保：

1. **类型一致**：使用 `types.rs` 的 `OperationProgress` 结构体序列化，而非手动 JSON
2. **字段完整**：emit payload 必须包含全部 `OperationProgress` 字段（含 `currentLines`）
3. **speed/elapsed 类型正确**：`Option<String>` 应序列化为 `null` 而非空字符串
4. **三方一致**：types.rs / progress.rs emit / svn.ts interface 三者字段和类型一致

```rust
// ❌ 当前代码（手动构造 JSON，容易遗漏字段和类型错误）
ah.emit("operation:progress", serde_json::json!({
    "operation": ...,
    "percent": pct,
    "stage": "processing",     // 硬编码
    "speed": "",               // 应为 null
    // 缺少 currentLines
})).ok();

// ✅ 应采用的方式（使用 typed struct 序列化）
let progress = OperationProgress {
    operation: operation_owned.clone(),
    percent: pct,
    stage: stage_string,
    file_count,
    completed_count,
    pending_count: 0,
    speed: speed_str,          // Option<String>，无数据时为 None
    elapsed: elapsed_str,      // Option<String>，无数据时为 None
    current_lines: vec![],
};
ah.emit("operation:progress", &progress).ok();
```

### 取消协调

- stdout/stderr 线程每次读取一行前检查 `is_cancelled()`
- 主循环每次处理一行后检查 `is_cancelled()`
- 取消后不再 emit 新事件
- 取消事件使用 `CancelledPayload` 结构体（在 `types.rs` 中定义，含 `reason: String` 字段），通过 `operation:cancelled` 事件推送

### stdout 管道关闭标记

stdout 线程检测到 BufReader 返回 `Ok(0)`（EOF/管道关闭）时，push `"__STDOUT_CLOSED__:{operation}"` 标记行。

### panic 恢复 + 5s 超时兜底

- 每线程用 `catch_unwind` 包裹
- panic 时关闭对应 channel
- 两个 receiver 都关闭后启动 5s 超时
- 5s 后若进程未退出，`kill_current_process()`

## 验收标准

- [ ] `run_svn_with_progress` 函数存在且编译通过
- [ ] 双管道线程正确启动用于 stdout/stderr 读取
- [ ] 后端 throttle（200ms 窗口）生效
- [ ] 取消时线程正确退出，无残留
- [ ] stdout 管道关闭时推送终止标记行
- [ ] panic 恢复触发 5s 超时兜底
- [ ] `operation:started` 事件在开始 emit
- [ ] `operation:cancelled` 使用 `CancelledPayload` 结构体（含 reason 字段）
- [ ] `operation:cancelled` 事件仅在整个操作生命周期内（started 之后、completed/error 之前）有效

## 事件生命周期约束

事件生命周期（源自操作进度提示设计-v4 §5.1）：

```
operation:started → (operation:line × N + operation:progress 节流)
                  → operation:completed | operation:error | operation:cancelled
```

**约束说明：**
- `operation:cancelled` 仅在 `operation:started` 之后、`operation:completed`/`operation:error` 之前有效
- 操作完成或出错后，前端不应再监听 cancelled 事件
- `operation:line` 与 `operation:progress` 独立并发，不相互阻塞

### 文件拆分指引

当前 `progress.rs` 共 402 行，超过 Rust 后端开发规范中 `svn/*.rs` 的 **300 行警戒线**。拆分信号已触发（多个 `pub fn` 且前缀重复）。建议在后续编辑中将以下辅助函数提取到子模块：

**建议拆分方案 A（推荐）：**

```
progress.rs（主文件，降至 ~250 行）
├── run_svn_with_progress（核心函数）
│
progress/cancel.rs（新建子模块）
├── is_cancelled()
├── cleanup_and_kill()
├── set_cancelled()
└── 5s 超时兜底逻辑
```

**建议拆分方案 B：**

```
progress.rs（主文件，降至 ~200 行）
├── run_svn_with_progress（核心函数）
│
progress/parser.rs（新建子模块）
├── extract_percentage()
├── extract_speed()
└── 文件行解析逻辑
```

拆分后需更新 `svn/mod.rs` 注册 `pub mod cancel;` 或 `pub mod parser;`。引用路径从 `svn::progress::xxx` 变为 `svn::progress::cancel::xxx`（方案 A）或 `svn::progress::parser::xxx`（方案 B）。主文件降至 250 行以内。
