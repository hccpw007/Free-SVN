# 8.5-验证run_svn_with_progress已推广到所有command

## 涉及文件（验证用，无需修改）

| 操作 | 文件 | 验证内容 |
|------|------|---------|
| 验证 | `src-tauri/src/commands/update.rs` | 确认已使用 `svn::progress::run_svn_with_progress`，无重复 event emit |
| 验证 | `src-tauri/src/commands/branch_ops.rs` | 确认 3 处均使用 `run_svn_with_progress`（switch/copy/merge）|
| 验证 | `src-tauri/src/commands/cleanup.rs` | 确认 export_workspace 已使用 `run_svn_with_progress` |
| 验证 | `src-tauri/src/commands/cancel.rs` | 确认已使用 `operation:cancelled` + `CancelledPayload` |
| 验证 | `src-tauri/src/commands/checkout.rs` | 确认 checkout_repo 已使用 `run_svn_with_progress` |
| 验证 | `src-tauri/src/svn/types.rs` | 确认 `is_marker` 字段已在 `OperationLine` 中定义 |
| 验证 | `src/types/svn.ts` | 确认 `isMarker` 字段已在 `OperationLine` 接口中定义 |

## 操作细节

### 实际代码状态

所有 7 处写操作命令已全部使用 `svn::progress::run_svn_with_progress`：

| 命令 | 操作名 | 文件 | 调用位置 | 状态 |
|------|--------|------|---------|------|
| checkout_repo | "checkout" | checkout.rs:61 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| update_workspace | "update" | update.rs:53 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| switch_branch | "switch" | branch_ops.rs:59 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| merge_branch | "merge" | branch_ops.rs:92 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| copy_branch_tag | "copy" | branch_ops.rs:123 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| export_workspace | "export" | cleanup.rs:52 | `svn::progress::run_svn_with_progress(...)` | ✅ 已完成 |
| cancel_operation | "cancel" | cancel.rs:37 | `emit("operation:cancelled", CancelledPayload{...})` | ✅ 已完成 |

### 验证项目

#### 1. update_workspace（update.rs）

- [ ] 使用的是 `svn::progress::run_svn_with_progress` 而非 `svn::executor::run_svn`
- [ ] 无手动 `emit("operation:started")` / `emit("operation:completed")` / `emit("operation:error")` 残留
- [ ] `Emitter` import 如无其他用途已移除
- [ ] cwd 参数使用 `&params.path`

**可执行验证：** `grep -n "run_svn_with_progress\|emit.*operation:\|use.*Executor\|use.*Emitter" src-tauri/src/commands/update.rs`

#### 2. branch_ops.rs（switch/copy/merge）

- [ ] `switch_branch` 使用 `run_svn_with_progress(args, path, creds, app_handle, "switch")`
- [ ] `copy_branch_tag` 使用 `run_svn_with_progress(args, path, creds, app_handle, "copy")`
- [ ] `merge_branch` 使用 `run_svn_with_progress(args, path, creds, app_handle, "merge")`
- [ ] AppHandle 在调用后如果有后续使用，已正确 clone

**可执行验证：** `grep -n "run_svn_with_progress" src-tauri/src/commands/branch_ops.rs`

#### 3. cleanup.rs（export_workspace）

- [ ] `export_workspace` 使用 `run_svn_with_progress(args, path, creds, app_handle, "export")`

**可执行验证：** `grep -n "run_svn_with_progress" src-tauri/src/commands/cleanup.rs`

#### 4. cancel.rs（cancel_operation）

- [ ] 取消完成后 emit `operation:cancelled` 事件（使用 `CancelledPayload` 结构体）
- [ ] 不再 emit `operation:completed` + `result:"cancelled"` 的旧模式
- [ ] **确认取消路径下无 `emit("operation:completed", ...)` 残留** — 使用 `grep -rn "emit.*operation:completed" src-tauri/src/commands/cancel.rs src-tauri/src/svn/progress.rs` 确认取消路径下没有误发射 `operation:completed` 事件
- [ ] cancel 流程完整：set_cancelled(true) → kill → cleanup → set_cancelled(false) → unlock → emit cancelled

#### 5. 事件协议验证

- [ ] `operation:started` 在 progress.rs 的 run_svn_with_progress 入口处已 emit
- [ ] `operation:completed` 在有 `success` result 时已 emit
- [ ] `operation:error` 在超时或错误时已 emit
- [ ] `operation:cancelled` 已在 cancel.rs 中正确 emit
- [ ] 事件生命周期符合设计文档 v4 §5.1 顺序约束

**可执行验证：** `grep -n "emit.*operation:" src-tauri/src/svn/progress.rs` — 确认包含 started/completed/error 三种事件

### 已存在且不需要修改的功能

以下字段/功能已在前序开发中完成：

**is_marker 字段：**
- `src-tauri/src/svn/types.rs`：`OperationLine` 已包含 `#[serde(default)] pub is_marker: bool`
- `src/types/svn.ts`：`OperationLine` 接口已包含 `isMarker?: boolean`
- `src-tauri/src/svn/progress.rs`：stdout 关闭标记 emit 已使用 `"isMarker": true`（第 237 行）

**create_commit 的可选状态（此阶段不做改造）：**
- commit 为原子操作，Sending/Transmitting 阶段信息量有限（文件数目少）
- 设计文档 v4 §5.2 明确将其列为**可选阶段**

### 前端监听验证

`src/stores/svnEvents.ts` 中的事件监听配置应验证：

- [ ] 已注册 `operation:cancelled` 监听（含旧模式 `operation:completed` + `result:"cancelled"` 的清理）
- [ ] 已注册 `operation:progress` 监听
- [ ] 已注册 `operation:line` 监听（push 到 fileLines 数组）
- [ ] 已注册 `operation:completed` / `operation:error` 监听（重置 isOperationRunning）

**可执行验证：** `grep -n "listen(" src/stores/svnEvents.ts` — 确认全部 5 个事件监听已注册

## 验收标准

- [ ] update_workspace 调用 run_svn_with_progress，无重复 event emit ✅（实际已完成）
- [ ] switch_branch 调用 run_svn_with_progress ✅（实际已完成）
- [ ] merge_branch 调用 run_svn_with_progress ✅（实际已完成）
- [ ] copy_branch_tag 调用 run_svn_with_progress ✅（实际已完成）
- [ ] export_workspace 调用 run_svn_with_progress ✅（实际已完成）
- [ ] cancel 已 emit `operation:cancelled` + `CancelledPayload` ✅（实际已完成）
- [ ] OperationLine 已包含 is_marker/isMarker 字段（Rust + TS 两侧同步）✅
- [ ] stdout 关闭标记使用 isMarker 字段 ✅
- [ ] 事件生命周期完整：started → (line+progress) → completed/error/cancelled
- [ ] 全部命令编译通过（`cargo build`）
