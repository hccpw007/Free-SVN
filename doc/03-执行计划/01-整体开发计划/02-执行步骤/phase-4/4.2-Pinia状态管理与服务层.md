# 4.2 Pinia 状态管理与服务层

> 对应开发计划 v2 步骤 4.2：4 个 Pinia Store（workspace/fileList/settings/svn）+ services/svn.ts 封装
> 前置状态：步骤 4.1 已完成（全部类型定义已就绪）

## 涉及文件

| 文件路径 | 操作类型 |
|---------|---------|
| `src/stores/workspace.ts` | **新建** — 工作副本状态 |
| `src/stores/fileList.ts` | **新建** — 文件列表状态（含 status 缓存策略）|
| `src/stores/settings.ts` | **新建** — 设置状态 |
| `src/stores/svn.ts` | **新建** — SVN 操作封装 store |
| `src/services/svn.ts` | **新建** — Tauri invoke 封装层 |

## 操作细节

### 4.2A — 新建 `src/stores/workspace.ts`

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { MAX_RECENT_WORKSPACES } from '@/types/workspace'

export const useWorkspaceStore = defineStore('workspace', () => {
  const currentPath = ref('')
  const recentWorkspaces = ref<string[]>([])
  const branchName = ref('')
  const currentRevision = ref(0)
  const lastCommitTime = ref('')
  const svnVersion = ref('')
  const isOffline = ref(false)
  const isLoading = ref(false)

  function reset() {
    currentPath.value = ''
    branchName.value = ''
    currentRevision.value = 0
    lastCommitTime.value = ''
    svnVersion.value = ''
    isOffline.value = false
    isLoading.value = false
  }

  function switchWorkspace(path: string) {
    reset()
    currentPath.value = path
    addRecent(path)
  }

  function addRecent(path: string) {
    const idx = recentWorkspaces.value.indexOf(path)
    if (idx >= 0) recentWorkspaces.value.splice(idx, 1)
    recentWorkspaces.value.unshift(path)
    if (recentWorkspaces.value.length > MAX_RECENT_WORKSPACES) {
      recentWorkspaces.value.pop()
    }
  }

  function removeRecent(path: string) {
    const idx = recentWorkspaces.value.indexOf(path)
    if (idx >= 0) recentWorkspaces.value.splice(idx, 1)
  }

  return {
    currentPath, recentWorkspaces, branchName, currentRevision,
    lastCommitTime, svnVersion, isOffline, isLoading,
    reset, switchWorkspace, addRecent, removeRecent,
  }
})
```

### 4.2B — 新建 `src/stores/fileList.ts`

包含 status 缓存策略。

```ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { FileItem } from '@/types/svn'

export const useFileListStore = defineStore('fileList', () => {
  const files = ref<FileItem[]>([])
  const selectedPaths = ref<Set<string>>(new Set())
  const searchQuery = ref('')
  const filterStatus = ref('all')
  const sortField = ref('status')
  const sortOrder = ref<'asc' | 'desc'>('asc')
  const isOperationRunning = ref(false)
  const lastCachedFiles = ref<FileItem[]>([])

  const filteredFiles = computed(() => {
    let result = files.value
    if (searchQuery.value) {
      const q = searchQuery.value.toLowerCase()
      result = result.filter(f => f.path.toLowerCase().includes(q))
    }
    if (filterStatus.value !== 'all') {
      const statusMap: Record<string, string[]> = {
        modified: ['M'], added: ['A', '+'],
        conflicted: ['C', '!'], deleted: ['D', '🗑'],
        unversioned: ['?'],
      }
      const ss = statusMap[filterStatus.value]
      if (ss) result = result.filter(f => ss.includes(f.status))
    }
    return result
  })

  const selectedCount = computed(() => selectedPaths.value.size)

  /** 刷新（写操作期间返回缓存） */
  async function refresh(fetchFn: () => Promise<FileItem[]>) {
    if (isOperationRunning.value) return lastCachedFiles.value
    try {
      const newFiles = await fetchFn()
      files.value = newFiles
      lastCachedFiles.value = newFiles
      return newFiles
    } catch {
      return files.value
    }
  }

  function reset() {
    files.value = []
    selectedPaths.value = new Set()
    searchQuery.value = ''
    filterStatus.value = 'all'
    sortField.value = 'status'
    sortOrder.value = 'asc'
    lastCachedFiles.value = []
  }

  function clearSelection() { selectedPaths.value = new Set() }

  function toggleSelect(path: string) {
    const set = new Set(selectedPaths.value)
    set.has(path) ? set.delete(path) : set.add(path)
    selectedPaths.value = set
  }

  function toggleSelectAll() {
    if (selectedPaths.value.size === filteredFiles.value.length) {
      selectedPaths.value = new Set()
    } else {
      selectedPaths.value = new Set(filteredFiles.value.map(f => f.path))
    }
  }

  return {
    files, selectedPaths, searchQuery, filterStatus,
    sortField, sortOrder, isOperationRunning, lastCachedFiles,
    filteredFiles, selectedCount,
    refresh, reset, clearSelection, toggleSelect, toggleSelectAll,
  }
})
```

**缓存策略**：isOperationRunning 时 refresh() 返回 lastCachedFiles（上次成功加载的快照），写操作完成后自动设置为最新数据。

### 4.2C — 新建 `src/stores/settings.ts`

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import type { AppSettings } from '@/types/settings'
import { DEFAULT_SETTINGS } from '@/types/settings'
import { invoke } from '@tauri-apps/api/core'

export const useSettingsStore = defineStore('settings', () => {
  const defaultCheckoutDir = ref(DEFAULT_SETTINGS.defaultCheckoutDir)
  const globalIgnorePattern = ref(DEFAULT_SETTINGS.globalIgnorePattern)
  const diffTool = ref(DEFAULT_SETTINGS.diffTool)
  const mergeTool = ref(DEFAULT_SETTINGS.mergeTool)
  const showUnversioned = ref(DEFAULT_SETTINGS.showUnversioned)
  const language = ref(DEFAULT_SETTINGS.language)
  const autoStart = ref(DEFAULT_SETTINGS.autoStart)
  const diffCommandTemplate = ref('')
  const mergeCommandTemplate = ref('')
  const isDirty = ref(false)

  async function load() {
    try {
      const s = await invoke<AppSettings>('load_settings')
      defaultCheckoutDir.value = s.defaultCheckoutDir
      globalIgnorePattern.value = s.globalIgnorePattern
      diffTool.value = s.diffTool
      mergeTool.value = s.mergeTool
      showUnversioned.value = s.showUnversioned
      language.value = s.language
      autoStart.value = s.autoStart
      isDirty.value = false
    } catch { /* 默认值 */ }
  }

  async function save() {
    await invoke('save_settings', {
      settings: {
        defaultCheckoutDir: defaultCheckoutDir.value,
        globalIgnorePattern: globalIgnorePattern.value,
        diffTool: diffTool.value,
        mergeTool: mergeTool.value,
        showUnversioned: showUnversioned.value,
        language: language.value,
        autoStart: autoStart.value,
      },
    })
    isDirty.value = false
  }

  return {
    defaultCheckoutDir, globalIgnorePattern,
    diffTool, mergeTool, showUnversioned, language, autoStart,
    diffCommandTemplate, mergeCommandTemplate,
    isDirty, load, save,
  }
})
```

### 4.2D — 新建 `src/stores/svn.ts`

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { invoke } from '@tauri-apps/api/core'
import { listen } from '@tauri-apps/api/event'
import { getErrorMessage } from '@/types/error-codes'
import type {
  FileItem, RepoInfo, DiffResult, OperationProgress,
  OperationResult, BlameLine, LogEntry,
} from '@/types/svn'
import type { AppSettings } from '@/types/settings'
import { useFileListStore } from './fileList'

export const useSvnStore = defineStore('svn', () => {
  const progress = ref<OperationProgress | null>(null)
  const isOperationRunning = ref(false)
  let initialized = false

  function initEventListeners() {
    if (initialized) return
    initialized = true
    listen<OperationProgress>('operation:progress', e => { progress.value = e.payload })
    listen('operation:started', () => {
      isOperationRunning.value = true
      useFileListStore().isOperationRunning = true
    })
    listen('operation:completed', () => {
      isOperationRunning.value = false; progress.value = null
      useFileListStore().isOperationRunning = false
    })
    listen('operation:error', () => {
      isOperationRunning.value = false; progress.value = null
      useFileListStore().isOperationRunning = false
    })
  }

  async function call<T>(command: string, args?: Record<string, unknown>): Promise<T> {
    try { return await invoke<T>(command, args) }
    catch (err) { throw new Error(getErrorMessage(err)) }
  }

  // ── 每个方法对应一个 Rust command，参数名 snake_case ──

  async function getStatus(path: string) { return call<FileItem[]>('get_status', { params: { path } }) }
  async function getInfo(path: string) { return call<RepoInfo>('get_info', { params: { path } }) }
  async function getDiff(path: string, revision1?: number, revision2?: number) { return call<DiffResult>('get_diff', { params: { path, revision1, revision2 } }) }
  async function getLog(path: string, limit?: number, revision?: number, search?: string) { return call<LogEntry[]>('get_log', { params: { path, limit, revision, search } }) }
  async function getBlame(path: string, revision?: number) { return call<BlameLine[]>('get_blame', { params: { path, revision } }) }
  async function checkout(url: string, targetPath: string, depth?: string, ignoreExternals?: boolean) { return call<string>('checkout_repo', { params: { url, target_path: targetPath, depth, ignore_externals: ignoreExternals } }) }
  async function update(path: string, revision?: number, depth?: string, ignoreExternals?: boolean) { return call<string>('update_workspace', { params: { path, revision, depth, ignore_externals: ignoreExternals } }) }
  async function commit(paths: string[], message: string, keepLocks?: boolean) { return call<OperationResult>('create_commit', { params: { paths, message, keep_locks: keepLocks } }) }
  async function addFiles(paths: string[]) { return call<string>('add_files', { params: { paths } }) }
  async function deleteFiles(paths: string[], keepLocal?: boolean) { return call<string>('delete_files', { params: { paths, keep_local: keepLocal } }) }
  async function revertFiles(paths: string[]) { return call<string>('revert_files', { params: { paths } }) }
  async function resolveConflict(path: string, resolution: string) { return call<string>('resolve_conflict', { params: { path, resolution } }) }
  async function setIgnore(path: string, pattern: string) { return call<string>('set_ignore', { params: { path, pattern } }) }
  async function switchBranch(path: string, targetUrl: string, ignoreAncestry?: boolean) { return call<OperationResult>('switch_branch', { params: { path, target_url: targetUrl, ignore_ancestry: ignoreAncestry } }) }
  async function copyBranchTag(srcUrl: string, dstUrl: string, message: string, revision?: number) { return call<OperationResult>('copy_branch_tag', { params: { src_url: srcUrl, dst_url: dstUrl, message, revision } }) }
  async function mergeBranch(srcUrl: string, revStart?: number, revEnd?: number, targetPath?: string) { return call<OperationResult>('merge_branch', { params: { src_url: srcUrl, rev_start: revStart, rev_end: revEnd, target_path: targetPath } }) }
  async function cleanup(path: string) { return call<string>('cleanup_workspace', { path }) }
  async function exportWorkspace(path: string, targetDir: string, revision?: number, ignoreExternals?: boolean) { return call<string>('export_workspace', { params: { path, target_dir: targetDir, revision, ignore_externals: ignoreExternals } }) }
  async function lockFiles(paths: string[], message?: string) { return call<string>('lock_files', { params: { paths, message } }) }
  async function unlockFiles(paths: string[]) { return call<string>('unlock_files', { params: { paths } }) }
  async function relocate(path: string, fromUrl: string, toUrl: string) { return call<string>('relocate_repo', { params: { path, from_url: fromUrl, to_url: toUrl } }) }
  async function propertyOps(path: string, propName?: string, action?: string, propValue?: string) { return call<string>('property_ops', { path, prop_name: propName, action, prop_value: propValue }) }
  async function cancelOperation() { return call<string>('cancel_operation') }
  async function getLogs() { return call<string>('get_logs') }
  async function exportLogs(targetPath: string) { return call<void>('export_logs', { target_path: targetPath }) }
  async function loadSettings() { return call<AppSettings>('load_settings') }
  async function saveSettings(settings: AppSettings) { return call<void>('save_settings', { settings }) }

  return {
    progress, isOperationRunning, initEventListeners,
    getStatus, getInfo, getDiff, getLog, getBlame,
    checkout, update, commit, addFiles, deleteFiles,
    revertFiles, resolveConflict, setIgnore,
    switchBranch, copyBranchTag, mergeBranch,
    cleanup, exportWorkspace, lockFiles, unlockFiles,
    relocate, propertyOps,
    cancelOperation, getLogs, exportLogs,
    loadSettings, saveSettings,
  }
})
```

### 4.2E — 新建 `src/services/svn.ts`

```ts
import { invoke } from '@tauri-apps/api/core'
import type { FileItem, RepoInfo, DiffResult, LogEntry, BlameLine } from '@/types/svn'

export async function fetchStatus(path: string): Promise<FileItem[]> {
  return invoke('get_status', { params: { path } })
}
export async function fetchInfo(path: string): Promise<RepoInfo> {
  return invoke('get_info', { params: { path } })
}
export async function fetchDiff(path: string, r1?: number, r2?: number): Promise<DiffResult> {
  return invoke('get_diff', { params: { path, revision1: r1, revision2: r2 } })
}
export async function fetchLog(path: string, limit?: number, rev?: number, search?: string): Promise<LogEntry[]> {
  return invoke('get_log', { params: { path, limit, revision: rev, search } })
}
export async function fetchBlame(path: string, rev?: number): Promise<BlameLine[]> {
  return invoke('get_blame', { params: { path, revision: rev } })
}
export async function cancelOperation(): Promise<string> {
  return invoke('cancel_operation')
}
```

**要点说明：**
- `stores/svn.ts` 带状态管理（isOperationRunning + progress 事件监听），是组件的主要调用入口
- `services/svn.ts` 纯 invoke 封装，无状态，供不需要事件监听的场景使用
- 所有 invoke 参数使用 snake_case 键名（与 Rust command 的 Deserialize 字段对齐）
- `call<T>()` 统一 try-catch，通过 `getErrorMessage()` 转换为用户可读文字

## 验收标准

- [ ] `cnpm run lint` 类型检查通过
- [ ] workspace store: 8 state + reset/switchWorkspace/addRecent/removeRecent + 上限 20
- [ ] fileList store: files/selectedPaths/searchQuery/filterStatus/sortField/sortOrder/isOperationRunning + 缓存策略（lastCachedFiles + refresh 判断）
- [ ] settings store: 8 字段 + load/save/isDirty
- [ ] svn store: 全部 25 个 invoke 方法 + initEventListeners + 统一错误处理
- [ ] services/svn.ts: 6 个纯 invoke 函数（fetchStatus/fetchInfo/fetchDiff/fetchLog/fetchBlame/cancelOperation）
- [ ] 所有 invoke 传参使用 snake_case 键名
