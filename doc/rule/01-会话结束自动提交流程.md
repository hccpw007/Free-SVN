# 01-会话结束自动提交流程

## 规则

每次对话结束前，必须执行以下命令：

```bash
git add -A
git commit -m "{本次修改的中文内容总结}"
```

## 触发条件

- 用户主动结束对话
- 问题已全部解决
- 工作告一段落
- 用户说"结束"、"好了"、"可以了"等类似语义

## 要求

- commit message **必须用中文**总结本次所有修改内容
- 直接执行，**不**额外执行 code review、lint 或其他流程
- 如果没有任何变更，则不执行 commit
