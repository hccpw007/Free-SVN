# Linux SVN Binary

放置 Linux 平台静态编译的 `svn`。

**来源建议：**
- 使用发行版包管理器安装后复制二进制（如 `apt-get install subversion`）
- 从源码静态编译：`./configure --enable-static --disable-shared`
- 或使用 musl 编译以保持最大兼容性

**版本要求：** 1.14.x（与 macOS 保持一致）
**许可证：** Apache 2.0
